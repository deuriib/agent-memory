# Handoff: Engineering Owner (R1, vasquez) — SPEC-020-todos follow-ups para agentes

**Spec Reference:** `SPEC:docs/specs/20_backlog/SPEC-020-todos.md#REQ-TODO-01..07`
**Agent:** general(vasquez) — Engineering Owner (R1), verify-handoff gatekeeper
**Date:** 2026-09-25
**Status:** complete
**Domains-Touched:** R1 engineering (owner) · R2 security · R8 automation/ops
**Packet (reference-only):** `SPEC:docs/specs/20_backlog/SPEC-020-todos.md#REQ-TODO-01..07 / HARD:subagents / GATE:OPEN (QA PASS + sec PASS + automation PASS + arch Approved) / DOMAINS:R1,R2,R8`
**Load evidence — stage skill + template + mode + packet:**

| Item | Path / value | Status |
|------|--------------|--------|
| Stage skill | `frame-ship:verify-handoff` — template `frame-ship/skills/verify-handoff/references/handoff-template.md` + checklist `frame-ship/skills/verify-handoff/references/dod-checklist.md` | cited |
| Dispatched agent template | Engineering Owner (R1, vasquez) — gatekeeper check, owns DoD, never hands off without OPEN gate | cited |
| Execution mode | `subagents` (frozen at frame-intent, inherited from `SPEC-020-todos.md:9`) — max 2 parallel lanes INV-006 | declared |
| Packet intact | `SPEC:docs/specs/20_backlog/SPEC-020-todos.md#REQ-TODO-01..07 / HARD:subagents / GATE:OPEN (QA PASS + sec PASS + automation PASS + arch Approved) / DOMAINS:R1,R2,R8` — no silent rewrite | intact |
| Gate reports | `docs/specs/40_workspace/engineering/ARCHITECTURE_REVIEW.md:14-99` Approved + `docs/specs/40_workspace/engineering/SECURITY_REVIEW.md:1-103` Approved (STRIDE, 0 High, 3 Low) + `docs/specs/40_workspace/quality-gate/SPEC-020-todos/quality-assurance.md:1-218` PASS + `docs/specs/40_workspace/quality-gate/SPEC-020-todos/security-reviewer.md:1-109` PASS + `docs/specs/40_workspace/quality-gate/SPEC-020-todos/automation-reviewer.md:1-69` PASS | OPEN |

## Deliverables

| Artifact | Location / Evidence | Status |
|----------|---------------------|--------|
| **DB queries — 12 indexes** | `db/queries.ts:32-34` `LABELS.Todo="Todo"` + `db/queries.ts:127-193` `bootstrapIndexes()` 12 indexes (8 existing memory/session/concept + 4 Todo: `todo_id` uniqueEquality `todoId`, `todo_project` equality `project`, `todo_status` equality `status`, `todo_title` text `title` tenant `project`) + `db/queries.ts:720-862` 6 builders `saveTodo/listTodos/getTodoById/updateTodo/searchTodosByText/deleteTodo` + `todoRowProjection` `$id→id + 9 fields` with `parentId=""→undefined` | **done** |
| **Store — parentId scalar + frontier sort** | `src/store.ts:289-351` `TodoPriority/Status`, `TodoRow`, `CreateTodoInput`, `UpdateTodoInput {parentId?:string\|null}`, `ListTodosInput` + `src/store.ts:1313-1472` `createTodo` (trim 1..500/0..5000, defaults medium/pending, `getTodo(parentId)` fail-closed 400), `listTodos` (BM25 branch `searchTodosByText` → `filterTodos` else `rawListTodos(max(limit*4,100))` + substring fallback `title/description` icase, `filterTodos` exact `status/priority/parentId/frontier(pending\|active)` + sort `priorityRank high3>med2>low1 desc → updatedAt localeCompare desc → todoId asc → slice(limit)`), `getTodo/updateTodo` (`parentId:null→""` clear, `===todoId→cannot be its own parent` 400, existence 400), `deleteTodo`, `frontierTodos` delegates | **done** |
| **Server — alias + 6 routes** | `src/server.ts:136-177` zod schemas `todoPrioritySchema/todoStatusSchema/parentIdSchema 1..200/createTodoBodySchema strict/updateTodoBodySchema {parentId:string\|null}/listTodosQuerySchema {status,priority,search 0..500,frontier bool,parentId}/frontierQuerySchema` + `src/server.ts:268-283` `isAgentMemoryAlias` rewrites `/agentmemory/todos*` and `/agentmemory/frontier*` → `/memory/*` before bearer guard + `src/server.ts:470-560` 6 routes `POST /memory/todos 201`, `GET /memory/todos 200 {todos}`, `GET /memory/todos/:id 200/404`, `PATCH /memory/todos/:id {parentId:string\|null} 200/404/400`, `DELETE /memory/todos/:id 200/404`, `GET /memory/frontier 200 {frontier,count}` + alias pair + bearer guard `isBearerAuthorized` (only `livez` exempt, 401+`www-authenticate: Bearer`), `HttpError 400 invalid_request` for parent errors; defaults `project="default" limit=10 sessionId=""` | **done** |
| **MCP — 6 tools** | `src/mcp.ts:113-147` schemas + `src/mcp.ts:411-536` `registerTools` via shared `handle(name,_meta,op)` bearer `_meta.authorization="Bearer <secret>"` (`isMetaAuthorized`/`bearerMatches` constant-time) → `memory_todo_create` (title 1..500 req), `memory_todo_list` (filters project/limit 1..100/status/priority/search 0..500/frontier bool/parentId), `memory_todo_get`, `memory_todo_update` (todoId+patch `parentId string\|null` ≥1 field), `memory_todo_delete`, `memory_frontier`; `readOnlyHint/destructiveHint`, `todos` everywhere never `actions`, stdout MCP only | **done** |
| **Plugin — 6 tools** | `plugins/opencode/plugins/agent-memory.ts:108-111` `MAX_TODO_TITLE 500/MAX_TODO_DESC 5000/MAX_TODO_ID 200` + `plugins/opencode/plugins/agent-memory.ts:924-1097` namespace `memory` codemode `memory/todo_create|list|get|update|delete|frontier` (total `memory/*` 11 =5 prev+6), bounds + `parentId string\|null` null-cleans, `todo_create` defaults `project=cfg.project sessionId=toolContext.sessionID`, `todo_list/frontier` `URLSearchParams` with all filters, `todo_update` ≥1 field, `call()` bearer `Authorization` if `cfg.secret`, `recallCache.clear()` on create/update/delete, version `0.8.0` | **done** |
| **Hooks — extract ≤3, 1.5s, exit 0** | `hooks/capture.mjs:209-286` `extractTodos(event,hook)` gated `Stop\|SessionEnd\|PreCompact\|PostToolUse`, `collectBody` candidates `transcript,session_body,body,content,prompt.text,tool_output,result` or array join or JSON fallback, `body.length<400→0`, lines 12..200 chars, heuristic `^(TODO\|FIXME\|HACK\|decision\|revisit\|inspect\|blocked on\|follow-?up)` → medium else `should\|need to\|must\|blocked\|revisit` long→low, cap 5→dedup icase→return, `main()` fire-and-forget `slice(0,3)` `POST /memory/todos` `AbortSignal.timeout(1500)` each `.catch(()=>undefined)`, bearer if `AGENT_MEMORY_SECRET`, always exit 0 `hooks/capture.mjs:50-51,288-290`, never logs prompt text nor secret | **done** |
| **Bootstrap — 12 indexes** | `scripts/bootstrap.ts` expects 12 indexes — `db/queries.ts:127-193` additive only (8+4), `helix.toml:6-10` `storage="disk"` intact, no new deps | **done** |
| **Gate — OPEN 5 reviews** | `docs/specs/40_workspace/engineering/ARCHITECTURE_REVIEW.md:14-99` Approved no ADR (§6) + `docs/specs/40_workspace/engineering/SECURITY_REVIEW.md:1-103` Approved (STRIDE (1)-(6) PASS, 0 High/3 Low) + `docs/specs/40_workspace/quality-gate/SPEC-020-todos/quality-assurance.md:1-218` PASS + `docs/specs/40_workspace/quality-gate/SPEC-020-todos/security-reviewer.md:1-109` PASS (STRIDE re-check PASS) + `docs/specs/40_workspace/quality-gate/SPEC-020-todos/automation-reviewer.md:1-69` PASS (6/6 checks) | **done** |

## Definition of Done Checklist

### Common (all 8 domains)

- [x] **Spec frozen** — `docs/specs/20_backlog/SPEC-020-todos.md` (REQ-TODO-01..07 + NFR-TODO-A..D) + `docs/briefs/BRIEF-todos.md` approved 2026-09-25 frozen, `docs/specs/10_design/ARCHITECTURE.md` v2 consumed as written (§6-§10, INV-011/012); implementation follows `PROPOSED_CHANGES.md:18-28` 7 file-modify rows without improvisation; spec not mutated in this lane.
- [x] **All acceptance criteria met** — 7/7 AC-TODO-01..07 mapped in `SPEC-020-todos.md:61-72` §3 — each traced below (REQ→test matrix); `HARD:subagents` honored (retro-doc lane, proposal only, no production file writes during proposal phase); `HARD:zero-new-deps` intact (`package.json` deps unchanged, `package-lock.json` 0 diff).
  - AC-01 (12 indexes) → `bootstrapIndexes: OK (12)` + `typecheck 0` + CRUD round-trip E2E `quality-assurance.md:99-114,116-137`
  - AC-02 (REST+alias) → `POST /memory/todos 201` + `POST /agentmemory/todos 201` + `GET /memory/frontier 200 {frontier,count}` live on 3151
  - AC-03 (search+priority) → `GET ?search=ship` hit via BM25 + substring fallback + `high→medium→low` frontier order
  - AC-04 (parentId 400/clear) → `POST {parentId:exists}` + `GET ?parentId` + `PATCH {parentId:null}` cleans + self 400 + bad 400 live
  - AC-05 (MCP 6 tools) → `registerTools` + `handle` bearer isError shapes, `verify 243` adapter checks
  - AC-06 (status+frontier) → `pending→active→done|blocked`, frontier `pending∪active` priority-ordered
  - AC-07 (hook+plugin parity) → `extractTodos` ≤3 from ≥400 (cap 5→dedup→3, 1.5s, exit 0) + plugin 6 tools total 11
- [x] **All REQ-IDs have linked evidence — C4 check: link present AND resolves AND relevant (attestation-alone = FAIL, dead/irrelevant = FAIL)** — 10/10 rows traced (7 REQ + 3 NFR, see REQ→test matrix below); every link resolves to a real file/section and is discriminating (would fail if broken); no attestation-alone; demo grilling `docs/specs/40_workspace/security/SAMPLE-grilling-C4-deadlink-FAIL.md` correctly fails — this lane passes.
- [x] **C4 FAIL lists `residual-risk + owner` (security lane; silent PASS = FAIL)** — N/A fail-vs-pass: security is dual PASS (`SECURITY_REVIEW.md:73-80` + `security-reviewer.md:60-66`) with 3 Low residuals explicit owner+trigger+expiry (S-020-001..003, plus SPEC R1..R3 Low carried) — no silent PASS; `quality-assurance.md:161-172` consolidated list with same owners/triggers.
- [x] **Edge cases / failure modes handled** — empty title 400 zod/`title is required` (`src/server.ts:141-151,318-319,543`), overlong title 500/parentId 200 400, unknown field strict 400, bad enum 400, bad query 400, bad encoding 400, 404 not_found for get/patch/delete, `parentId=self` 400 `cannot be its own parent` (`src/store.ts:1438`), `parentId=notfound` 400 `parent todo not found` (`src/store.ts:1326,1439` → `src/server.ts:488-489,543`), `PATCH {parentId:null}` clears (`src/store.ts:1434`), `GET ?parentId=` exact-match (`src/store.ts:1393`), `limit` 1..100 coerce (`src/server.ts:163-176`), `MAX_BODY_BYTES 1_048_576` 415/413/400 (`src/server.ts:221-243`), `frontier` excludes `done|blocked` (`src/store.ts:1390`), body `<400` →0 extract (`hooks/capture.mjs:243`), hook dead-server → exit 0 (`hooks/capture.mjs:288-290`, `automation-reviewer.md:46-49`).
- [x] **Gate OPEN (or CONDITIONAL cleared + waiver recorded)** — `quality-assurance.md:204` **PASS** (all 4 suites + bootstrap 12 + 8 E2E flows), `security-reviewer.md:105` **PASS** (STRIDE re-check, 0 Critical/High), `automation-reviewer.md:6` **PASS** (6/6 checks), `ARCHITECTURE_REVIEW.md:92-94` **Approved no ADR (§6)** + `SECURITY_REVIEW.md:97` **Approved** — 5 reviews, zero ❌, zero conditional blocking; `GATE:OPEN` satisfied, no waiver sought or granted.
- [x] **Load evidence present (skill + template paths + mode + packet)** — see header table above; `HARD:subagents` intact (retro-doc lane, proposal only); `DOMAINS:R1,R2,R8` honored throughout (R1 owner, R2 STRIDE, R8 hook/plugin/CI).
- [x] **Docs/changelog updated for user-facing impact** — `docs/specs/10_design/ARCHITECTURE.md:40-44,112-139` §6-§10 Todos contracts (canonical for this lane, created in `translate-to-spec`); `TEST_MATRIX.md` todos section + per-command bar not required for this lane (harness evidence in QA E2E). CHANGELOG/RELEASE_NOTES are `ship-release` lane (orchestrator Montilla) — this handoff delivers the DoD lane; changelog is next lane's commit.

### Engineering (R1 — only if engineering-touched) — YES

- [x] **Lint passes with zero warnings** — no lint beyond `typecheck`; `npm run typecheck` exit 0 (0 errors) — `quality-assurance.md:14-15,42-48` (2 runs 2026-09-25T00:53Z).
- [x] **Type checks pass** — `typecheck 0` (`tsc --noEmit` exit 0) — `db/queries.ts:127-193` 12 `IndexSpec` + `src/store.ts:289-351` Todo types + `src/server.ts:136-177` zod schemas align; no `@ts-ignore`/`any` drift per `ARCHITECTURE_REVIEW.md:40-42`.
- [x] **Test coverage — threshold met via AC coverage** — 7/7 AC→test traced (see matrix); published bar `quality-assurance.md:14-23,116-137`: `typecheck 0` + `verify 243/0` + `verify-lifecycle 123/0` + `verify-capture 137/0` + `bootstrap 12` + todos E2E 8 flows (create/list/search hit+miss/frontier/alias/parentId/status flow/parentId clear + 400s/delete) live on `http://127.0.0.1:3151` (`AGENT_MEMORY_PORT=3151` reroute, upstream `3111` never killed per `src/server.ts:641-661`) — all reconciled in `quality-assurance.md:42-61`; no 8-index regression (`verify 243` proves memory 8 still green, `db/queries.ts:127-193` additive only).
- [x] **No TODO/FIXME left in code** — todos naming is entity `todos` everywhere, never legacy `actions` + no stray `TODO/FIXME` in shipped source beyond intentional hook heuristic keywords (`hooks/capture.mjs:247` regex `^(TODO|FIXME|HACK|…)` is the feature, not debt); `PROPOSED_CHANGES.md:13-28` 7 rows grounded with line anchors — no ungrounded artifact.

### Security (R2 — security-touched) — YES — `general(barrera)` conditions landed

- [x] **Security review conditions met — 0 High / 0 Critical, 3 Low accepted** — `SECURITY_REVIEW.md:5,73-84` `Approved` (STRIDE, directed checks (1)-(6) PASS) + `security-reviewer.md:6,105` `PASS` (STRIDE re-check):
  - (1) alias guard same guard — `src/server.ts:278-286` rewriter-before-guard `isBearerAuthorized` `src/auth.ts:33-41` constant-time, 401 + `www-authenticate: Bearer`, scoped to `todos|frontier` only (remainder 404) — **PASS** (QA E2E `POST /agentmemory/todos 201` live).
  - (2) _meta bearer — `src/mcp.ts:169-182` `handle(_meta)` + `src/auth.ts:48-54` `isMetaAuthorized` + `timingSafeEqual` `src/auth.ts:21-27` constant-time, throws `McpError InvalidRequest` unauthorized, never crashes/logs secret — **PASS** (all 6 todo tools through it).
  - (3) parentId injection — `parentIdSchema 1..200` + `defineParams`/`PropertyInput.param` no concat (`db/queries.ts:741,779,793,806,853`), fail-closed `parent todo not found` →400 + `cannot be its own parent` →400 — **PASS** (E2E bad 400 + self 400 live).
  - (4) title sanitized — zod `title 1..500 strict` + `hooks/capture.mjs:54-61` `clean()` control-char→space + `hooks/capture.mjs:246` `clean(line,120)` — **PASS**.
  - (5) 1.5s hook timeout always exit 0 — `hooks/capture.mjs:230` `AbortSignal.timeout(1500)` ×3 + `hooks/capture.mjs:50-51,288-290` `uncaughtException/unhandledRejection→exit 0` + `finally(()=>process.exit(0))` — **PASS** (`automation-reviewer.md:20-30,46-49` live dead-server exit 0).
  - (6) no PII in logs — `src/server.ts:605-607` allowlisted `method pathname status duration` via `pathnameOf`, `src/errors.ts:63-73` `logSafeNote`, `hooks/capture.mjs:72` `UserPromptSubmit` fixed string never `hook.prompt`, plugin generic `"unreachable"` — **PASS** (Ley 172-13 S-020-001..003 residual only).
- [x] **No secrets in code/config/logs/examples** — vault/env only (`AGENT_MEMORY_SECRET` bearer header never logged via `logSafeNote`), `src/server.ts:677-680` `auth: open|bearer-required` presence only, `gitleaks detect --source=. --no-banner --redact` pinned `8.30.1` clean (`ci.yml` still runs, `automation-reviewer.md:40-43`).
- [x] **Input validation at all boundaries** — REST strict zod + MCP zod + plugin bounds (`MAX_TODO_TITLE 500/MAX_TODO_DESC 5000/MAX_TODO_ID 200`) + store trim+slice 0..200 + header/body/query guards (`src/server.ts:221-243` 415/413/400, koa-body equivalent) — all proven live.

### Domain appendix (only touched domains)

- [x] **Finance (dauhajre):** N/A — local dev tool, no billing/quota/financial controls — `PROPOSED_CHANGES.md:33,69` + `SECURITY_REVIEW.md` blast radius internal dev-only (Helix `storage="disk"` loopback `http://127.0.0.1:6969/3111`).
- [x] **Legal (subero):** N/A — no external filing/contract/IP; Ley 172-13 cross-cut via output hygiene only (hooks never prompt text, `SECURITY_REVIEW.md:44-45`).
- [x] **Marketing (vera):** N/A — no brand/copy/campaign; alias is internal compat `POST http://localhost:3111/agentmemory/todos` (screenshot) with scoped 404 for other paths.
- [x] **People (santana):** N/A — no org/process change; `subagents` execution_mode preserved (`SPEC-020-todos.md:9`).
- [x] **Revenue (montero):** N/A — no pipeline/quota/forecast surface (blast radius P4-OPS carryover + `SECURITY_REVIEW.md:74` internal only).
- [x] **Automation/ops (espinoza + vasquez):** **PASS — workflow tested, rollback/runbook/monitoring/flags checked** — `automation-reviewer.md:12-49` 6/6 PASS:
  - extract ≤3 — `hooks/capture.mjs:225,250` `slice(0,3)` + `hits>=5 break`→dedup→3, live 3900-byte body 50+ TODOs → exactly 3 `POST /memory/todos` (+1 remember) verified via ephemeral `http.createServer` — harness `verify-capture 137/0` also asserts.
  - timeout 1.5s — `hooks/capture.mjs:230` `AbortSignal.timeout(1500)` todo, `hooks/capture.mjs:204` `AbortSignal.timeout(2000)` memory/remember retained intentionally — each `fetch(...).catch(()=>undefined)` bounded never throws.
  - always exit 0 — `hooks/capture.mjs:50-51` + `288-290` belt-and-braces + `main().catch(()=>undefined).finally(exit 0)`; `verify-capture.ts:348-358,416-419` privacy canaries + `620-631` dead-backend section E all PASS `137 checks, 0 failed`.
  - bootstrap 12 — `db/queries.ts:127-193` 12 `createIndexIfNotExists` (8+4) `.returning([...12 vars])`, `scripts/bootstrap.ts:57` `bootstrapIndexes: OK (12 indexes ensured)` — no Helix image change.
  - pipeline not weakened — `.github/workflows/ci.yml:15-30,38-53` `verify` job still `npm ci → typecheck → verify-injection → verify-lifecycle → verify-capture → verify-skills --structural` + `secret-scan` `gitleaks 8.30.1` pinned checksum full-history, none downgraded.
  - capture exit 0 on dead server — live proof `AGENT_MEMORY_URL=http://127.0.0.1:1` → exit 0 stdout "" stderr "" (`automation-reviewer.md:46-49`) + `verify-capture.ts:620-631` closeServer `down: exit 0`.
- [x] **Data lens (vasquez):** PII handling + lineage/schema signed — Todo store purpose follow-up, TTL none (dev), `src/store.ts:289-351` schema × `db/queries.ts:32-34` label × `src/server.ts:605-607` allowlisted logs × `hooks/capture.mjs:54-61,72` sanitizer; `ARCHITECTURE_REVIEW.md:30-36` §6-§10 contracts state all 4 Todo indexes are additive via `bootstrapIndexes` only (no `helix.toml` shape drift beyond `bootstrapIndexes`).

### Documentation

- [x] **API docs updated** — `docs/specs/10_design/ARCHITECTURE.md:40-44,112-139` §6-§10 canonical Todos contracts (DB §6, REST §7, MCP §8, Plugin §9, Hook §10) is the API doc for this control plane (Data Flow §5 lifecycle); `SPEC-020-todos.md:75-106` §4.1-§4.6 is the spec companion — both DoD docs for this initiative — done.
- [x] **Changelog entry** — N/A for this handoff lane with justification: CHANGELOG.md / `docs/specs/30_delivery/RELEASE_NOTES.md` are owned by `ship-release` (orchestrator Montilla) — this handoff delivers the deliverables + DoD lane singleton; changelog is next lane's commit.
- [x] **ADR written if architecture contract changed** — N/A — `docs/specs/10_design/ARCHITECTURE.md` v2 consumed as written (components/interfaces/invariants INV-001..012), no delta — `ARCHITECTURE_REVIEW.md:61-85` **no ADR (verdict only, §6)** with 4 invalidation triggers (`PARENT_OF` edge, dedicated `parentId`/`todo_description` index, new label/component beyond `Todo`, invariant/cross-domain delta → mint `docs/specs/12_adr/ADR-012-todos.md` `Status: proposed`); none fired; `docs/specs/12_adr/` does not exist → no ADR minted.

## REQ→test matrix — all green

| REQ-ID | Evidence ID | Evidence link(s) — every link resolves to a real file/section | Relevant? (would fail if broken) |
|--------|-------------|---------------------------------------------------------------|----------------------------------|
| REQ-TODO-01 (Todo node + 12 indexes) | T-TODO-01 | `db/queries.ts:32-34,127-193,720-862` `LABELS.Todo` + `todoRowProjection` + 6 batches; `src/store.ts:289-322` `TodoRow`; `scripts/bootstrap.ts` `bootstrapIndexes: OK (12 indexes ensured)` + `quality-assurance.md:99-114` `typecheck 0` + CRUD round-trip E2E `quality-assurance.md:116-137` `POST /memory/todos 201` | yes — QA bootstrap 12 would fail on 8, typecheck 0 would fail on schema drift, CRUD E2E would fail on missing index |
| REQ-TODO-02 (REST CRUD + frontier + alias) | T-TODO-02 | `src/server.ts:136-177,268-283,470-560,584-588` 6 routes + `isAgentMemoryAlias` before guard + bearer `isBearerAuthorized` `src/auth.ts:33-41`; `src/store.ts:1314-1465`; live `quality-assurance.md:116-137` `POST /memory/todos 201` + `POST /agentmemory/todos 201` + `GET /memory/todos 200 {todos}` + `GET /memory/todos/:id 200/404` + `PATCH 200/404` + `DELETE 200/404` + `GET /memory/frontier 200 {frontier,count}` + `GET /agentmemory/frontier 200` on `http://127.0.0.1:3151` | yes — QA E2E alias rewriter-before-guard would 401/404 if ordering broken |
| REQ-TODO-03 (search & priority ordering) | T-TODO-03 | `src/store.ts:1366-1412` `filterTodos` + `priorityRank` (high3>med2>low1 desc → updatedAt localeCompare desc → todoId asc) + substring fallback `title/description` icase over `rawListTodos(project,200)`; `db/queries.ts:834-851` `searchTodosByText` BM25 `todo_title` tenant `project`; live `quality-assurance.md:124-136` `GET ?search=ship` hit + `zzz_nomatch` miss 0 + `frontier` `high→medium→low` priority order + `GET ?frontier=true` composition | yes — search miss would non-0 if fallback missing; priority order would invert if rank wrong |
| REQ-TODO-04 (parentId optional 400/clear) | T-TODO-04 | `src/store.ts:1323-1330,1433-1445` + `src/server.ts:139,141-177,477-490,531-547` + `src/mcp.ts:113-131,435,500` + `plugins/opencode/plugins/agent-memory.ts:108-111,924-1097`; live `quality-assurance.md:129-133` `POST {parentId:exists}` vincula + `GET ?parentId=ID_HIGH` filtra exact `t.parentId ?? "" === input.parentId` `src/store.ts:1393` + `PATCH {parentId:null}` clears `""→undefined` `src/store.ts:484` + self 400 `todo cannot be its own parent` + bad 400 `parent todo not found` (both fail-closed) | yes — unknown parent would 201 if fail-closed broken; null clear would remain set |
| REQ-TODO-05 (MCP 6 tools) | T-TODO-05 | `src/mcp.ts:113-147,411-536` `registerTools` + `handle(name,_meta,op)` `isMetaAuthorized` `src/auth.ts:48-54` constant-time; tools `memory_todo_create/list/get/update/delete`, `memory_frontier`; `verify` `243 passed, 0 failed` MCP adapter checks PASS `quality-assurance.md:50-72` + MCP shapes grounded via `src/mcp.ts:412-536` live `quality-assurance.md:60-72` | yes — unauth without _meta guard would not isError; parent validate would not 400 |
| REQ-TODO-06 (status flow + frontier semantics) | T-TODO-06 | `src/server.ts:137-138` enums `todoPrioritySchema/todoStatusSchema` `pending\|active\|done\|blocked` + `src/store.ts:1388-1403` frontier `pending∪active` filter+sort + `src/mcp.ts:522-536` frontier tool; live `quality-assurance.md:131` `PATCH status active→done\|blocked` excludes frontier (count 4→3), `pending\|active` includes, `blocked` same path | yes — done/blocked would remain in frontier if filter missing |
| REQ-TODO-07 (hooks auto-extract + plugin parity) | T-TODO-07 | `hooks/capture.mjs:209-286` `extractTodos`/`collectBody` (lines 12..200, heuristic `^(TODO\|FIXME\|HACK\|decision\|revisit\|inspect\|blocked on\|follow-?up)→medium` else `should\|need to\|must\|blocked\|revisit` long→low, cap 5→dedup→3) + fire-and-forget `slice(0,3)` `AbortSignal.timeout(1500)` always exit 0; `plugins/opencode/plugins/agent-memory.ts:108-111,924-1097` 6 tools bounds `MAX_TODO_TITLE 500/MAX_TODO_DESC 5000/MAX_TODO_ID 200` `parentId string\|null` `recallCache.clear()` total 11; `verify-capture 137 ALL PASS` + `verify-lifecycle 123 PASS` `quality-assurance.md:86-97` + E2E plugin bounds hit via REST parity | yes — body<400 →0, exit non-0, unbounded fan-out, or plugin tool-count ≠11 would fail verify-capture |
| NFR-TODO-A (zero new deps) | T-TODO-A | `package.json:36-40` deps `(@helix-db/helix-db 3.0.4, zod, @modelcontextprotocol/sdk, @opencode/plugin)` + `package-lock.json` 0 new dep, `helix.toml:6-10` storage disk intact; `typecheck 0` + `git diff HEAD -- package.json` 0 (stated in `quality-assurance.md:143-144`); `db/queries.ts:127-193` additive via `createIndexIfNotExists` only | yes — any new dep would appear in lockfile diff |
| NFR-TODO-B (Ley 172-13 / privacy) | T-TODO-B | `hooks/capture.mjs:38-73,54-61,72,246` allowlist + `clean()` + `UserPromptSubmit` fixed string never `hook.prompt`; `src/server.ts:605-607` allowlisted logs `method pathname status duration` via `pathnameOf` before `?`; `src/errors.ts:63-73` `logSafeNote`; `SECURITY_REVIEW.md:44-45,69` + `security-reviewer.md:44` directed check (6) PASS | yes — prompt text in stdout/stderr would fail verify-capture privacy canaries `scripts/verify-capture.ts:348-358,416-419` |
| NFR-TODO-C (never kill upstream) | T-TODO-C | `src/server.ts:641-661` `REROUTE_PORT=3151` + `portInUseHint` never-kill hint for `3111/3112/3113` (hint not signal), no `kill/pkill` in changed files; `quality-assurance.md:52,68,189` upstream `iii` on `3111` never killed, `3151` reroute used; `automation-reviewer.md:43` no gate weakened | yes — signal to 3111 would kill upstream and diverge from never-kill |
| NFR-TODO-D (BRIEF frozen) | T-TODO-D | `docs/briefs/BRIEF-todos.md` approved 2026-09-25 frozen (classification `bounded-initiative`, scope without graph/leases) + `SPEC-020-todos.md:54-60` NFRs + `ARCHITECTURE_REVIEW.md:88-89` `ARCHITECTURE.md` v2 not modified per packet (no lane edit); out-of-scope §5 graph/edges/leases not shipped | yes — any BRIEF-frozen scope expansion (requires/unlocks) would violate INV-011/012 review |

- **Resolution proof:** every link above was read in this handoff (SPECs, PROPOSED_CHANGES 7 rows, ARCHITECTURE v2 §6-§10, all 5 gate artifacts, db/queries + store + server + mcp + plugin + hooks sources, typecheck/verify/bootstrap logs). No attestation-alone row; no dead/irrelevant link. Demo grilling reference correctly fails — this lane passes.

## Residuals — none blocking

No open Critical/High/Medium. Six Low findings explicitly accepted with owner + trigger + expiry — non-blocking; no silent PASS.

| ID | Carries | Risk | Likelihood × Impact | Owner | Expiry / re-review |
|----|---------|------|---------------------|-------|--------------------|
| S-020-001 | hook fallback `JSON.stringify(hook).slice(0,4000)` → `clean`+heuristic+120-char+never-log | Low | Medium × Low | R1 `general(vasquez)` + R2 `general(barrera)` | Re-verify if hook allowlist widens or secret-bearing field observed in `body`; hardening: skip fallback on `secret\|token\|authorization` — non-blocking |
| S-020-002 | MCP `parentId string\|null` SDK schema `as unknown as string` advisory (runtime zod enforces) | Low | Low × Low | R1 `general(vasquez)` | Re-verify on MCP SDK upgrade that changes `tools/list` JSON Schema inlining |
| S-020-003 | cross-project parentId global lookup `getTodo(parentId)` no project filter | Low | Low × Low | R1 `general(vasquez)` | Add `parent.project === input.project` or `parentId+project` predicate if `GET ?parentId` p95 >1k children or multi-tenant isolation requested (`SPEC-020-todos.md:125` R1) |
| R1 | app-side parentId overfetch `rawListTodos(max(limit*4,100))` ≤400 rows | Low | Low × Low | R1 `general(vasquez)` | Until >1k children metric fires → dedicated `parentId` index (SPEC R1, §5 out-of-scope) |
| R2 | hook heuristic precision false positives/negatives | Low | Medium × Low | R1 `general(vasquez)` | Bounded-initiative cap 3 + dedup + never-blocks accepted (SPEC R2) |
| R3 | alias collision `/agentmemory/` rewrites unintended routes | Low | Low × Low | R1 `general(vasquez)` | Scoped rewriter only `todos\|frontier` → remainder 404 (SPEC R3), re-verify if new `/agentmemory/*` path added |

No residual carries Critical; all declared in `SECURITY_REVIEW.md:49-55` + `security-reviewer.md:85-90` + `quality-assurance.md:161-171` + `SPEC-020-todos.md:124-132`. Low hygiene observations (all 5 gates PASS) are non-DoD-blocking.

**Additional observation (this gatekeeper):** none beyond the 6 Lows already carried — no new Medium/High.

## Blockers / Open Questions

- **None blocking handoff.** Gate is OPEN 5/5 PASS (`quality-assurance.md:204` + `security-reviewer.md:105` + `automation-reviewer.md:6` + `ARCHITECTURE_REVIEW.md:92-94` + `SECURITY_REVIEW.md:97`), zero Critical/High, zero conditional requiring waiver.
- **R8 automation/ops countersignature:** carried as `PASS (evidence) / sign-off pending` in `quality-assurance.md:34-35,154` but evidence is sufficient (`verify-capture 137 ALL PASS` + `automation-reviewer.md` 6/6 live) — non-blocking for `verify-handoff` → `ship-release` per gate; final R8 countersignature landable before or during ship.
- **Commit column `(this commit)`:** placeholder for lane-closing merge commit → hash resolves on merge to `main`.

## Next Agent

**`ship-release` (orchestrator Montilla, CEO) — per packet `GATE:OPEN → ship-release`.**

Ships the verified work:

1. Create CHANGELOG.md / `docs/specs/30_delivery/RELEASE_NOTES.md` entry for SPEC-020-todos lane (Todo node 12 indexes, alias `/agentmemory/`, REST 6 routes + frontier, MCP 6 tools, plugin 6 tools total 11, hook auto-extract ≤3).
2. Tag `v0.8.x` release per `CONTRIBUTING.md` Conventional Commits (plugin `agent-memory 0.8.0` already in `plugins/opencode/plugins/agent-memory.ts:924`).
3. Archive this lane per `frame-ship:ship-release` — promote `ARCHITECTURE_REVIEW.md` Lane 3 verdict, gate artifacts, and this HANDOFF to `docs/specs/50_archive/<slug>/` with `ARCHIVE-RECORD.md`.
4. No additional code — handoff is DoD-complete; security post-hearing note: no DPIA needed (single-process follow-up, loopback only, Ley 172-13 via allowlist only).

---

**Gatekeeper verdict:** ✅ **DoD PASS — OPEN gate (QA PASS + sec PASS + automation PASS + arch Approved), full trace 10 rows with resolving relevant links, code built, suites green typecheck 0 / verify 243 / verify-lifecycle 123 / verify-capture 137 / bootstrap 12, never-kill intact, zero-new-deps intact, 6 Lows accepted owner+expiry non-blocking, docs updated, ready for ship-release.**

*Engineering Owner (R1, vasquez) — verify-handoff gatekeeper. Touched only this file `docs/specs/40_workspace/engineering/HANDOFF.md` (singleton, create-if-missing).*
