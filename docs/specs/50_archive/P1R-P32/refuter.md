# Refuter Review: P1 remainder + P3.2 (REQ-P1-2 / REQ-P1-4 / REQ-P1-5 / REQ-P3-2, docs v1.2)

**Reviewer:** review-refuter (adversarial, engineering domain, 1 subagent — own checklist only)
**Date:** 2026-09-23
**Verdict:** conditional (COND-001)

## Mission

Attempt to **falsify** the lane's claims (`591c79c`, `39fec28`, `df39d7d`, `c69636d`, `2deda68` vs `d17294b`). Success = a counterexample. Review-only: I ran `npm run typecheck` (exit 0) and `npx tsx scripts/verify-lifecycle.ts` (**86 passed, 0 failed** — reproduced independently) plus a pure-module hostile-input probe (`/tmp/opencode/refuter-pure-probe.ts`, outside the repo). No write-path suites, no Helix restart, no port 3111 contact, no repo edits outside this artifact, no PII/secrets herein.

## Attack Vectors Tried

| ID | Hypothesis | Attempt | Result |
|----|-----------|---------|--------|
| RF-001 | The substring guard can be gamed (ordering, case, variant-of-a-variant, Unicode context) into an unbounded re-merge loop or text loss | Executed `mergedContent` under: reverse ordering (B-first), UPPERCASE variant, whitespace variants, token-reorder variant (j=1, non-substring), Greek junk (token-stripped → j=1), alternating Greek-final-sigma pair ×8 saves, exact merged re-save | **Confirmed (guard holds).** Every distinct normalized form appends at most ONCE, then guards (`normalizeContent` lowercases + collapses ws, so raw append ⇒ normalized containment); sigma alternation bounded at ~2 appends (the "\n" separator preserves word-final context); exact re-save hits the rewritten dedupKey first (verify.ts §P P7, live 212/212). No unbounded loop; content never discarded |
| RF-002 | `mergeThreshold()` fails open on garbage inputs | Probed 13 env values: `"0.9 "`, `"1e-3"`, `"-0"`, `"NaN"`, `"Infinity"`, `""`, `"  "`, `"\t0.9\n"`, `"0x1"`, `"1_0"`, `".5"`, `"+0.9"`, `"0.999…9"`, `"undefined"` | **Confirmed fail-closed.** All non-parseable / ≤0 / ≥1 / non-finite → OFF (incl. `-0`); parseable-in-(0,1) → honored, exactly as CONTRACT §3 words it ("parseable in (0,1) → that threshold"). Nuance kept out of findings: extreme exponents (`1e-10`) are honored — documented semantics, no spec deviation. §G goldens cover 8 of these live in CI |
| RF-003 | `confidenceBoost` breaks total ordering (NaN / negative / ∞ inputs escape the comparator) | Probed `boost(NaN,5)=0`, `boost(0.6,NaN)=0`, `boost(-1,3)=0`, `boost(0.6,-3)=0.6`, `boost(0.6,Infinity)=0`, and `boost(NaN,0)!==boost(NaN,0)` (the comparator NaN hole) | **Confirmed safe.** clamp01 collapses every non-finite to 0 — the NaN-hole expression is `false` (no NaN escapes). `boost(0.6,Infinity)=0` is unreachable (recallCount is integer Map counts; zod `min(0).max(1)` + JSON literals block non-finite importance; `decayedImportance` clamps first). Order remains total: finite boost → createdAt DESC → unique memoryId ASC. `noteRecall` runs post-sort so a search never reorders itself |
| RF-004 | Ledger cap claim (10k) is wrong | Filled to 10k, re-noted existing key, then a new key | **Confirmed.** 10k held; existing key increments without clear; new key at cap clears (probe + verify-lifecycle §F4 goldens 23/23) |
| RF-005 | Derived importance breaks an existing caller/test that relied on the 0.5 default | Grep `0.5`/`DEFAULT_IMPORTANCE` across `src/`, `plugins/`, `scripts/`; typecheck | **Confirmed no break.** Plugin drops its client-side default (`2deda68` diff, spreads `importance` only when given); antigravity `capture.mjs` sends none → derives (intended, `hook:*` → 0.55); `verify-injection` passes explicit 0.5 and asserts preserved (verify-injection.ts:134); `verify-capture` asserts nothing stored (counting server); demo seeds explicit; REST/MCP schemas `.optional()`; typecheck exit 0 proves the type-level callers. Only residue: `store.ts:285` read-fallback `0.5` for legacy rows missing the property — a read default, no claim violated |
| RF-006 | verify.ts §O "recall-lift" restates the criterion instead of asserting it | Read every §O assertion (verify.ts:973-1165) | **Partially falsified.** Assertions are REAL (constructed exact 1/61 RRF tie asserted, baseline newer-Y order asserted, flip to X after recall asserted, ledger asymmetry 3-vs-0, stored importance still 0.6) — but the 8 ledger/tie checks run IN-PROCESS against a stub store, never through the server, while TEST_MATRIX/ROADMAP label all 22 "E2E" → **CE-003 (Low)**. Disclosed in the §O comment itself |
| RF-007 | The "boost AFTER decay" ordering has a discriminating test | `grep DECAY_LAMBDA scripts/verify.ts` → no match; §O explicitly runs decay OFF | **Falsified (no such test).** With λ off, boost-then-decay ≡ decay-then-boost, so no shipped test distinguishes the documented order (CHANGELOG/T-201 headline claim). Code today is correct (`src/search.ts:152-161`: `confidenceBoost(decayedImportance(…))`) — unproven, not wrong → **CE-002 (Low)** |
| RF-008 | Eval silently passes when qrels are empty or the corpus fails to seed | Traced every exit path in `scripts/eval.ts` | **Confirmed fail-loud.** `EVAL_QUERIES=0` → fail (l.287); any qrel `<1` or unknown docId → fail (l.289-297); seed non-201 → fail (l.213); duplicate resolved id → fail (l.544); re-seed id/dedup drift → fail (l.561/568); project count ≠ 40 → fail (l.583); smoke 0 rows ×5 → fail (l.607); R@5≤0 either mode → fail (l.650). NaN aggregate unreachable (queries validated non-empty first). All `process.exit(1)` — cannot PASS silently |
| RF-009 | Eval seed leaks into other projects' counts | Audited every remember/search/health call for a project argument; grepped suites for instance-wide counts | **Confirmed isolated.** All traffic carries `project=agent-memory-eval` (eval.ts:179, 206-252); searches map foreign memoryIds → hard fail (l.619-631); the only project-less health calls in the repo are `verify-env` status probes (401/200), no count assertions. Consolidation probe during seed is likewise project-scoped (`store.ts` `searchByText({q, project})`) — no cross-tenant merge |
| RF-010 | "R@5/R@10/MRR/nDCG computed correctly" has a test proving it | Looked for metric tests anywhere; examined the scorecard for discriminating evidence | **FALSIFIED → CE-001 (Medium).** All 15 scored queries are rank-1 in BOTH modes (SCORECARD per-query appendix) — every metric degenerates to 1.0000, so the artifact is equally consistent with a wrong implementation (constant-1 recall, MRR ignoring rank, nDCG without IDCG all produce the same numbers). Metric functions are module-private (`eval.ts:326-379`, not exported); grep across `scripts/` + `eval/` finds ZERO references outside `eval.ts` itself; verify-lifecycle's 86 = 39+§F 23+§G 24 — no metrics section. The matrix's "decoy query returns 0 → discriminates" is an out-of-band raw-REST audit, not a scored query (validateCorpus forbids qrel-less queries). My own formula review passes (recallAt/ndcgAt10/scoreQuery/aggregate are textbook-correct) — the defect is EVIDENCE, not code → COND-001 |
| RF-011 | verify-skills' hardcoded `SKILL_CONTRACTS` drifts from docs/CONTRACT.md §3 | Route-by-route diff: all 8 rows vs the §3 frozen table; all cited tools vs the 11-tool list (CONTRACT:336-338); read all 8 SKILL.md bodies for foreign routes/fields | **Confirmed no drift.** Every route exists verbatim (`/memory/smart-search`, `/search`, `/remember`, `/recap`, `/handoff`, `/forget`, `/delete`, `/lesson`, `/sessions`, `/sessions/:sessionId/memories`); all 10 cited tools ⊆ contract's 11 (`memory_health` is index-covered, legitimately outside per-skill rows); no accidental-substring pass (checked `memory_search` ⊄ `memory_smart_search`, `memory_sessions` ⊄ `memory_session_memories` — each skill cites both literally); bodies' shapes/examples match §3 (lesson origin→400, delete reason 1..1000, recap bullet format, index = 8 links + 11 tools + 12 routes exactly) |
| RF-012 | verify-skills doesn't really round-trip every skill's route behind the identity guard | Mapped SKILL_CONTRACTS routes → live flow calls; recomputed counts | **Confirmed.** All 10 distinct skill routes exercised live (verify-skills.ts:398-567); identity guard (recap→200) before the first write (l.353); cleanup 4/4 asserted (l.583-587). Counts recomputed from source: structural 73 (8×(1-9 checks) + 9 index) + live 46 = 119 — EXACT |
| RF-013 | The headline check counts are inflated/restated | Hand-recomputed every count from the diffs: §F 23 (F1 4+F2 6+F3 6+F4 7), §G 24 (7+8+4+5), §O 22, §P 38, totals 39+23+24=86, 152+22+38=212, 73+46=119 | **Confirmed exact** — every count matches to the unit; §O/§P counts recomputed check-by-check from `git show`. Independently re-ran typecheck (0) and verify-lifecycle (86/0) |
| RF-014 | Merge re-links create duplicate HAS_CONCEPT edges that corrupt graph retrieval | Read `conceptBody()` (queries.ts:189-227: `addE` unconditional on every merge) + `graphSearch` + fusion | **Confirmed safe.** `graphSearch` carries `.dedup()` and fusion is a Map keyed by memoryId (search.ts:190-207) — duplicate edges can't produce duplicate rows or inflated RRF scores. Edge-count growth mirrors documented content growth; no claim violated |
| RF-015 | Docs/version claims don't match shipped code | Diffed CONTRACT v1.2 §3/§4/§5, README counts/config row, CHANGELOG v0.5.0, ROADMAP ticks, version lockstep | **Confirmed match.** Lockstep ×5 all 0.5.0 (package.json:3, lockfile:3+9, mcp.ts:376, plugin VERSION, README badge); README/CONTRACT §5/CHANGELOG counts (212/119/86/115/73/21) match TEST_MATRIX and my recounts; `AGENT_MEMORY_MERGE_JACCARD` row states the fail-closed rule accurately; ROADMAP P1.2/1.4/1.5/P3.2 ticks carry evidence strings identical to the matrix; §3 amendments (derived importance, `consolidated`, tier-1, decay-then-boost) state exactly what the code does |
| RF-016 | Concurrent merges / cross-tenant merges lose data | Traced lock scope + probe scoping | **Confirmed documented.** Probe and merge run under the incoming key's FIFO lock, project-scoped; same-survivor concurrent append loss is a written residual (`consolidateInto` docstring) under contract RL-001 single-writer — declared, not silent |
| RF-017 | Any §O/§P assertion is a tautology (restate-not-assert) | Read every assertion in both sections | **Confirmed real assertions.** §O computes expected values from the 201-echoed concepts with a `!== 0.5` anti-self-pass guard and flips a constructed exact tie; §P asserts the literal content string `v1\nv2\nv3`, id equality across 3 saves, session set equality, 6/6 search hits, re-save `deduped:true`, count invariance. No assertion passes by construction |

## Counterexamples Found

| ID | Counterexample | Severity | Location | Evidence | Owner |
|----|----------------|----------|----------|----------|-------|
| CE-001 | REQ-P1-5 claim "R@5/R@10/MRR/nDCG computed correctly" has **no discriminating test**: the corpus yields rank-1 for all 15×2 queries, so the all-1.0000 scorecard cannot distinguish a correct metrics implementation from several plausible broken ones; metric functions are module-private and imported by no test. Mitigation available: hand-computed fixture goldens (a rank-2 hit, a missed doc, a 2-doc qrel) or narrow the claim to "formulas reviewed, degenerate-corpus evidence" | **Medium** (conditional impact — evidence/regression gap; shipped formulas verified correct by review, no runtime defect found) | `scripts/eval.ts:326-379` (no export, no test), `eval/corpus.ts:285-299` (all-qrels trivially rank-1), claim at `TEST_MATRIX.md` T-203 | `docs/benchmarks/SCORECARD.md` per-query appendix (all first ranks = 1); grep `recallAt\|ndcgAt\|reciprocal` across `scripts/`+`eval/` outside eval.ts → 0 matches; verify-lifecycle 86 = 39+23+24, no metrics section | orchestrator (execute-spec lane) |
| CE-002 | "confidenceBoost applied AFTER decay" (CHANGELOG v0.5.0, T-201) has no test that discriminates the order: verify.ts never sets `AGENT_MEMORY_DECAY_LAMBDA` and §O runs decay OFF, where both orders are equivalent. Code is correct today (`src/search.ts:154-158`) | **Low** (hygiene — unproven claim, backlog) | `scripts/verify.ts` §O (decay-OFF comment ~l.1044; tie checks l.1130-1160); missing test surface | `grep DECAY_LAMBDA scripts/verify.ts` → no matches; verify-lifecycle §F is env-free by design (l.356) | orchestrator |
| CE-003 | §O is labeled "E2E … incl. recall-lift" (T-201, ROADMAP P1.4) but 8 of its 22 checks exercise the real `bm25Search`/`hybridSearch` **in-process against a stub store** — only the stored-importance half crosses REST. Scope disclosed in the §O comment; assertions and code path are real, and server→hybridSearch composition is covered by other REST sections | **Low** (evidence-label accuracy, backlog) | `scripts/verify.ts:973-1165` (stubStore l.1088, comment l.977-991); claim at `TEST_MATRIX.md` T-201 | verify.ts §O comment: "the LEDGER WIRING and the recall-lift ORDERING run the REAL bm25Search/hybridSearch in-process against a stub store … exact-RRF-tie REST construction documented as skipped" | orchestrator |

## Verdict Rationale

- **pass** would require zero counterexamples — three claims' *evidence* does not establish the claim, so pass is not honest.
- **conditional** fits: no shipped-code defect was found — every correctness claim survived hostile inputs (guard gaming ×7 vectors, 13 garbage thresholds, NaN/∞ boosts, caller-breakage sweep, graph-duplication, cross-tenant leaks), every guard is fail-loud, every count (22/38/73/46/23/24/86/212/119) recomputes exactly, and the skills table is contract-identical — but CE-001 (Medium) is a real evidence gap in an acceptance claim.
- **fail** is reserved for a counterexample invalidating the spec — none exists; the formulas and code I could inspect are correct.

**Gating condition:**

- **COND-001** — before ship: add discriminating metric goldens (export the eval metric functions or add a hand-computed fixture covering a rank-2 hit, a missed doc, and a multi-doc qrel), **or** amend TEST_MATRIX T-203 to state the evidence honestly (formulas reviewed + degenerate rank-1 corpus, metric math unregression-tested). CE-002/CE-003 are Low, non-gating backlog (one λ-on recall golden; relabel §O as in-process integration of the real search path).

## What I could NOT falsify (claims that survived)

Substring-guard loop closure (all orderings/casings incl. Unicode), concatenation-never-discards, dedupKey rewrite → merged-text re-save dedups (§P P7 live), threshold fail-closed on every malformed input, boost/comparator determinism and total ordering, ledger cap semantics, caller-wins/derived-importance with no broken 0.5-dependent caller, eval deadman matrix (empty qrels, failed seed, zero retrieval, foreign rows — all exit 1), project isolation of the eval seed, verify-skills table + bodies vs CONTRACT §3 route-by-route, every skill's live round-trip behind the identity guard, probe4 VERDICT A gates (text+vector refresh+unthrone) as real live assertions, concept re-link safety under duplicates, version lockstep ×5, and all docs counts/ticks.
