# Risk Review: REQ-RL-001 + REQ-F-01 (residual-closure lane)

**Reviewer:** review-risk (independent gate reviewer, residual-risk lens)
**Date:** 2026-09-24
**Scope (commits, main, unpushed):** `01224cc` (REQ-RL-001 — per-survivor FIFO lock + fresh `getMemoryById` re-read under lock), `a0257d6` (REQ-F-01 — post-write verify + ONE heal + fail-closed named throw; guard-path concept heal), `fb8e661` (docs: CONTRACT v1.5 + README + CHANGELOG). Diff range: `a9ef417..HEAD`. Primary code: `src/store.ts`, `db/queries.ts`, `src/consolidate.ts`; tests `scripts/verify.ts`, `scripts/verify-lifecycle.ts`; docs `docs/CONTRACT.md` (v1.5), `TEST_MATRIX.md`, `IMPLEMENTATION_PLAN.md`, `ROADMAP.md`, `README.md`, `CHANGELOG.md`.
**Packet (accepted by reference):** SPEC:ROADMAP.md#1.3-rows-RL-001,F-01 + docs/CONTRACT.md#v1.5-amendment (§2+§3-tier-1+§5) / HARD:subagents+review-only+no-code-edits+no-push / GATE:execution-evidence-commits-01224cc,a0257d6,fb8e661-bar-green / DOMAINS:R1-engineering,risk.
**Checklist applied:** `frame-ship/skills/quality-gate/references/engineering/risk-review.md` (the packet named `…/risk.md`; the actual file is `risk-review.md` — same checklist, applied).
**Verdict:** ⚠️ **conditional** — findings: **Critical 0 · High 0 · Medium 5 · Low 4**; conditions: **COND-RK-01, COND-RK-02, COND-RK-03** (all docs-only, owner engineering/ship-release). Sibling conditions (COND-RD-01, COND-RF-01..04, COND-RS-01..03) are **inherited, not re-issued** — counted where they gate my findings.

## Rules honored (evidence environment)

Review-only: no code/script/test/doc edit except this report; no Helix restart/stop/upgrade (`:6969` observed only); upstream `:3111` untouched; live probes read-only against `http://127.0.0.1:3151`; rollback analysis via read-only `git merge-tree` (no worktree, no revert executed, repo tree untouched); no memory content, embeddings, secrets, or concept names reproduced below — statuses/counts/file:line only. This review created **zero write probes** (`health?project=review-risk` → `{"counts":{"memories":0,"sessions":0}}` before and after), so no `forget` cleanup was needed.

## Checklist

- [x] **Blast radius analysis bounded and verified** — every write flows through `remember()` (11 call sites across `src/server.ts` / `src/mcp.ts` / scripts; hooks ride REST), so the blast radius is the whole write path; verified bounded and green (RK-006): 243/243 live + 113/113 offline re-run by this reviewer, adjacent suites green in-lane, search path + routes + MCP tools + index count untouched by the diff, zero dependency change.
- [x] **Backward compatibility preserved (no breaking API/schema shifts without ADR)** — all three queries ADDITIVE (`db/queries.ts:535,572,611`), no route/MCP-tool/`RememberResult`/index change (bootstrap stays 8; no `IndexSpec` added in the diff), contract surface updated in v1.5 (`docs/CONTRACT.md:52-72,138-140`). One frozen-surface doc drift only: §2 param order (RK-008 → inherited COND-RF-04), functionally inert (named params).
- [x] **Dependencies pinned; zero critical/high CVEs or supply-chain hazards** — `git diff a9ef417..HEAD -- package.json package-lock.json` is empty: no new/changed dependency, no script-growth supply-chain surface, no install step added.
- [~] **Rollback determinism proven (revertible in production in ≤15 minutes)** — proven for the ONLY safe procedure: reverse-order whole-commit revert `fb8e661` → `a0257d6` → `01224cc` (each revert lands exactly on its parent tree, all three parents historically green: `a0257d6` 243/113, `01224cc` 227/104, `a9ef417` pre-lane green; no schema/index/route/data backout needed — additive queries become dead code, merged rows + healed links stay valid under old code; well within 15 min). **But** a partial revert of `01224cc` while keeping `a0257d6` CONFLICTS (read-only `git merge-tree --merge-base=01224cc HEAD a9ef417` → exit 1, conflicted stages in 5 files: `TEST_MATRIX.md`, `db/queries.ts`, `scripts/verify.ts`, `scripts/verify-lifecycle.ts`, `src/store.ts`), and `IMPLEMENTATION_PLAN.md:41-49` still documents that per-commit hunk-level partial rollback as if it were executable → RK-005 / COND-RK-03.
- [x] **Architectural contract invariants intact** — frozen route table / MCP tool list / `RememberResult` shape / index count 8 / fail-closed posture all unchanged; the two-lock order (dedupKey OUTER → survivor INNER) is documented at the seam (`src/store.ts:495-503`, `docs/CONTRACT.md:297-311`); `ARCHITECTURE.md` does not exist in this repo — the frozen contract `docs/CONTRACT.md` is the invariant source and was amended (v1.5), not violated.
- [~] **Zero unmitigated regression risk across touched systems** — mitigated by conditions: three cited fail-closed behaviors have NO executing test (inherited COND-RF-03 / COND-RS-03) and queue-poisoning recovery is untested (reliability F8, backlog). Residual severity of a regression in those branches is capped at **availability**: they are throw-only fail-closed paths (regression ⇒ HTTP 500, never silent corruption) — see RK-006.

## Findings

| ID | Severity | Location | Finding |
|----|----------|----------|---------|
| RK-001 | **Medium** | `ROADMAP.md:66-70` (no new rows); `docs/CONTRACT.md:297-333` (no residual declarations); `src/store.ts:1017-1026` | **This lane CREATED residuals that no dated accepted-risk ledger declares.** (a) The batch's `embedding` write escapes the F-01 verify entirely (corroborates refuter CE-001); (b) the lock-queue wait has no cap and this lane amplified in-lock hold ~3–4× (resilience finding 2); (c) three fail-closed behaviors ship untested (refuter CE-002); (d) successful heals are 100% silent (RK-004); (e) crash-window links-partial commits heal only lazily (resilience finding 4). ROADMAP §1.3 — the gate's accepted-risk inventory — contains NONE of them, while it still carries the two CLOSED rows (RK-002). The ledger therefore both **under-declares** the new risk and **over-declares** the closed one: acceptance criterion "accepted risks documented with owner, justification, expiry" is not met yet for this lane's net-new exposure. → **COND-RK-01** |
| RK-002 | **Medium** | `ROADMAP.md:68` (RL-001), `:70` (F-01), `:44` (stale counts 214/104) vs `docs/CONTRACT.md:297,312` ("CLOSED 2026-09-24"), `README.md:547+` (REMEDIATED), `CHANGELOG.md:27,37` | **Stale residual rows contradict the CLOSED verdict on the packet's own SPEC anchor.** The three docs lanes' commit `fb8e661` updated CONTRACT/README/CHANGELOG but not ROADMAP: the gate's cited SPEC rows still say "can lose one append" / "one writeBatch, unverified" in present tense with expiry **2026-12-31** — the dated re-review would fire for already-fixed residuals, and a reader trusting ROADMAP gets the wrong risk picture. Corroborates refuter CE-003 / reliability F1 / resilience finding 1. **Condition inherited: COND-RF-02 + COND-RS-01** (not re-issued). |
| RK-003 | **Medium** | "closed" claim: `docs/CONTRACT.md:312` ("ATOMICITY — CLOSED 2026-09-24"), `:325-326` ("commit is DETECTED-and-healed"), `CHANGELOG.md:37`, `README.md:582-598` vs coverage: `src/store.ts:1017-1026` (checks content/dedupKey/concepts only) · write: `db/queries.ts:675-677` (setProperty content/**embedding**/dedupKey) · read: `db/queries.ts:529` ("never `embedding`") | **"CLOSED" overclaims by exactly one property.** A mid-batch partial commit that lands content+dedupKey+links but drops the `embedding` refresh passes every invariant and leaves the vector index serving the pre-merge embedding — the exact retrieval-defect class F-01 was opened for — while all `f-01:` checks stay green. The doc enumerates its three invariants honestly, so the *implementation* matches the text; the **closure word** does not. Corroborates refuter CE-001. **Condition inherited: COND-RF-01** (close by adding an embedding invariant, or re-scope CLOSED + declare embedding residual ≤2026-12-31 / next engine upgrade). Note: CONCURRENCY "CLOSED" (`:297-311`) does NOT overclaim — it explicitly scopes SAME-PROCESS and keeps the cross-process boundary. |
| RK-004 | **Medium** | heal paths: `src/store.ts:959-991` (`ensureConceptLinks`), `:1009-1040` (`verifyMergedState`) · `grep -c "console\." src/store.ts` → **0** · log path: `src/server.ts:445`, `src/mcp.ts:144`, `src/errors.ts:63-73` · README grep "runbook/troubleshoot" → none | **Monitoring/observability gap for the new failure modes — asymmetric and undocumented.** (a) *Failures* ARE greppable: the named prefixes `REQ-F-01:` / `REQ-RL-001:` / `contract §2 fail-closed` survive `logSafeNote`'s 200-char trim (local messages kept, `src/errors.ts:71`; prefix is at message start, e.g. `src/store.ts:1037`, `:989`, `:852`) and reach both REST (`-> 500: Error: REQ-F-01: …`, `src/server.ts:445`) and MCP (`src/mcp.ts:144`) logs — client body stays detail-free `internal_error` (correct). (b) But *successful heals are invisible*: detect+heal emits no log, no counter, no signal — an operator cannot distinguish "engine never partially commits" from "engine partially committed 100 times and the app silently papered over it", which is precisely the trend F-01 exists to surface. (c) Not *actionable*: no README/CONTRACT runbook line tells an operator what to do when `-> 500: Error: REQ-F-01: post-write verify failed after one heal…` appears (grep README/CONTRACT for runbook/troubleshoot/REQ-F-01 → only limitation #11 prose). → **COND-RK-02** (default = document the posture + operator line; the heal log itself is an optional code follow-up needing its own proposal lane) |
| RK-005 | **Medium** | `IMPLEMENTATION_PLAN.md:41-49` (Rollback Points) vs read-only probes: `git merge-tree --write-tree --merge-base=01224cc HEAD a9ef417` → **exit 1, conflicts in 5 files**; `--merge-base=a0257d6 HEAD 01224cc` → exit 0; `--merge-base=fb8e661 HEAD a0257d6` → exit 0 | **Documented rollback procedure is now wrong; commits are stacked, not independent.** `01224cc` is NOT independently revertible once `a0257d6` exists (the F-01 hunks live inside the RL-001 merge body + §I harness), so PLAN's "after step 1: revert the getMemoryById hunks + §P block + §I harness" would stop an incident responder mid-conflict. Reverting `a0257d6` alone is git-clean but — with `fb8e661` kept — leaves contract/README/CHANGELOG declaring CLOSED behavior the code no longer performs (a git-clean, semantically-false state). The SAFE procedure is reverse-order full revert of all three (each step tree-equal to a historically green parent, no data backout) — fast and deterministic, but it exists only in this report until documented. → **COND-RK-03** |
| RK-006 | Low | evidence: `TEST_MATRIX.md:48-61`; re-run this review: typecheck 0, lifecycle 113/0, live verify 243/0 | **Blast radius — regression evidence assessed SUFFICIENT with named gaps.** Changed code sits exclusively on the write path (diff touches no `src/search.ts`/`src/server.ts`/`src/mcp.ts` — 0 lines); the read/degradation posture was live-confirmed untouched by siblings. Bar: `verify` 243 (superset of pre-lane 227, includes §P `rl-001:` 13 + `f-01:` block), `verify-lifecycle` 113 offline, plus in-lane adjacent suites `verify-capture` 137 / `verify-env` 21 / `verify-skills --structural` 73 / `verify-injection` ALL PASS (`TEST_MATRIX.md:61`) — capture+injection suites exercise the same `remember()` surface hooks use. Gaps (inherited/backlog, not re-gated): 3 untested fail-closed sub-paths (COND-RF-03/RS-03), queue-poisoning recovery test (reliability F8), live `verify-skills` 46 round-trips / eval / demo / probe3-4 not re-run this lane (refuter F-6). Fail-closed-by-construction caps any gap's worst case at 500s, not data loss ⇒ sufficient for a conditional pass. |
| RK-007 | Low | `src/store.ts:989` (`${missing.join(", ")}`), `:1037` (`${after.join(", ")}`) vs `src/errors.ts:12` ("Local error messages never see request content") | **Named-invariant throws embed concept names, which reach process logs** via `logSafeNote` (local messages kept verbatim ≤200 chars). Concept names are content-derived tokens (`extractConcepts`) or caller labels — declared non-PII-by-posture (DAT-001, contract v1.4 §3) and rare-path/short-trimmed, so impact is bounded; but the errors.ts comment's blanket claim is now false, and caller-supplied labels are caller-responsibility content being echoed into an operator log stream. Fix = soften the comment to "never direct request content (derived tokens excepted)" or strip names from the messages (ids + counts only). Backlog — does not gate. |
| RK-008 | Low | `docs/CONTRACT.md:140` (`memoryId, project, concepts`) vs `db/queries.ts:611-615` (`memoryId, concepts, project`) | Frozen-surface §2 param-order drift on `linkMemoryConcepts`; inert at runtime (named params), but §2 is the frozen surface. Corroborates refuter CE-004. **Condition inherited: COND-RF-04.** |
| RK-009 | Low | carried-over sibling Lows: reliability F3/F4/F5/F6/F7/F8, refuter F-5/F-6, readability RD-001..006, resilience findings 4/5 | Recorded, not re-litigated: over-merge below threshold under deep queues; expired-while-waiting no re-probe; duplicate HAS_CONCEPT edge growth; `withTimeout` no AbortController; guard path never repairs legacy keyless `dedupKey`; queue-poisoning recovery test gap; docstring drift (COND-RD-01); crash-window lazy-heal doc gap; `verify.ts` session-node accumulation with no declared run budget; TEST_MATRIX commit-hash columns. All Low, owner engineering, tracked in the residual table below. |

## Risk Assessment Matrix

| ID | Risk Dimension | Impact | Likelihood | Mitigation | Residual |
|----|----------------|--------|------------|------------|----------|
| RK-001 | Undeclared net-new residuals (embedding / queue / untested branches / silent heals / crash window) | Med | Med (already true) | COND-RK-01 ledger rows | Low once declared |
| RK-002 | Governance doc contradiction (ROADMAP OPEN vs CONTRACT CLOSED) | Med | High (present fact) | COND-RF-02 / COND-RS-01 | Med until docs lane |
| RK-003 | "CLOSED" overclaims: stale vector passes all F-01 checks | Med | Low (needs unobserved partial commit) | COND-RF-01 (verify embedding or re-scope) | Med until closed |
| RK-004 | Silent heal trend invisible; named 500s not actionable | Med | Med (heals fire whenever engine misbehaves) | COND-RK-02 | Low once documented |
| RK-005 | Rollback procedure conflicts under incident pressure | Med | Low (only if rollback needed) | COND-RK-03; full reverse-order revert proven | Low once documented |
| RK-006 | Regression in untested fail-closed branches | Med | Low | 243+113+adjacent bar; fail-closed ⇒ availability-only | Low (COND-RF-03/RS-03) |
| RK-007 | Concept tokens in logs vs errors.ts claim | Low | Low (rare path, ≤200 chars) | comment/message fix (backlog) | Low |
| RK-008 | §2 param-order doc drift | Low | High (present fact) | COND-RF-04 (one-line doc) | Low |
| RK-009 | Carried-over Lows (edges, TTL window, legacy keys, budgets) | Low | Low–Med, narrow windows | sibling backlog items | Low |

## Residual-risk inventory (what REMAINS after this lane)

| # | Risk (remains after `fb8e661`) | Owner | Expiry / trigger | Declared? |
|---|--------------------------------|-------|------------------|-----------|
| 1 | **Cross-process writers** to one Helix out of contract — survivor lock is in-process only | engineering | **P4.3 multi-instance start or 2026-12-31, whichever first** | YES — `docs/CONTRACT.md:60-61,309-312`, `README.md:547+`, `src/store.ts:725-728` |
| 2 | **`embedding` write escapes F-01 verify** — stale vector possible with every check green (RK-003) | engineering | **≤2026-12-31 or next Helix engine upgrade**, whichever first | **NO — COND-RK-01 (via COND-RF-01 doc path)** |
| 3 | **Stale ROADMAP §1.3 rows** (RL-001/F-01 OPEN + `:44` counts) contradicting CLOSED (RK-002) | engineering (ship-release) | **at ship-release** — before push | **NO — COND-RF-02 / COND-RS-01** |
| 4 | **Unbounded FIFO queue depth + amplified in-lock hold** (per-send 15s × ~13 sequential sends, no request deadline, no queue cap; `src/store.ts:50,517-533`) | engineering | default = **document**; trigger = P4.3 multi-instance or first observed retry storm; align with RL-001's expiry | **NO — COND-RS-02 (default: document)** |
| 5 | **Deadline/acceptance expiries 2026-12-31** on the §1.3 ledger + F-01's "next Helix engine upgrade (re-verify)" carve-out | engineering | **2026-12-31 / next engine upgrade** | YES but **stale** (rows say OPEN for fixed items) — reconcile under COND-RF-02 |
| 6 | **Untested fail-closed sub-paths**: expired-while-waiting fall-through, fresh-read vanish/wrong-id/non-string throws, heal-still-violated → throw (`src/store.ts:744-751,850-868,1029-1038`) | engineering | **before gate close** — §I seam cases or explicit code-reviewed-untested declaration | **NO — COND-RF-03 / COND-RS-03** |
| 7 | **Successful heals unobservable** (no log/counter) + **no operator runbook** for `REQ-F-01:`/`REQ-RL-001:` 500s (RK-004) | engineering | **at ship-release** (docs default) or declared accepted-silence with expiry; code-side heal log = follow-up proposal lane | **NO — COND-RK-02** |
| 8 | **Rollback doc wrong** — per-commit partial revert conflicts; safe path = reverse-order full revert of 3 commits (RK-005) | engineering | **at ship-release** | **NO — COND-RK-03** |
| 9 | §2 `linkMemoryConcepts` param order drift (RK-008) | engineering | at ship-release | tracked — COND-RF-04 |
| 10 | `consolidateInto` docstring drift "WITHOUT a write" (readability RD-001) | engineering | comment-only fix, at ship | tracked — COND-RD-01 |
| 11 | **Crash-window links-partial commit** heals only lazily on next guard-path save, no journal (resilience finding 4) | engineering | next Helix upgrade / P4.3; doc carve-out requested in COND-RK-01/RS-01 wording | NO (doc gap) |
| 12 | Duplicate HAS_CONCEPT edge accumulation (traversal cost; `.dedup()` masks correctness) — reliability F5 | engineering | edge-count audit or DAT-001 procedure re-run | in query comment only |
| 13 | `verify.ts` session-node accumulation, **no declared run budget** (resilience finding 5; `verify-skills` has one at 25 runs) | engineering | P4.1 re-review / COND-DAT-002-style declaration | NO |
| 14 | Carried-over Lows: over-merge below threshold (RF-018/F3), expired-fall-through no re-probe (F4), `withTimeout` no abort (F6), guard never repairs legacy keyless `dedupKey` (F7), queue-poisoning recovery test (F8), TEST_MATRIX commit-hash columns (RD-006), errors.ts comment vs concept names (RK-007) | engineering | at owner's discretion (backlog) | recorded here |

**Accepted-risk declaration honesty (lens item 2):** the EXISTING §1.3 declarations are honest in form — every row carries owner + acceptance date + dated expiry/trigger (e.g. `ROADMAP.md:68,70`, and the pattern precedent `:69` DAT-001 CLOSED, plus the purge no-timeout accepted risk at `docs/CONTRACT.md:367-371`). The dishonesty is in CONTENT, not format: rows 3 and 5 above are stale-closed, rows 2/4/6/7/8/11/13 are undeclared. Net: **not yet honest-and-dated for this lane → COND-RK-01.**

## Conditions (verdict ⚠️)

- **COND-RK-01** (RK-001) — **Residual-ledger completeness before push.** After COND-RF-01 (embedding: verify it or re-scope CLOSED) and COND-RS-02 (queue wait: document) clear, ensure every surviving row of the residual table marked *NO* appears in `ROADMAP.md` §1.3 or `docs/CONTRACT.md` §3 with **owner + dated expiry/trigger**: embedding gap (≤2026-12-31 / engine upgrade), unbounded queue wait (P4.3 / first retry storm), untested fail-closed paths (or their explicit code-reviewed-untested declaration), crash-window lazy-heal carve-out, and `verify.ts` session-node run budget. Verifiable as: ROADMAP/CONTRACT diff reviewed by this role; no residual in this table left with `Declared? = NO`.
- **COND-RK-02** (RK-004) — **Make the new failure modes operator-visible (docs default).** Add one operator line (README Troubleshooting or CONTRACT §3): on `-> 500: Error: REQ-F-01: …` / `REQ-RL-001: …` in the log, what it means (fail-closed, write not reported), what to do (retry is safe — converges via substring guard → `ensureConceptLinks`; escalate to engineering if it repeats), and where to look (`grep "REQ-F-01:\|REQ-RL-001:"`, prefixes survive the 200-char trim). Also record the **silent-successful-heal posture** explicitly — accepted-silence with owner + expiry, or schedule a follow-up proposal lane for a one-line allowlisted heal log (memoryId + which invariant, no content). Do NOT hotfix code inside this docs condition. Verifiable as: doc diff reviewed by this role.
- **COND-RK-03** (RK-005) — **Correct the Rollback Points in `IMPLEMENTATION_PLAN.md:39-55`.** Replace the per-commit hunk-level partial-rollback steps with the proven procedure: full reverse-order revert `fb8e661` → `a0257d6` → `01224cc` (each lands on a historically green tree; no schema/index/route/data backout; additive queries become dead code), and state explicitly that reverting `01224cc` alone CONFLICTS (evidence: merge-tree probe exit 1, 5 conflicted files) and that reverting `a0257d6` while keeping `fb8e661` leaves docs claiming CLOSED for reopened behavior. Verifiable as: plan diff + the same merge-tree probes still exit 1/0/0 as recorded.

Sibling conditions **COND-RD-01** (docstring), **COND-RF-01** (embedding/CLOSED scope), **COND-RF-02** (ROADMAP rows), **COND-RF-03** (untested fail-closed paths), **COND-RF-04** (param order), **COND-RS-01** (ROADMAP anchor), **COND-RS-02** (queue-wait declaration), **COND-RS-03** (negative tests) are taken as-is and gate my findings RK-002, RK-003, RK-006, RK-008 — **no duplicates issued**.

## Verdict Rationale

- **pass** = bounded blast radius, honest dated residuals, proven rollback, observable failure modes, no overclaim → **not met**: RK-001..RK-005 (ledger under/over-declaration, CLOSED overclaim by one property, silent heals + no runbook, stale SPEC rows, wrong rollback doc).
- **fail** = unbounded blast / unrollbackable / unbounded-undiagnosable risk → **not met**: rollback of the full lane is proven deterministic and fast (reverse-order, green parents, no data backout); both headline fixes hold with two independent green bars re-run today (243/0, 113/0, typecheck 0) plus sibling counterfactuals; failures ARE greppable by named prefix in both REST and MCP logs; the read/degradation path is untouched; no dependency or schema surface moved; every remaining risk has a cheap, identified mitigation.
- **conditional** = residual risks real but each with an available mitigation and an owner ⇒ ⚠️, gated by **COND-RK-01..03** (plus the inherited sibling conditions).

**Overall verdict: ⚠️ conditional** — Critical 0 · High 0 · Medium 5 (RK-001..RK-005) · Low 4 (RK-006..RK-009); conditions **COND-RK-01, COND-RK-02, COND-RK-03**.

## Evidence log (allowlisted — statuses/counts only; no content, no ids)

| Run / probe | Target | Result |
|-------------|--------|--------|
| `npm run typecheck` | repo tree `fb8e661` | exit 0 |
| `npx tsx scripts/verify-lifecycle.ts` | offline | **113 passed, 0 failed / VERIFY PASS** |
| `AGENT_MEMORY_URL=http://127.0.0.1:3151 npm run verify` | live `:3151` | **243 passed, 0 failed / VERIFY PASS** (incl. `rl-001:` 13 + `f-01:` block, cleanup forgets green) |
| `GET /memory/livez`, `GET /memory/health?project=review-risk` | live `:3151` | 200 ok; `memories:0, sessions:0` (zero write probes by this review — nothing to forget) |
| Rollback probe 1 (revert `01224cc`, keep `a0257d6`) | `git merge-tree --merge-base=01224cc HEAD a9ef417` | **exit 1 — conflicts in 5 files** (TEST_MATRIX.md, db/queries.ts, scripts/verify.ts, scripts/verify-lifecycle.ts, src/store.ts) |
| Rollback probe 2 (revert `a0257d6`, keep `fb8e661`) | `git merge-tree --merge-base=a0257d6 HEAD 01224cc` | exit 0 (git-clean; semantically doc-contradictory — see RK-005) |
| Rollback probe 3 (revert `fb8e661`) | `git merge-tree --merge-base=fb8e661 HEAD a0257d6` | exit 0 (docs-only, runtime-safe) |
| Hygiene | `:6969` / `:3111` | never restarted/stopped; upstream untouched; `git status` shows only the untracked gate dir `docs/specs/40_workspace/`; repo files modified by this review: **only this report** |

## Findings summary

Critical: 0 · High: 0 · Medium: 5 (RK-001, RK-002, RK-003, RK-004, RK-005) · Low: 4 (RK-006, RK-007, RK-008, RK-009) · Conditions issued: **COND-RK-01, COND-RK-02, COND-RK-03** (inherited: COND-RD-01, COND-RF-01..04, COND-RS-01..03)
