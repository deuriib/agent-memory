# Data Review: REQ-RL-001 + REQ-F-01 (residual-closure lane)

**Reviewer:** review-data (independent gate reviewer — data-engineer / lineage lens)
**Date:** 2026-09-24
**Scope (commits, unpushed on main):** `01224cc` (REQ-RL-001 — per-survivor FIFO lock + fresh `getMemoryById` re-read), `a0257d6` (REQ-F-01 — `memoryConcepts` read + `linkMemoryConcepts` link-only heal + post-write verify/heal + pure `missingConcepts`), `fb8e661` (docs: CONTRACT v1.5 + README + CHANGELOG). Diff under review: `a9ef417..HEAD`. Primary code: `db/queries.ts`, `src/store.ts`, `src/consolidate.ts`; contract `docs/CONTRACT.md` §1–§3; `TEST_MATRIX.md`.
**Review criteria applied:** `frame-ship/skills/quality-gate/references/domains/data-review.md`
**Verdict:** ⚠️ **conditional** — findings: **0 Critical / 0 High / 1 Medium / 3 Low** ⇒ 1 condition (`COND-DT-01`); Lows are backlog, no conditions.

**Rules honored:** review-only (this report is the only file written — no code/script/test edits), no Helix restart/stop/upgrade, upstream `:3111` untouched. Evidence: static lineage review + read-only curls on live `:3151` + one self-cleaning write probe under project `review-data` (cleanup via `POST /memory/forget` → `forgotten:true`, `health?project=review-data` memories=0; one residual empty Session node remains — declared `forget` semantics, Memory-only removal). No secrets, no PII, no memory content, no embeddings printed anywhere in this review.

---

## Checklist

- [x] **Schema changes versioned** — no schema change: 3 ADDITIVE queries (`db/queries.ts:534-631`), no new index (`IndexSpec` untouched by the diff; bootstrap stays 8 — CONTRACT v1.5 §2, `TEST_MATRIX.md` commit-1 bar). CONTRACT v1.5 amendment records all three with params + return shapes.
- [x] **Data lineage documented** — CONTRACT v1.5 §2/§3 amendment documents the fresh re-read, link-only heal, verify/heal flow, and echo semantics. One lineage branch is NOT documented: the batch's `embedding` write escapes the verify (DT-01 ⇒ `COND-DT-01`).
- [x] **Quality checks (nulls, types, ranges)** — fail-closed shape/type asserts on every new read: missing `memory`/`names` return, wrong type, miss, wrong `memoryId`, non-string `content` all throw (`src/store.ts:841-899`, `:927-951`); `missingConcepts` exact-name, deduped, sorted (`src/consolidate.ts:94-113`) with 9 pure goldens (`scripts/verify-lifecycle.ts` §G).
- [x] **PII handling compliant** — no new caller-facing surface: `getMemoryById`/`memoryConcepts`/`linkMemoryConcepts` have ZERO references in `src/server.ts`/`src/mcp.ts` (grep: 0 matches) — reads stay in-process. Content read for verify is never logged (zero `console.*`/stdout in `src/store.ts`, `src/consolidate.ts`, `db/queries.ts`); new check messages print lengths/counts/status only (`scripts/verify.ts:1559-1563`, `f-01` `contentLen=…`). Projections minimized: `memoryRowProjection` + `dedupKey` only (`db/queries.ts:104-112`, `:553`); Concept `name` only (`:591`). Ley 172-13 posture UNCHANGED: `src/server.ts`/`src/mcp.ts` untouched by the lane (git stat empty), governance log / access-log minimization declarations intact.
- [x] **Migration path defined** — none needed: additive reads + one link-only write; no label/edge/property added; `db/index.ts` grows exports only.
- [x] **Backfill strategy (if applicable)** — n/a for new constructs; the pre-existing legacy `dedupKey`-less-row backfill gap is unchanged (see DT-02, backlog).
- [x] **Analytics impact assessed** — none: no search/ranking/pipeline changed; fusion, TTL signals, recall ledger untouched by the diff.

---

## Data-lens specifics (packet items 1–7)

1. **Schema/projection allowlist + embedding gap** — all three new queries project only allowlisted fields: `getMemoryById` = `memoryRowProjection` + `dedupKey` (`db/queries.ts:553`), `memoryConcepts` = `name` (`:591`), `linkMemoryConcepts` returns the anchor var only (`:631`, consumed in-process by `indicatesPresence`, `src/store.ts:985-989`). **Embedding is never returned to callers** (deliberate, `db/queries.ts:529` docblock; `FreshSurvivorRow` excludes it, `src/store.ts:165`). The verify gap is REAL and assessed as DT-01: it is a **lineage/correctness exposure, not a privacy exposure** — a partial commit that lands content+links but drops the `embedding` refresh passes all three invariants, leaving the vector index serving the pre-merge vector while content/dedupKey reflect merged state (vector-representation lineage diverges from stored content; the vector leg of hybrid fusion serves stale retrievability until the next content rewrite). Privacy delta: none — `embedding` is derived from content, lives/dies on the same row (`forget` drops the node), never printed/returned. Condition raised.
2. **dedupKey invariant across the merge lifecycle** — CONSISTENT on every write path: merge computes `nextDedup = contentHash(project, normalize(nextContent))` once and the SAME value goes to the write (`src/store.ts:787-796`) and to verify invariant (b) (`:1024-1025`); heal `retryWrite` re-sends the full `mergedWrite` incl. key; `linkMemoryConcepts` contains zero `setProperty` — only `conceptBody()` edges (`db/queries.ts:617-631`) — so heal never touches content/key. Guard path writes nothing ⇒ key untouched (byte-identical survivor) — see DT-02 for the legacy-key backlog note.
3. **Concept lifecycle vs DAT-001 orphan closure** — the link-only heal CAN newly CREATE `Concept` nodes on the guard path (`db/queries.ts:195-214` create branch), where before the lane that path wrote nothing; under the same admitted mid-batch non-atomicity a partial commit could orphan a fresh Concept (zero in-edges). The orphan **calculus is unchanged**: DAT-001's procedure defines orphan = zero incoming `HAS_CONCEPT`, evaluated GLOBALLY (CONTRACT §3 v1.4 (b)) — creation-path agnostic, so it catches heal-created orphans with no procedure edit; the lost *edge* itself is surfaced fail-closed by `ensureConceptLinks`' post-heal re-read (`src/store.ts:981-984`). Logged as DT-03 (Low, informational).
4. **TTL × merge on the fresh re-read** — consistent: `filterExpired` runs on probe candidates (`src/store.ts:623`) AND again on the fresh row under the lock (`:745-750`); expired-while-waiting ⇒ `undefined` ⇒ plain insert, never absorbs (`:746-751`). Lineage of the abandoned survivor: the expired row retains prior merged variants under its ORIGINAL `createdAt` (first-wins, unchanged by merges) and expires/purges WITH them — declared P1.1 TTL semantics, no text added or lost outside TTL intent; the incoming lands as a fresh row with a distinct `dedupKey` (key = hash of its own content) — no dedup collision, no cross-row key rewrite. PASS.
5. **Where-clause project scoping** — all three new queries anchor `memoryId` AND `project` in the SAME where-clause: `db/queries.ts:545-552`, `:582-588`, `:622-628`. Live probe (project `review-data`, cleaned up): positive control with matching project → 1 row; same `memoryId` with `project="wrong-project"` → `leak: []` AND the `HAS_CONCEPT` traversal with wrong project → `leakNames: []`. No cross-project leakage. PASS.
6. **Test fixtures synthetic only** — CONFIRMED: `rlBase`/variants are deploy-checklist token strings, `fA`/`fB` synthetic (`scripts/verify.ts:1593-195`), `fConcept` = random non-content-derived token (`:1596`), projects/sessionIds = `verify-*` + uuid slices. No names, emails, credentials, or real user text anywhere in the lane's verify blocks; failure messages print lengths/counts/ids only. PASS.
7. **Ley 172-13 posture** — UNCHANGED: server/log/governance files untouched; content read by the verify never printed (zero print sites in lane code, grep-verified); minimization strengthened, if anything (`name`-only concept projection). PASS.

---

## Findings

| ID | Severity | Finding | Evidence | Mitigation |
|----|----------|---------|----------|------------|
| DT-01 | **Medium** | The batch writes **4** properties (`content`, `embedding`, `dedupKey` + links) but `verifyMergedState.violations()` asserts only 3 — `embedding` is never verified and `getMemoryById` deliberately never projects it. A mid-batch partial commit that lands content+links but drops the `embedding` refresh passes ALL invariants green while the vector index serves the PRE-merge vector: vector-representation lineage diverges from stored content and vector-leg retrievability of the merged text degrades until the next content rewrite — the exact retrieval-defect class F-01 was opened for. CONTRACT §3 tier-1(b) says "ATOMICITY — CLOSED" / "commit is DETECTED-and-healed app-side" which over-reaches by one written property; the residual is documented NOWHERE (embedding absent from the invariant list and from ROADMAP F-01). Data-governance angle: correctness/lineage only — no PII delta (derived vector, same row lifecycle, never returned, never logged). | write: `src/store.ts:787-796` + `db/queries.ts:676`; verify: `src/store.ts:1017-1026`; projection exclusion: `db/queries.ts:529`, `src/store.ts:165`; claims: `docs/CONTRACT.md:62`, `:312-327` | `COND-DT-01` |
| DT-02 | Low | The substring-guard path runs ONLY the concept-link verify (`src/store.ts:764`) — it never asserts or repairs `dedupKey`. A legacy `dedupKey`-less survivor that keeps hitting the guard path stays keyless forever (verify invariant (b) runs only on the write path, `:1024-1025`), so the exact-dedup pre-check never converges for that row. Pre-existing, documented backfill gap (CONTRACT §1 "legacy rows may lack it"); the lane's minimization choice (byte-identical survivor, no gratuitous write) is defensible — but the gap now has a second, longer-lived path. | `src/store.ts:744-767`, `:1024-1025`; `docs/CONTRACT.md:113` | Backlog: consider a key-only repair (`setProperty dedupKey` without content) or an explicit contract note that the guard path deliberately does not backfill keys. Owner: engineering. |
| DT-03 | Low | The link-only heal widens the Concept-creation surface: the guard path previously wrote NOTHING, now `conceptBody()` can CREATE Concept nodes (`db/queries.ts:195-214`) whose `HAS_CONCEPT` edge rides the same non-atomic batch — a partial commit can orphan a fresh Concept (zero in-edges) after `ensureConceptLinks` fail-closes. DAT-001 orphan calculus UNCHANGED: the global zero-in-edge audit + gate + drop procedure (CONTRACT §3 v1.4 (b)) is creation-path agnostic and catches these with no edit; edge loss is surfaced by the fail-closed re-read. | `db/queries.ts:189-216`, `:617-631`; `src/store.ts:953-989`; `docs/CONTRACT.md` §3 v1.4 (b) | No procedure change needed. Informational: heal-created orphans are covered by the existing operator-run audit; keep DAT-001's "re-run on demand" owner note. |
| DT-04 | Low | Docs hygiene in the lane's evidence trail: `TEST_MATRIX.md:4` still heads `Date: 2026-09-23` although 2 of 3 commits (and the contract amendment) are 2026-09-24; `ROADMAP.md:68,70` §1.3 rows still describe RL-001/F-01 in present-tense defective state (lane deliberately left ROADMAP to the docs/ship pass per `TEST_MATRIX.md` coverage note). When those rows ARE updated, the DT-01 decision (embedding covered vs named residual) must land there too. | `TEST_MATRIX.md:4`; `ROADMAP.md:68,70`; `git log -3` commit dates | Fold into the docs/ship lane; cross-link with `COND-DT-01`. |

---

## Conditions (must clear to reach ✅)

- **COND-DT-01** (DT-01, Medium) — Close or re-scope the embedding lineage gap, ONE of:
  1. add an `embedding` invariant to `verifyMergedState` (re-read `embedding` under the survivor lock and compare against `embed(expectedContent)` — deterministic embedder makes this exact; adding it to `getMemoryById`'s projection is an internal-only read, no caller surface, no privacy delta), plus a §I seam case; **or**
  2. amend `docs/CONTRACT.md` §3 tier-1(b) so "ATOMICITY — CLOSED / DETECTED-and-healed" explicitly covers only content/dedupKey/links, with `embedding` declared as a named residual (owner: engineering, expiry ≤ 2026-12-31 or next Helix engine upgrade — align with the standing F-01 trigger), mirrored into `ROADMAP.md` F-01 when that row is next touched.

  Verifiable as: either a new `violations()` check + green seam/E2E evidence with counts updated, or a contract (+roadmap) diff reviewed by this role.

---

## Verdict Rationale

The lane's core data claims HOLD under the data lens: projections are strictly allowlisted with no embedding ever surfaced to callers; all three new queries are fail-closed on shape AND project-scoped in the where-clause (proved live: wrong-project anchor and traversal both empty, positive control 1 row); the dedupKey invariant is rewritten and verified coherently on every write path while both heal paths provably never touch content/key; TTL×merge fall-through lineage is consistent with declared first-wins/expiry semantics and creates no key collision or text loss; the guard-path heal does not worsen DAT-001's orphan calculus (global audit still covers all creation paths); fixtures are fully synthetic with no content printed at any check site; and the Ley 172-13 posture is untouched (server/log files unchanged, reads in-process, `name`-only concept projection).

One genuine lineage hole keeps this from PASS: DT-01 — the verify closes F-01 over 3 of the 4 properties the batch writes, so a partial commit can silently strand the vector index in pre-merge state while every check reports green, and neither the contract's CLOSED claim nor the roadmap discloses that boundary. It is a correctness/retrievability exposure (not privacy), bounded and cheaply closeable or documentable — hence ⚠️ conditional with a single condition, `COND-DT-01`. DT-02..04 are backlog/documentation hygiene, no conditions.
