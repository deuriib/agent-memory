# Automation Review: P1 remainder (REQ-P1-4 / P1-2 / P1-5) + P3.2

**Reviewer:** automation-reviewer (automation/ops domain — independent, review-only; did NOT write this code)
**Date:** 2026-09-23
**Verdict:** conditional — **COND-AUT-001**
**Lane under review:** `d17294b..HEAD` = `591c79c`, `39fec28`, `df39d7d`, `c69636d`, `2deda68` (repo `/mnt/DATA/GitHub/agent-memory`)
**Checklist:** `frame-ship/skills/quality-gate/references/domains/automation-review.md`
**Evidence basis:** read-only scans of the diff, `ci.yml`, scripts, README, CHANGELOG, SCORECARD, CONTRACT, plan/matrix (TEST_MATRIX run logs cited by reference — write-path suites NOT re-run per lane contract; 3111/Helix dev untouched).

## Checklist

- [x] **Workflow/port/adapter/event boundary mapped (PII checkpoint)** — both new suites talk REST only, behind the read-only identity guard (`POST /memory/recap` must 200) *before any write*: `scripts/eval.ts:17-33` (header), `scripts/verify-skills.ts:14-16`. Eval corpus = invented dev/ops facts, no PII (`eval.ts:31-33`); `verify-skills` scans all 8 SKILL.md for secret patterns ghp_/sk-/AKIA/`AGENT_MEMORY_SECRET=` (`verify-skills.ts:107-112`). Boundary = local REST adapter → shared dev Helix; scorecard/stdout carry no secrets (SCORECARD.md, TEST_MATRIX T-203/T-204).
- [x] **Least-privilege scopes verified** — no new keys/credentials. `AGENT_MEMORY_SECRET` forwarded as bearer only when set, never logged (`eval.ts:99-104`, `verify-skills.ts:17-19`). CI stays `permissions: contents: read` (`ci.yml:8-9`); gitleaks pinned + sha256-verified (`ci.yml:39-48`); local gitleaks unavailability declared → CI secret-scan enforces on push (TEST_MATRIX T-205).
- [x] **Idempotency + retry budget defined** — eval re-run idempotent via content dedup: **declared** (`eval.ts:20-25`: pass 2 must return same ids `deduped:true`, health count == corpus size or fail-loud) and **tested** (SCORECARD.md:14 "pass 2: 40/40 deduped, stable ids"; TEST_MATRIX T-203 `EVAL PASS` ×2 — Lane B + orchestrator re-run). Retries are read-only polls with budgets: smoke 5 attempts × 2 s (`eval.ts:66-68`), one content-only batch retry in probe4 (`probe4.ts:470-474`) — no fix-loop, no third loop anywhere.
- [x] **Deployment plan + rollback tested** — ship type = local library/server release (no external deployment target). Rollback defined per step in `IMPLEMENTATION_PLAN.md` §Rollback Points (delete `eval/`, `scripts/eval.ts`, `docs/benchmarks/`, the 8 skill dirs + `verify-skills.ts`; MERGE env leaves no index artifacts; additive, no schema touched). Revert itself not executed this lane — assumption stated by the plan (review-only; evidence = plan L52-65 + additive-inert diff). See Ops Lens.
- [x] **Monitoring/alerting + runbook updated** — runbooks present and operable: README Quick start (L87-113) + Verification (L499-581) + conflict procedure (L426-444, ports 3111 default / 3151 reroute / `AGENT_MEMORY_URL` explicit), SCORECARD.md §Reproduce, and script-header runbooks (`eval.ts:43-46`, `verify-skills.ts:1-23`). Monitoring/alerting N/A: local dev instance, no serving SLO; purge audit/`purge-progress` lines pre-existing (v0.4.0). One runbook nit → AUT-002.
- [x] **Capacity/scaling + feature flags reviewed** — capacity N/A (shared dev Helix gains ≤40 + 4 rows per run; negligible). Flag reviewed: `AGENT_MEMORY_MERGE_JACCARD` default 0.9 **ON**, fail-closed OFF on ≤0/≥1/garbage — documented in README config table (L416) and CONTRACT §3 (L238); env has no index artifacts (plan rollback L57-58).
- [x] **No freelance fixes** — review-only session: zero edits outside this artifact; no write-path suites executed; no Helix restart/stop; port 3111 never touched.

## Findings

| ID | Severity | Finding | Mitigation |
|----|----------|---------|------------|
| AUT-001 | **Med** | v0.5.0 release notes: `docs/specs/30_delivery/RELEASE_NOTES.md:1` still reads `# Release Notes: v0.4.0` (top entry), yet `README.md:610-611` points readers to it as "per-release notes". The lane neither updated it nor recorded an explicit deferral: `grep 'release notes\|RELEASE_NOTES\|deferred' IMPLEMENTATION_PLAN.md TEST_MATRIX.md` → **0 hits** (exit 1); plan step 5 (shared-file list) omits RELEASE_NOTES. v0.4.0's notes were written at ship-release, so chain order implies deferral — but P0 precedent recorded exactly that as an owned pending item (`50_archive/P0/HANDOFF.md:23`), this lane records nothing → silent skip risk at ship. | **COND-AUT-001:** write the v0.5.0 section (CHANGELOG entry + lockstep facts already exist as source material) **or** record an explicit deferral row naming owner = orchestrator/ship-release — at ship-release, whose mandate includes NOTES. Owner: orchestrator (ship-release stage). |
| AUT-002 | Low | Scorecard reproduce block hardcodes port **3152** (`scripts/eval.ts:463,466,470`, repeated in fail-runbook `eval.ts:503-504,530-531`) while the committed scorecard's own Server row records the actual run on **3151** (`docs/benchmarks/SCORECARD.md:12` vs `:54-61`) and README's documented reroute port is **3151** (`README.md:430-437`). Instructions remain self-consistent and upstream-safe (identity guard aborts first), so impact is reader confusion / drift, not correctness. Evidence: eval render uses dynamic `NORMALIZED_BASE` for the Server row (`eval.ts:415`) but static 3152 for Reproduce. | Render the reproduce port from `NORMALIZED_BASE.port` (or standardize on README's 3151) — engineering — orchestrator; backlog, next eval touch. |
| AUT-003 | Low | The Helix-free structural half of `verify-skills` (73 of 119 checks — TEST_MATRIX Coverage Summary L23) is CI-eligible but not wired in, because `main()` runs structural then live round-trip and the live half aborts exit 1 with no server (`verify-skills.ts:84,594-606`). The declared no-CI rationale "eval/skills/verify exigen server vivo" (`IMPLEMENTATION_PLAN.md:28`, quality gate L77) is true of the script *as invoked* but only half-true of its checks; skill-doc drift (frontmatter/routes/tool tables) is caught only locally. Evidence: `git diff d17294b..HEAD -- .github` = **empty** (no CI change made) + `ci.yml:21-28` has no skills step. | Backlog: structural-only entry point (`--structural-only` or a second npm script) added to `ci.yml` — engineering — orchestrator. Not blocking: exclusion is declared and the shipped behavior is honest. |
| AUT-004 | Low | `verify-skills` header claims "best-effort cleanup of **every row** created here" (`verify-skills.ts:14-15`), but cleanup removes only the 4 memory rows (`verify-skills.ts:571-590`, `created = 4`); `forgetMemory` drops the Memory node only (`db/queries.ts:388-402`), while novel writes materialize Session nodes (`src/store.ts:132-133` — sessions on novel writes only) with per-run UUID session ids (`verify-skills.ts:393-396`). Each re-run of the fixed project `verify-skills` therefore leaves undeclared session residue (`healthCount` counts sessions separately, `db/queries.ts:404-415`). Volume tiny, dev-instance only; cleanup itself proven 4/4 (TEST_MATRIX T-204). | Declare session residue in the script header/README (or forget sessions too) — engineering; backlog. Companion note: Concept-node residue on forget is the pre-existing **DAT-001** (data domain, not re-opened here). |
| AUT-005 | Info | Eval data-ops story is sound but implicit: project `agent-memory-eval` keeps its 40 rows + 1 session **by design** (SCORECARD.md:13-14 — re-seed idempotency depends on them; declared). Purge path exists (`scripts/purge.ts --project agent-memory-eval`), yet `--days` guard rejects 0 (`purge.ts:109-113` — `^[1-9]…`, "days=0 would delete EVERYTHING"), so same-day purge is impossible; earliest is `--days 1` after 24 h. Ops burden acceptable (40 rows, dedicated tenant, documented projects in README:554-565 / CONTRACT:426-433). | No action required; optionally document the eval-residue purge line next to the scorecard — engineering; backlog. |

**Findings: 5** (1 Medium, 3 Low, 1 Info; 0 Critical/High).

## Verdict Rationale

**⚠️ conditional — COND-AUT-001 (release notes: write v0.5.0 or record the deferral with an owner).** Every hard automation check in this lane's brief passes on direct evidence: (1) CI/CD — `.github/` is byte-untouched by the lane (`git diff d17294b..HEAD -- .github` = empty) and `ci.yml:21-28` still runs `typecheck` + `verify-injection` + **`verify-lifecycle` (86)** + `verify-capture`, with `secret-scan` intact; the three server-dependent suites (`verify`, `eval`, `verify-skills`) are correctly and explicitly excluded (`ci.yml:24` comment + plan L28/L77) — no needed CI change was missed (AUT-003 is an optimization, not a miss). (2) Tooling — `eval`/`verify-skills` npm scripts exist (`package.json:26-27`); README Quick start + Verification document ports (3111 default, 3151 reroute) and `AGENT_MEMORY_URL` explicit-pointing; exit codes are honest: eval `0` implicit on PASS / `fail()`→1 / crash→1 exactly as its header contract says (`eval.ts:77-81,675-679`), verify-skills `0` on PASS / `1` on failures, abort, or crash (`verify-skills.ts:82-85,594-606`). (3) Idempotency — eval declared + proven ×2 (40/40 dedup), verify-skills re-run safe with 4/4 cleanup (AUT-004 residue caveat), probe4 explicitly id-targeted "rerun-safe" with best-effort finally-cleanup and crashed-run residue declared (`probe4.ts:26-28,484`). (4) Release ops — CHANGELOG v0.5.0 present and count-accurate (86/212/119 match plan, matrix, README, CONTRACT); version lockstep ×5 verified **by reading each carrier**: `package.json:3` 0.5.0 · `package-lock.json:3,9` 0.5.0 · `src/mcp.ts:376` 0.5.0 · plugin `agent-memory.ts:63` 0.5.0 · `README.md:3` badge v0.5.0 — but RELEASE_NOTES lags at v0.4.0 with no deferral record (AUT-001, the condition). (5) Data ops — both projects declared, eval persistence intentional and documented, purge exists with fail-closed guards; burden negligible (AUT-005 Info). No Critical/High; nothing touching prod, secrets, or the upstream 3111. The single Medium is a one-line-closable release-ops record that the next chain stage (ship-release, whose mandate includes NOTES) can satisfy immediately — hence conditional, not closed, and not pass (no silent skip).

## Ops Lens

- **Applied — yes** (lane is infra-adjacent: it writes persistent rows into the shared dev Helix, adds an env flag, decides CI placement, and ships operator runbooks/ports), scoped per `ops-review.md`:
  - *Deployment plan:* local library/server release, no external deployment target — plan Step 6 runs the suites against OUR server on 3151 (upstream 3111 never touched; contract §0).
  - *Rollback:* defined per step (`IMPLEMENTATION_PLAN.md` §Rollback Points) and additive-inert (code deletion only, env leaves no index artifacts, dev-instance rows = seed/verify data) — **defined, not executed** this lane; assumption stated by the plan.
  - *Monitoring/alerting:* N/A — local dev instance, no SLO/on-call surface; pre-existing governance/purge audit lines unchanged (out of this diff).
  - *Runbook:* present (README Verification + Quick start + conflict procedure, SCORECARD §Reproduce, script headers) — AUT-002 is the only gap (port nit).
  - *On-call / capacity:* N/A — dev-only data growth ≤44 rows/run in dedicated tenants.
  - *Feature flags:* `AGENT_MEMORY_MERGE_JACCARD` reviewed — default ON, fail-closed OFF on bad config, documented README:416 + CONTRACT:238; rollback leaves no artifacts.
