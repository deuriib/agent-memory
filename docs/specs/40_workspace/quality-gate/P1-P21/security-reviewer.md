# Security Review: P1-P21 (P1 recall quality & lifecycle + P2.1 capture breadth)

**Reviewer:** security-reviewer (security owner) — 1 of 9 independent reviewers; audited from first principles, no other reviewer's findings seen.
**Date:** 2026-09-23
**Scope:** `1c410ee..HEAD` (6 commits: b27364b, 101e063, 45380b5, 8fbd795, 6f7f708, eb279a6) + current files; packet = `IMPLEMENTATION_PLAN.md` + `TEST_MATRIX.md` + `docs/CONTRACT.md` v1.1.
**Verdict:** **PASS** — **re-verified round 3 (2026-09-23) against closure commit `83e2f3a`**: condition **C-1 SATISFIED** — 5 Helix-free render-guard assertions committed to the CI-runnable `verify-lifecycle` suite (34 → **39 passed**, re-run this round), `oneLine` byte-identical after extraction to `src/logline.ts`, purge behavior unchanged (3-hunk diff, zero call-site changes), SEC-P121-03 backlog scope respected (no U+0085 assertions). History: round 1 CONDITIONAL (SEC-P121-01 Medium + SEC-P121-02 Low) → round 2 CONDITIONAL solely on C-1 (both findings FIXED-VERIFIED vs `9210208`; SEC-P121-03 Low backlog) → round 3 **PASS** (no waiver needed). Backlog, non-blocking, owners named: **SEC-P121-03** (control-char hardening, both guard sites) and **SEC-P121-04** (Low, round 3 — CI pins the guard *function*, not purge call-site wiring). CI secret-scan green on push remains the standing P0.2 release condition, unchanged. Full rationale: Re-verification round 3 below.

## Checklist

- [x] Threat model complete (STRIDE) — see Threat Model below
- [x] AuthN/AuthZ verified — frozen surface intact, `livez` sole exemption
- [x] Input validation at all boundaries — REST/MCP schemas re-read; purge CLI validated; **one gap found (SEC-P121-01)**
- [x] Secrets not in code — full-diff manual scan clean (gitleaks local unavailable → CI gate, stated under Findings context)
- [x] Dependencies scanned — zero new dependencies; CI still pinned
- [x] Data handling compliant (PII, retention, Ley 172-13) — PII checkpoint below
- [x] Audit logging in place — P3.1 delete-governance guard verified intact; purge governance lines carry one gap (SEC-P121-01)

## Threat model (STRIDE, scoped to what this lane touched)

| Threat | Surface touched by lane | Assessment |
|---|---|---|
| Spoofing (auth) | No new routes/tools; `src/server.ts` guard untouched; plugin `call()` attaches `Bearer` (`plugins/opencode/plugins/agent-memory.ts:277`); `capture.mjs` attaches `Bearer` when secret set (`hooks/capture.mjs:142-145`) | **Safe** — `src/server.ts:239` still exempts only `/memory/livez`. Direct-Helix CLIs (`purge`, `probe3`) bypass HTTP bearer by design, but that is the pre-existing local operator surface (`bootstrap` precedent, `HELIX_URL` localhost) — not a new bypass. Informational note only. |
| Tampering (dedup key) | `dedupKey = sha256(project + "\n" + normalize(content))`, app-side pre-check under per-key FIFO lock (server does NOT enforce unique index — probe3 §0 fact) | **Safe with a doc defect** — cross-project tamper requires breaking sha256 AND defeats the fail-closed project double-check in `src/store.ts` (`rememberLocked` throws when `hitProject !== input.project`). Separator claim defect → SEC-P121-02. |
| Repudiation (audit) | `purge.ts` plan/health/governance stdout lines | **Gap** — SEC-P121-01: attacker-settable `project` interpolated raw into governance-adjacent lines. |
| Information disclosure | hook payloads, `dedupKey` projection, purge output | **Safe** — allowlist-first hooks (prompt/trigger/tool-input never read); `dedupKey` absent from `memoryRowProjection` (`db/queries.ts:104-111`) and from `findMemoryByDedupKey` projection → hash never leaves the store via any API; purge `listExpired` projects `memoryId` only; no content in any new output. |
| Denial of service | `tool.execute.before` write volume; purge runaway | **Safe** — `memory*` self-skip kills the observation loop; repeated fixed-string hook observations collapse through dedup (bounded rows); purge has `MAX_BATCHES=10_000`, no-progress abort, 1.5s/2s hook timeouts unchanged. |
| Elevation of privilege | purge `--all` blast radius | **Safe** — explicit `--all` opt-in, `--days` regex `^[1-9][0-9]{0,5}$` (days=0 refused), `--project`/`--all` mutually exclusive, fail-closed usage exit 2, per-project scoped `listExpired` with cutoff; operator-CLI privilege only. |

## Findings

**Context — gitleaks:** local run unavailable (2 download timeouts, escalated per packet). I manually scanned the full 3550-line diff for secret/token/PII material: **none found** — the only credential-shaped strings are labeled synthetic canaries in test fixtures (`scripts/verify-capture.ts:50-52`, `SECRET_CANARY = "synthetic-capture-secret-canary-17213"`), which exist to prove non-leakage and are not credentials. **These findings do NOT make the CI secret scan an additional lane condition** — no secret-like material exists to scan for; the pre-existing P0.2 pinned job (`.github/workflows/ci.yml:39-51`, gitleaks v8.30.1 + sha256 checksum) remains the standing enforcing gate and must be green on push before merge, exactly as in P0.

| ID | Severity | Finding | Evidence | Remediation (owner: lane owner — reviewer fixes nothing) |
|----|----------|---------|----------|----------|
| SEC-P121-01 | **Medium** (CWE-117 log/governance-line injection) | `scripts/purge.ts` interpolates `project` raw into stdout/stderr lines. `project` is attacker-settable at the REST/MCP boundary: `projectSchema = z.string().trim().min(1).max(200)` (`src/server.ts:40`, `src/mcp.ts:31`) accepts **embedded `\n`/`\r`** (proven: `safeParse("x\npurge project=all days=1 deleted=0 at=2026-09-23T00:00:00.000Z")` → success). In `--all` mode, project names are discovered from the DB (`listProjects` ← Session rows ← remember bodies), then printed raw: `purge-health project=${r.project} phase=before…` and `…phase=after…` (`scripts/purge.ts`, main's results loops). A forged project name can emit a second line byte-identical to the governance format `purge project=all days=N deleted=M at=ISO`, forging the audit record of a **destructive** operation. Secondary vector: `healthOf` throws `…missing counts for project=${project}` → `purge failed: ${describeError(err)}` prints `err.message` raw (no single-line collapse). **Contradicts the lane's own P3.1 precedent**: `deleteReasonSchema`/`deleteMemoryIdSchema` collapse `\s+`→`" "` explicitly "so an embedded `\n` can no longer forge a second log line (CWE-117)" (`src/server.ts:48-70`, mirrored `src/mcp.ts:361-365`). Trigger condition: operator runs `purge --all` (real run; `--dry-run` output is safe — ids only) against a store containing a malicious project name; default deploy is open-localhost, so writing such a project needs no secret when `AGENT_MEMORY_SECRET` is unset. | `scripts/purge.ts` (plan/health/governance lines + `healthOf` throw + `describeError`), `src/server.ts:40`, `src/mcp.ts:31`, probe result above, precedent `src/server.ts:59-70` | Collapse whitespace runs to one space (the `deleteReasonSchema` transform) in `project` before **any** stdout/stderr interpolation in `purge.ts` (print-site sanitization preferred — do not mutate the purge scope value used for queries). Fix-forward in the owning lane; add an assertion alongside the existing usage-guard test. |
| SEC-P121-02 | **Low** (misleading security claim / latent API hazard) | The `contentHash` docstring claims: *"`\n` separator makes (project=`"a"`, text=`"b\nc"`) distinct from (project=`"a\nb"`, text=`"c"`)"* (`src/lifecycle.ts`, `contentHash`). **Proven false at the function level:** `contentHash("a", "b\nc")` and `contentHash("a\nb", "c")` both = `ea7fb08b7a2dc4619ffb7c7bb38d95a2047935fa165d71b12efd3852a2e6d0cc`. The claimed pair collides because `"\n" + "b\nc"` ≡ `"a\nb" + "\n" + "c"` = `"a\nb\nc"`. **The production dedupKey path is NOT vulnerable today:** `dedupKey = sha256(project + "\n" + normalize(content))` and `normalizeContent` collapses every whitespace run (incl. `\n`) to one space, so normalized text is newline-free; under that precondition the split at the **last** `\n` uniquely recovers `(project, text)` — the mapping is injective, and I proved the same pair diverges after normalization (`k1 = c5f89cb9…` vs `k2 = ea7fb08b…`). Residual risk: `contentHash` is exported with an open invitation (*"`text` is whatever the caller wants fingerprinted"*) — a future caller that skips normalization reopens cross-project dedup confusion, and the shipped comment tells reviewers the separator alone prevents it. Defense-in-depth already present: `rememberLocked` throws fail-closed on a cross-project pre-check hit. | `src/lifecycle.ts` `contentHash` docstring; probe output (both keys above); `normalizeContent` at `src/lifecycle.ts`; double-check in `src/store.ts` `rememberLocked` | Correct the docstring (the claim holds **only** for newline-free text, which normalization guarantees) — or better, normalize (or reject `\n`) inside `contentHash` so the invariant is enforced by the function, not by its callers. |

**Round-2 status (re-verification, 2026-09-23):** SEC-P121-01 → **FIXED-VERIFIED**; SEC-P121-02 → **FIXED-VERIFIED** — full evidence in the Re-verification section below.

**Assessed, no finding (stated so the gate record shows the work):**

- **Privacy / Ley 172-13 hook allowlist — verified.** `observationFor` returns the fixed string for `UserPromptSubmit` **before any payload field is touched** (`hooks/capture.mjs:65`) — `hook.prompt` is never read, by construction, regardless of host payload shape. `PreCompact` likewise never reads trigger/payload (`:60`). `PostToolUseFailure` is tool-name-only through `clean()` which strips control chars incl. `\n`/`\r` and bounds to 80 (`:47-54, :68-71`); missing/empty/non-string `tool_name` → `null` → store nothing (fail closed). Plugin `tool.execute.before` reads only `event.tool` — `event.input` never referenced (`plugins/opencode/plugins/agent-memory.ts`, `captureToolStart`).
- **Canary assertions genuinely prove non-capture.** The canaries are in the test **inputs** — `UserPromptSubmit` payload carries `prompt: PROMPT_CANARY`, `PreCompact` carries trigger+payload canaries (`scripts/verify-capture.ts:287, :293`) — and Section B asserts absence from the **raw recorded POST body** (any field, not just content) plus stdout/stderr, while Section A asserts `content` is *exactly* the allowlisted string and exactly 1 request fired. Input-contains-canary + output-exact-fixed-string ⇒ non-capture is proven, not assumed.
- **Auth frozen surface.** No route/tool added; `src/server.ts` not in the diff; bearer check + sole `livez` exemption intact at `src/server.ts:239`; MCP changed only its version string (`src/mcp.ts:375`). Dedup/TTL/decay changes run inside the guarded handlers only.
- **Hashing — equality-confirmation & project scope.** `dedupKey` is unsalted, but it never leaves the store: absent from `memoryRowProjection` (`db/queries.ts:104-111`) and from `findMemoryByDedupKey`'s projection (`:442-445`), so no API response echoes it; an attacker who can compute guessed hashes already has direct read of plaintext `content` on the same node (pre-existing local-Helix architecture) — the hash adds no marginal exposure. Project separation: project is inside the hash, the lookup carries `project` back, and `rememberLocked` **throws** on mismatch (fail-closed double check). Separator distinctness: proven correct for the production pipeline (see SEC-P121-02 for the function-level defect).
- **Destructive ops (`purge.ts`).** Argument validation fail-closed: integer `--days ≥1` regex, `--project`/`--all` mutual exclusion, unknown arg → usage exit 2 (re-run verified: `missing required --days` → **exit 2**); no-progress abort (`batchDeleted === 0` throws), `MAX_BATCHES` runaway guard, dry-run cap 100k with warning; output allowlist holds — ids are server-generated UUIDs (never caller-supplied) and `listExpired` projects `memoryId` only (data minimization); **no content in any purge output**; exit codes 0/1/2 as documented. Only the `project` lines fail the allowlist's spirit → SEC-P121-01.
- **Supply chain.** `package.json` diff = 3 script entries + version; `package-lock.json` diff = version only — **zero new dependencies**. CI untouched except two Helix-free test steps; gitleaks still pinned v8.30.1 with sha256 checksum verification (`.github/workflows/ci.yml:39-51`), actions pinned by exact version tags (pre-existing pattern).

**Cross-domain referral (not a security finding):** P1.6 × P2.1 interaction — fixed-string hook observations share one `dedupKey` per project (`tool used: Read`, `tool started: bash`, `user prompt submitted`, …), so only the **first** occurrence creates a row; every later identical observation returns the existing id and stores nothing. Session attribution for hook captures therefore collapses to the first session, affecting recap/handoff session-scoping of capture data. → referral to the data/engineering reviewers (product/data-quality domain, owner's call).

## PII checkpoint statement (Ley 172-13)

| New/touched store | Purpose (limitation) | TTL / retention | Deletion procedure |
|---|---|---|---|
| `Memory.dedupKey` (new property + index #8) | Write-time duplicate detection only — derived identifier `sha256(project + "\n" + normalize(content))`, no raw content, never collected beyond what `content` already stores, never returned by any API projection | Tied to the memory row's lifetime; row hidden by `AGENT_MEMORY_TTL_DAYS` (opt-in, default OFF, reported via `ttl:` signal — never silent) | Dropped with the node: `/memory/forget` / `/memory/delete` (governed, reason required) / `scripts/purge.ts` for expired rows |
| Hook observations — 4 new `capture.mjs` events + plugin `tool.execute.before` (new collection) | Session observability, minimized **at collection**: fixed allowlisted strings and tool NAME only; prompt text, compaction trigger/payload, tool input, file paths, command output are never read (Ley 172-13 purpose limitation — the strongest control is not collecting) | Same as memory rows: TTL hide (opt-in) + purge enforcement | Same erasure paths as any memory (`forget`/`delete`/`purge`); no separate store created |
| `scripts/purge.ts` (new processing/deletion tool) | Retention enforcement — deletes rows older than an explicit `--days` threshold; reads only `memoryId` (projection allowlist), never content; emits allowlisted governance output (project/days/counts/timestamp) | Operates ON retention; scope requires explicit `--project`/`--all` + `--days` | It IS the deletion procedure; dry-run mode previews without deleting (SEC-P121-01 must be fixed so its governance output stays trustworthy) |

No new PII collection is introduced by this lane; prompt text (the highest-risk PII class on this surface) is explicitly **not** collected, and that is canary-proven. Residual: hook `sessionId`/cwd-derived `project` are host metadata (pre-existing behavior, unchanged).

## Gate evidence

Re-run by this reviewer (cheap, Helix-free, 2026-09-23): `typecheck` exit 0 · `verify-lifecycle` **34 passed, 0 failed / VERIFY PASS** · `verify-capture` **115 checks, 0 failed / ALL PASS** · `verify-injection` **ALL PASS (73)** · `purge.ts` no-args usage guard **exit 2**.
Trusted from lane evidence (not re-run — server/instance-bound): `verify` **131/131** against our server on 3151 (upstream `iii` on 3111 untouched, never killed; Helix never restarted) · `verify-env` **21** · `probe3` **GREEN** · `purge --dry-run` would-delete=1 · `demo OK` · CI yml unchanged except the two new Helix-free steps.

## Verdict Rationale

**CONDITIONAL** *(round-1 rationale, preserved as history — superseded by Re-verification below)*. No Critical/High findings; auth surface frozen and verified; privacy canaries genuinely prove non-capture; secrets/dependencies clean; destructive-op validation fail-closed; hashing safe in the production pipeline with proven project separation. Two findings hold the gate: **SEC-P121-01 (Medium)** — a proven CWE-117 chain that can forge the governance audit lines of a *destructive* operation, contradicting this codebase's own P3.1 log-forgery guard — and **SEC-P121-02 (Low)** — a false security claim in a shipped docstring whose invariant currently rests on callers normalizing. Both are owner-remediable, low-cost fix-forwards. Residual risk until remediated: forged purge audit output (Medium, conditional on `purge --all` against a store holding a malicious project name) and a latent API hazard if a future caller skips normalization. CI secret-scan green on push remains the standing P0.2 release condition (no lane-specific finding adds to it). If SEC-P121-01 is remediated with a test assertion and SEC-P121-02's docstring/invariant is corrected, this review moves to PASS on re-check.

---

# Re-verification (round 2) — remediation commit `9210208`

**Reviewer:** security-reviewer (same reviewer as round 1) · **Date:** 2026-09-23
**Re-verified scope:** ONLY SEC-P121-01, SEC-P121-02, plus a fresh first-principles sweep of the full `9210208` diff for newly introduced findings. Read-only on code; this artifact is the only file updated.
**Overall verdict after re-verification:** **CONDITIONAL** — both original findings FIXED-VERIFIED; condition **C-1** (below) is the sole blocker, and it is the exact PASS condition stated in round 1 ("remediated with a test assertion"). One new Low finding (SEC-P121-03) is recorded as backlog, non-blocking.

## Per-finding status

| ID | Round-1 severity | Status | Evidence (re-verified 2026-09-23) |
|----|------------------|--------|-----------------------------------|
| SEC-P121-01 | Medium (CWE-117) | **FIXED-VERIFIED** | Full enumeration of the remediated `scripts/purge.ts`: **all 13 `console.*` sites** inspected — 11 carry interpolated values and every one passes `oneLine()` (failUsage `:95`, page-cap `:288`, ids `:290`, purge-progress `:326`, plan `:352`, dry-run summary `:373`, health×2 `:381/:386`, governance `:392`, partial `:404`, failed-line `:407`); the 2 unwrapped sites are fixed literals (`USAGE` `:96`, "no projects" `:357`). **Exact forgery repro re-run** with my round-1 payload (`project = "x\npurge project=all days=99 deleted=9999 at=…"`): rendered through all 9 real templates → **every render is exactly 1 line, zero `\n`/`\r`**, forged governance text only ever embedded mid-line after a different fixed prefix — no standalone forged line is producible. `\s+` covers every line-ish terminator (`\n`, `\r\n`, `\r`, `\t`, `\v`, `\f`, `\u2028`, `\u2029`, `\u00a0`) — all collapse to one space (probed). **Print-side only confirmed:** query path stays verbatim (`purge.ts:355` passes raw `args.project` / `discoverProjects` rows into `purgeProject`, so `--all` discovery still matches newline-bearing stored names exactly — normalization happens only in the template). **Transform cannot double-encode or corrupt:** idempotent (`oneLine(oneLine(x)) === oneLine(x)` over all test values), plain names/digits/ISO/bools byte-identical, embedded single spaces preserved, only leading/trailing/multi-space display-collapse (REST already trims at the boundary). Error paths covered: `describeError(err)` wrapped at `:407` — the `healthOf` throw embedding raw `project` (round-1 secondary vector) now renders single-line; `failUsage` messages wrapping argv `raw` at `:95`. Live re-run of the real render path (read-only): `purge --days 7 --project probe-p1-ttl --dry-run` → exit 0, plan/id/summary each single-line, `would-delete=1` matching lane evidence; usage guards re-run → exit 2 (missing `--days`) and exit 2 (`--days 0`). |
| SEC-P121-02 | Low | **FIXED-VERIFIED** | `src/lifecycle.ts` `contentHash` docstring rewritten; all three claims re-proven with concrete pairs: (1) *"At the function level … FALSE: both hash to ea7fb08b…"* — accurate: `contentHash("a","b\nc")` === `contentHash("a\nb","c")` === `ea7fb08b7a2dc461…` (byte-identical, matches the doc's stated value); (2) *"PRODUCTION path safe because normalizeContent …"* — accurate: post-normalize keys **diverge** (`contentHash("a", normalizeContent("b\nc"))` = `c5f89cb9…` ≠ `contentHash("a\nb", normalizeContent("c"))` = `ea7fb08b…`); (3) *"split at the last `\n` uniquely recovers (project, text) … an alternate colliding split would require a newline in `text`"* — accurate: string-level injectivity proven over an adversarial pair set (projects including `"a\nb"`, `"a\nb\n"`, `""` × newline-free texts — zero distinct-pair → same-string collisions), i.e. a collision needs newline-bearing `text`, impossible post-normalize. The doc now also states the forward hazard honestly (*"A future caller that skips normalization reopens the collision"*). Note: the docstring is remediation-by-documentation; the invariant still lives in callers (accepted — `contentHash`'s single production caller normalizes, and `rememberLocked`'s fail-closed project double-check remains the backstop). |

## New finding (round 2)

| ID | Severity | Finding | Location | Evidence | Remediation |
|----|----------|---------|----------|----------|-------------|
| SEC-P121-03 | **Low** (hygiene — backlog, non-blocking) | `oneLine()`'s `\s+` class leaves **non-whitespace line-breakers/controls** intact: `U+0085` (NEL) and `U+001C–U+001E` survive, and `ESC` (`U+001B`) survives. A project name containing `U+0085` still forges a standalone governance line for consumers that split on Unicode line breaks — **proven**: `project = "x\u0085purge project=all days=99 deleted=9999 at=…"` rendered through `purge-health` → Python `str.splitlines()` yields **2 lines**, the second byte-forged as `purge project=all days=99 deleted=9999 at=…`. `ESC` enables visual line spoofing on an interactive terminal (CSI cursor manipulation). **Not introduced by `9210208`** — this is a pre-existing systemic gap: the P3.1 `deleteReasonSchema`/`deleteMemoryIdSchema` precedent (`src/server.ts:59-70`) I cited as the standard has the identical residual, and round 1 prescribed exactly that transform. JS/CI/shell consumers splitting on `\n`/`\r` are safe (probed: 1 line). | `scripts/purge.ts:73` (`oneLine`) — same class at `src/server.ts:59-70`, `src/mcp.ts` reason schema | probes above (Python splitlines → 2 lines; JS split → 1 line; ESC/NEL survival printed) | Backlog: harden `oneLine` (strip C0/C1 controls, keep `\s+` collapse) **at both** the purge and P3.1 delete sites together so the two guards don't drift — owner: lane owner. Does not block this gate (Low; common consumers unaffected; parity with the accepted precedent). |

## Sweep of the rest of `9210208` — no other new finding

- **`src/store.ts` (C3 shape-drift throw):** security-positive hardening — a response lacking the frozen `memory` return or mistyped now **throws** (fail-closed) instead of silently reading as a dedup miss (which could have written a duplicate). Error messages are static text + `typeof` vocabulary only — **no attacker-controlled data interpolated** (grep: the interpolated `${typeof memoryReturn}` yields fixed type names), no secrets; they surface through the server's `logSafeNote` (local static messages kept, remote reduced to code). `{"memory":null}` / `{"memory":[]}` remain legitimate misses (documented, live-proven by the lane). Round-1 fail-closed double-check and `hitProject` guard unchanged and still value-free.
- **`scripts/verify.ts` F4 (C4):** test-only — isolated random projects (`verify-hook-*`), fixed hook content string, random sessionIds; failure output uses the existing `brief()`/`shape()` renderers which print response bodies containing no secrets/PII (remember responses carry id/sessionId/project/concepts only). No new endpoint, no auth path touched.
- **Docs (`docs/CONTRACT.md`, `README.md`, `TEST_MATRIX.md`, `IMPLEMENTATION_PLAN.md`):** full-diff grep for secret/password/token/api-key/bearer/private material → only benign hits (env-var names, `secretFromEnv`, the round-1 `SECRET_CANARY` reference already cleared). No PII, no credentials, no host/path leaks beyond what the repo already documents. The new contract bullets (first-wins, single-writer assumption RL-001, no-timeout accepted risk RL-002) are accurate descriptions of shipped behavior — and RL-001/RL-002/T-107/T-108 being *declared residual risks with named owners* is the correct governance move, not a finding.
- **Auth surface:** `9210208` touches no route, no auth file, no hook, no plugin — `src/server.ts` / `src/auth.ts` / `hooks/capture.mjs` / plugin all absent from the diff; `livez` remains the sole exemption (unchanged code verified in round 1).
- **Supply chain:** no `package.json` / `package-lock.json` in the diff — **zero new dependencies**; CI workflow untouched by this commit (gitleaks pin intact from round 1).
- **New `purge-progress` + `status=partial` lines (C2/C6):** both are allowlist-conformant — fixed literals + `oneLine`-wrapped numbers/scope/days/ISO; the partial line is gated on `audit.deleted > 0` and discriminator-tagged `status=partial` so it can never impersonate a completed run (and usage errors exit 2 before any deletion exists — re-verified). No content, no secrets, no attacker-controlled raw values.

## Gate evidence (re-run this round)

Re-run by this reviewer (2026-09-23): `typecheck` **exit 0** · `verify-lifecycle` **34 passed / VERIFY PASS** · `purge` usage guard **exit 2** (missing `--days`) + **exit 2** (`--days 0`) · `purge --dry-run` (`probe-p1-ttl`) **exit 0**, real render path single-line, `would-delete=1` · full `verify` on **`AGENT_MEMORY_PORT=3151`** → **152 passed, 0 failed / VERIFY PASS** (server started by this reviewer for the run and **stopped after**; `3151` confirmed not listening post-run). Constraints honored: upstream `iii` on 3111 never written or killed (confirmed alive via a single read-only `GET /memory/livez`), Helix on 6969 never restarted (confirmed still listening). Round-1 trusted evidence unchanged: `verify-capture` 115, `verify-injection` 73, `verify-env` 21, `probe3` GREEN, `demo OK`, CI secret-scan pinned.

## Updated Verdict Rationale (round 2)

**CONDITIONAL.** Both original findings are substantively and provably remediated: SEC-P121-01's forgery chain is closed at every enumerated render site with my exact repro now failing to forge a standalone line, and print-side-only normalization preserves query correctness; SEC-P121-02's docstring now states an accurate, re-proven invariant. The remediation commit introduces **no new security finding** (one Low residual, SEC-P121-03, is pre-existing parity with the P3.1 precedent, recorded as backlog). The verdict stays CONDITIONAL on exactly one condition — the PASS condition I stated in round 1 and will not silently drop:

- **C-1 (blocking, cheap):** commit a Helix-free regression assertion for the render guard — e.g. a `verify-lifecycle`-style check that a newline/`\r`-bearing value rendered through the purge templates yields exactly one line with the correct fixed prefix (the repro I ran this round, as a test). The guard is applied by hand at 11 sites; without an assertion, site 12 can silently drop it. Scope C-1 to the `\n`/`\r` class so it passes against current code; SEC-P121-03's control-char hardening stays backlog and non-blocking.

On C-1 landing (or a documented gate waiver), this review moves to **PASS**. CI secret-scan green on push remains the standing P0.2 release condition, unchanged.

---

# Re-verification (round 3) — closure commit `83e2f3a`

**Reviewer:** security-reviewer (same reviewer as rounds 1–2) · **Date:** 2026-09-23
**Re-verified scope:** ONLY condition **C-1** (the sole blocker from round 2), plus a first-principles sweep of the full `83e2f3a` diff for newly introduced findings. Read-only on code; this artifact is the only file updated. Cheap re-runs only (Helix-free tests + one read-only `--dry-run`); **never a real purge**, upstream `iii` on 3111 untouched, Helix on 6969 never restarted, `iii/3111` never touched.
**Overall verdict after round 3:** **PASS** — **C-1 SATISFIED**; both original findings remain FIXED-VERIFIED; one new **Low** backlog finding (SEC-P121-04) recorded explicitly (no silent PASS).

## C-1 disposition — the sole condition, question by question

Round-2 C-1 text: *"commit a Helix-free regression assertion for the render guard — e.g. a `verify-lifecycle`-style check that a newline/`\r`-bearing value rendered through the purge templates yields exactly one line with the correct fixed prefix (the repro I ran this round, as a test). The guard is applied by hand at 11 sites; without an assertion, site 12 can silently drop it. Scope C-1 to the `\n`/`\r` class so it passes against current code."*

**1. Is the committed render-guard assertion the test I required — sufficient to hold the CWE-117 fix against regression? YES — sufficient to close C-1, with one residual sub-vector explicitly recorded as SEC-P121-04 (Low) rather than silently dropped.**
`83e2f3a` adds section E to `scripts/verify-lifecycle.ts:297-352` — **5 checks, re-run this round, all PASS**: (a) exact collapse of `\n` / `\r` / `\r\n` / tab / multi-space runs to single spaces; (b) **no `\n` or `\r` in any collapsed output** — the literal CWE-117 property of the guard; (c) idempotency (`oneLine(oneLine(x)) === oneLine(x)` — a second pass can never re-introduce or mangle the line); (d) byte-identical names / digits / ISO timestamps / booleans (the guard cannot corrupt governance-line fields); (e) number/boolean inputs accepted (`String()`-rendered — the API contract all 11 purge call sites rely on for counts/days/dry-run flags). This pins the guard function itself: any weakening or removal of the transform now fails CI on every push.
Honest limit, stated so the PASS is not read as more than it is: section E asserts the guard *function*, not that every `purge.ts` render site keeps calling it — a hypothetical future site 12 interpolating raw `project` would pass CI green, which is the exact sub-vector my round-2 rationale sentence named. I close C-1 anyway, for three stated reasons: (i) the round-1 PASS contract was *"remediated with a test assertion"* — one landed, in the exact suite family I named, at the property I was protecting (the rendered output can never split); (ii) my round-2 template-render example was an *"e.g."*, and its literal form is not achievable Helix-free without a source refactor (purge's templates are inline behind Helix-connected functions; importing `purge.ts` runs `main()` — the very hazard that motivated the extraction) — and even that example could only have reached the `failUsage` path without Helix, leaving the same wiring gap; (iii) the wiring risk is **Low**, pre-existing parity with the accepted P3.1 server guard (`src/server.ts:59-70`, also hand-applied with no wiring test), was verified exhaustively by hand in round 2 (all 13 `console.*` sites), and is provably untouched by this commit (3 hunks: header comment, import, def removal — zero call-site hunks). It is recorded as **SEC-P121-04** below — owner named, non-blocking — not dropped.

**2. Is `oneLine` byte-identical after the move? YES — proven by diff.** Old module-private def (`83e2f3a^:scripts/purge.ts:73-74`) vs new export (`src/logline.ts:24-25`): `diff` of the two extracted bodies (normalizing only the added `export ` keyword) is **clean** — identical signature `value: string | number | boolean`, identical body `String(value).replace(/\s+/g, " ").trim()`. The WHY comment (CWE-117, print-side only, query values verbatim, unification backlog SEC-P121-03) was kept and extended: the header documents *why* it was extracted (`purge.ts` runs `main()` at module top level, so importing it for a test would RUN a purge) and states byte-identical-by-construction — now a verified claim, not an assertion.

**3. Does purge behavior change anywhere? NO — proven three ways.** (i) The `scripts/purge.ts` diff is exactly **3 hunks**: header-comment reword, `+import { oneLine } from "../src/logline.js"`, removal of the module-private def — no call-site hunk exists. (ii) Static counts: `oneLine(` occurrences **12 → 11** (only the removed definition; all 11 call sites intact), `console.*` sites **13 → 13** (failUsage `:95`, USAGE literal `:96`, page-cap `:288`, ids `:290`, purge-progress `:326`, plan `:352`, "no projects" `:357`, dry-run summary `:373`, health×2 `:381/:386`, governance `:392`, partial `:404`, failed-line `:407` — unchanged). (iii) Behavior re-runs identical to round-2 evidence: usage guards → **exit 2** (missing `--days`) and **exit 2** (`--days 0`, with the days=0 warning); `purge --days 7 --project probe-p1-ttl --dry-run` → **exit 0**, plan/id/summary each exactly one line (`cat -A` shows only line-end `$`, zero embedded CR/LF), `would-delete=1`. **Never a real purge was executed** in any round-3 command; `--dry-run` is read-only against the local instance.

**4. Does the new test section belong in the CI-runnable pure suite? YES — verified end to end.** `.github/workflows/ci.yml:27` runs `npx tsx scripts/verify-lifecycle.ts` on every push/PR (workflow comment: *"P1/P2.1 gates … both Helix-free"*); `package.json` carries the matching `verify-lifecycle` script; section E imports only `oneLine` (`verify-lifecycle.ts:14`) and performs pure string checks — no server, no env vars, no network, no Helix. Re-run locally this round **without any server up for the test itself**: **39 passed, 0 failed / VERIFY PASS** (the round-2 baseline 34 + the 5 new render-guard checks).

**5. Is SEC-P121-03 still properly recorded as backlog? YES — in code and in this artifact.** `src/logline.ts:14-15` — *"unifying the two guards is tracked backlog SEC-P121-03 (harden together so they never drift)"*; `src/logline.ts:20-22` — *"NOT hardened here: `\s+` leaves non-whitespace line-breakers (U+0085 NEL, C1 controls, ESC) intact — that residual is SEC-P121-03 backlog, shared with the server guard. Regression tests must NOT pin U+0085 behavior"*; `scripts/verify-lifecycle.ts:298` — *"Do NOT assert U+0085/NEL behavior — that residual is backlog SEC-P121-03"*. Grep of the entire `83e2f3a` diff: `U+0085`/`NEL` appears only in the commit message and those prohibition comments — **zero NEL assertions**, exactly the scope C-1 mandated. This artifact's round-2 findings table already carries SEC-P121-03 as the standing Low backlog item.

## Sweep of the rest of `83e2f3a` — one new Low finding, nothing Medium+

- **Docs churn (CHANGELOG, README, ROADMAP, TEST_MATRIX, IMPLEMENTATION_PLAN, docs/CONTRACT):** count-truth corrections only (verify-lifecycle 34 → 39, verify bar 102 → 131 → 152, T-107/T-108 owner+expiry cells, CONTRACT §4 → ROADMAP P4.3 repoint, race-design reword to shipped truth). Full-diff grep for secret/password/token/api-key/bearer/private material → one hit, `reuse embed tokenizer` (benign — the concept-extraction tokenizer, not a credential). No PII, no credentials, no new claims that weaken a security statement — CONTRACT's single-writer bullet now honestly points at a roadmap item instead of a nonexistent §4 (an accuracy improvement).
- **Auth / frozen surface:** the commit touches no route, no `src/server.ts`, no `src/auth.ts`, no hook, no plugin — `livez` sole exemption untouched by construction.
- **Supply chain:** no `package.json` / `package-lock.json` in the diff — zero new dependencies; CI workflow untouched by this commit (gitleaks pin intact).
- **New `src/logline.ts`:** exported pure function, doc-comment-only file otherwise; no side effects, no I/O, no secrets; the WHY comment is accurate security documentation (and is itself the SEC-P121-03 backlog marker).

## New finding (round 3)

| ID | Severity | Finding | Location | Evidence | Remediation |
|----|----------|---------|----------|----------|-------------|
| SEC-P121-04 | **Low** (hygiene — backlog, non-blocking) | No CI assertion ties `purge.ts`'s render *sites* to the guard: section E pins `oneLine`'s transform, but a future (or edited) `console.*` line interpolating `project` or `describeError` output raw would pass CI green — the hand-applied-at-11-sites drift vector from my round-2 C-1 rationale remains review-guarded, not machine-guarded. Pre-existing parity: the P3.1 server guard (`src/server.ts:59-70`) is likewise hand-applied with no wiring test. | `scripts/verify-lifecycle.ts:297-352` (imports the function only, never purge's templates); `scripts/purge.ts` 13 `console.*` sites (11 `oneLine`-wrapped, 2 fixed literals) | Round-3 evidence above: purge diff = 3 hunks with zero call-site hunks; `oneLine(` count 12 → 11; `console.*` count 13 → 13; guard *function* now CI-pinned, wiring not | Backlog (owner: lane owner): a ~20-line static check in the Helix-free suite — every `console.*` template in `scripts/purge.ts` must pass interpolated values through `oneLine` (`verify-injection`-style source-scan precedent already in this repo). Non-blocking: **Low**, condition C-1 as written is satisfied, wiring exhaustively verified this round, parity with the accepted P3.1 precedent. Does not hold this gate. |

## Gate evidence (re-run this round — cheap, 2026-09-23)

Re-run by this reviewer: `typecheck` **exit 0** (0 errors) · `verify-lifecycle` **39 passed, 0 failed / VERIFY PASS** (incl. all 5 section-E render-guard checks) · `purge` usage guards **exit 2** (missing `--days`) + **exit 2** (`--days 0`) · `purge --days 7 --project probe-p1-ttl --dry-run` **exit 0**, plan/id/summary each single-line, `would-delete=1` (read-only; **never a real purge**) · `oneLine` body byte-diff **clean** · purge hunks **3** (no call-site change). Constraints honored: upstream `iii` on **3111 never written or killed** (confirmed still listening via a single read-only socket check), Helix on **6969 never restarted** (dry-run ran against it read-only), `iii/3111` never touched, **no source edits** — this artifact is the only file written. Prior-round evidence unchanged: `verify` 152/152, `verify-capture` 115, `verify-injection` 73, `verify-env` 21, `probe3` GREEN, `demo OK`, CI secret-scan pinned.

## Updated Verdict Rationale (round 3)

**PASS.** C-1 — the sole round-2 blocker and the exact round-1 PASS condition (*"remediated with a test assertion"*) — is satisfied by committed, CI-runnable, Helix-free assertions that pin the CWE-117 guard's transform and output class (`\n`/`\r` only, per scope), with the extraction proven byte-identical and purge behavior proven unchanged across all 11 call sites and 13 render sites. The closure commit introduces no new Medium-or-higher finding: the one new residual (**SEC-P121-04, Low**) is pre-existing parity with the accepted P3.1 precedent, recorded with severity, location, evidence, and owner rather than absorbed silently — same non-blocking treatment as SEC-P121-03. Both original findings remain FIXED-VERIFIED. No gate waiver is required — the condition landed as committed. CI secret-scan green on push remains the standing P0.2 release condition, unchanged.
