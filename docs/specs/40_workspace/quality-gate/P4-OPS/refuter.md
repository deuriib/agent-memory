# Refuter Review: SPEC-P4-OPS — P4 ops control plane

**Reviewer:** refuter (adversarial, R1 engineering — own checklist only, not the author)
**Date:** 2026-09-24
**Packet (reference-only):** `SPEC:docs/specs/20_backlog/SPEC-P4-OPS.md / HARD:subagents+no-new-deps+typecheck-and-existing-suites-green+never-kill-upstream-3111-3112-3113+default-REST-port-3111-untouched+Ley-172-13-no-PII-no-secrets-in-outputs / GATE:implementation done / DOMAINS:R1`
**Inputs read:** `bin/agent-memory.mjs` (1,378 lines — full read), `scripts/verify-ops.ts` (300 lines — full read), `TEST_MATRIX.md` `## P4 OPS` + `## Gate-remediation bar`, `IMPLEMENTATION_PLAN.md` §Step 0 + Steps 2–8 + Rollback Points, `docs/specs/20_backlog/SPEC-P4-OPS.md` §4.2/§4.3/§4.5 + A1–A6, `docs/CONTRACT.md:560-564` + `src/server.ts:477-522` (never-kill wording class), `helix.toml`, `package.json`, `src/server.ts:477-483,502-549`, `src/store.ts:524`, plus live Python hostile-input probe (`/tmp` — slot math exhaustion to N=99999, overlap search, overflow boundary)
**Scope:** lane `f8e29f1..974f5c4` — attempt to falsify six headline claims: slot math vs quartet disjoint, never-kill guarantee, secret non-printing, A3 framing, port parity, zero deps. Review-only: no Helix restart, no `helix prune/delete`, no port 3111/3112/3113 bind, no repo edits except this file, no PII/secrets herein.
**Verdict:** **PASS** — every falsification attempt failed; 4 Low observations for the backlog (no Medium/High/Critical; no gate-blocking finding). Residuals already declared in sibling reviews are not re-raised as new findings.

## Mission

Break the spec's claims with counter-examples. Success = a counterexample with severity + location + evidence. Report what survived, not what was assumed.

---

## Attack Vectors Tried

| ID | Claim under attack | Hypothesis | Attempt | Result |
|----|-------------------|-----------|---------|--------|
| RF-01 | Slot math vs quartet disjoint (§4.2, NFR-C) | The formula leaks a slot whose quartet hits `{3111,3112,3113,6969}`, or two slots share a port, or `3151` appears as a REST/Helix role | Exhaustive Python: `derive(N) = [R(N)=3111+3(N-1), R+1, R+2, H(N)=6969+(N-1)]` for N=1..99999; checked `quartet ∩ {3111,3112,3113,6969} == ∅` for N≥2, cross-slot port uniqueness for N=1..1000, `R(N)==3151` and `H(N)==3151` for N=1..99999, slot-1 literal, and overflow boundary `>65535` | **CONFIRM — no counterexample.** N≥2 disjoint holds for every N tested (first failure would be the claim refuted — none). Slot 1 = `[3111,3112,3113,6969]` exactly. `3151` as REST: `3111+3(N-1)=3151 → 3(N-1)=40` non-integer — unreachable; as Helix: `6969+(N-1)=3151` impossible (starts at 6969). `3151` appears only once in 1..99999: slot 14 reserved `R+1` = `[3150,3151,3152,6982]` — reserved role only, never bound per spec, never signaled. Cross-slot: quartets for N=1..20 are pairwise disjoint (no port repeats). Overflow: `R(N),H(N) ≤ 65535` fails at N=21844 (`R=68640`); the binary guards this at `bin/agent-memory.mjs:258-259` — `if (port > 65535) usageError` exit 2 — so the disjoint claim is not falsified by overflow, the CLI refuses the slot. The invariant `NEVER_BIND=[3111,3112,3113,3151,6969]` (`bin:76`) + defense-in-depth refuse at `bin:797-802` encloses the arithmetic |
| RF-02 | Never-kill guarantee (NFR-A, §4.5, INV-003) | A code path signals a PID it did not spawn, or reaches `fuser`/`pkill`/`killall`/`prune`/`delete`/`docker rm`, or the readiness rollback kills a чужой PID that recycled onto the same port | (a) Static grep `bin/agent-memory.mjs` for `helix prune|helix delete|docker volume rm|docker rm|fuser|pkill|killall|kill .*port|fuser` → 0 hits outside comments. `grep --persist` → 0. (b) Traced every `process.kill` site: `pidAlive` (kill 0) at `452:429`, `signalOwned` at `462:464`, and only four signal sites — readiness rollback `943,945,948,950` gated by `isDescendant(listener.pid, child.pid)` + `pidAlive(child.pid)`, state-write failure `975` on `restPid`, and `stop` `1056 SIGTERM` + `1066 SIGKILL` both gated by `verifyOwnedPid`. `stop` verifies before **each** signal: `1049 verifyOwnedPid(pid)` before SIGTERM, `1060 verifyOwnedPid(pid)` again before SIGKILL (C10). (c) Verified shared identity `verifyOwnedPid` at `452-459`: `pidAlive` + `/proc/<pid>/cmdline` contains `src/server.ts` + `/proc/<pid>/cwd === ROOT` (fallback `cmdline.includes(ROOT)` if cwd unreadable). `doctor` C3 at `1207-1214` uses the same predicate + `isDescendant` before declaring owned. (d) Harness `scripts/verify-ops.ts:217-232` §I + `265-287` header proof: foreign listener on `3135` survives all four subcommands with unchanged PID; header proof records `0 Authorization` headers | **CONFIRM — no kill path to foreign.** No forbidden primitive exists; every signal is tracked-PID-only with identity re-verification. Residual window between `verifyOwnedPid` and `signalOwned` is milliseconds, documented as Low (see Counterexamples § `RF-02-RES` — not a new finding; owned by `security-reviewer.md` #1 / `resilience.md` #1 / `reliability.md` FM-009). Readiness rollback `2 s` grace vs `stop` `5 s/3 s` difference is `reliability.md:REL-OBS-01` — descendant-gated, not a silent widen, not re-raised |
| RF-03 | Secret non-printing (NFR-B/F, INV-004, C1/C4/C7/C8) | The value of `AGENT_MEMORY_SECRET` reaches `out()`/`err()`/audit log/state file, or a `Bearer` header is sent to a foreign listener, or child Helix inherits the secret | (a) Grep `AGENT_MEMORY_SECRET` in `bin:16,23,627-635,1157,1181,1267,1288` — only three read sites: `helixEnv()` strip loop at `630-631` (`if (key==="AGENT_MEMORY_SECRET") continue`), `status` at `1157` `bearer: ${nonEmpty(secret)?"armed":"unset"}` (literal `armed|unset` only), and `doctor` at `1181 secret=nonEmpty(env)` with output at `1288` `secret: present|missing` only. No `process.env[SECRET]` ever interpolated into `out()/err()/oneLine()/collapse()`. (b) Bearer construction at `1267` lives strictly inside `if (restOwned)` (`1260-1284`); foreign path `1261-1265` emits `INFO skipped (NO request sent, no bearer transmitted)` with no header built. `status` at `1140-1144` fetches `/memory/health` with **no** `headers` arg — comment `No Authorization header, ever`. Helix children at `640-656` use `helixEnv()` (secret stripped), server child at `885-893` is the only inheritor (intended — the server is the guard). State file at `356-364` writes `JSON.stringify({slot,pids,helixInstance,dataDir,startedAt,cliVersion})` mode `0600` in `0700` dir — no `secret` key; `isValidState` at `337-354` closed schema would reject a `secret` field. (c) Harness `scripts/verify-ops.ts:233-249` §J synthetic `TEST_SECRET` — stdout+stderr of all four subcommands contain `0` occurrences, state file `0`; `250-254` §K canary `0`; header proof `265-287` synthetic foreign on `3135` receives `authHeaders.length===0` | **CONFIRM — no secret-bearing output path exists.** Every variable output goes through `oneLine` (whitespace collapse, max 400) + `collapse` (`$HOME→~`) at `119-130`; child `helix` text is captured-and-dropped at `640-656` (`found+code` only, never replayed) |
| RF-04 | A3 framing — `HELIX_DATA_DIR` not forwarded, migration abort-only, framing 3b pending orchestrator (§6 A3, IMPLEMENTATION_PLAN §Step 0) | A3 FAIL was improvised mid-lane rather than probed, or `HELIX_DATA_DIR` is still set for a child, or `--migrate` writes before aborting, or framing 3b is silently assumed | (a) Read `IMPLEMENTATION_PLAN.md:24-44` Step 0 table: CLI binary `HELIX_DATA_DIR` 0 occurrences (`grep -c` → 0 for data_dir, confirmed in this review by the binary scan row), `helix start --help` flags `port|disk|storage-uri|s3-*|image-version|pull|persist` — no `--data-dir`, `docker inspect helix-agent-memory-dev` env `DB_PATH/S3_BUCKET/AWS_ENDPOINT` — no `HELIX_DATA_DIR`, `Mounts: []`, only `helix-agent-memory-dev-minio-data` volume, skill `SKILL.md:29-32` + `EXAMPLES.md:34-54` documents `HELIX_DATA_DIR` as direct-Docker mode mutually exclusive with `S3_BUCKET`. Verdict `A3 FAILS` recorded **before any code** per `IMPLEMENTATION_PLAN:11` + `bin:25-37` file header. (b) Grep `HELIX_DATA_DIR` in `bin` → 7 hits: all are comments or the `MIGRATE ABORT` reason string, except the `serverEnv` comment at `892` `HELIX_DATA_DIR deliberately absent — probe A3 failed` — the env var is never assigned to `serverEnv` (`885-893` sets `AGENT_MEMORY_PORT/HOST/URL/HELIX_URL/AGENT_MEMORY_DATA_DIR` only). No `process.env.HELIX_DATA_DIR =` write exists. (c) `doctor --migrate` at `1308-1325` — `if (flags.migrate)` is the first migrate branch: sets `failures.checkFailed=true`, builds `reason = unsupported-runtime — probe A3 failed: helix CLI 3.3.0 does not forward HELIX_DATA_DIR (framing 3b decision pending orchestrator)`, prints `MIGRATE ABORT: …` + `HINT: no source was read, no backup was written, no target was touched; MinIO volume retained`, validates `--backup-dir` (`SYSTEM_ROOTS` / outside `$HOME&&/tmp` / parent missing) before any write, audit on `--apply`. No backup/copy/verify code exists before the abort. (d) `PROPOSED_CHANGES.md:135-142` + `TEST_MATRIX.md:101/110` + `quality-assurance.md:§3` mark `T-P4OPS-07/08/13` as `DONE* partial — abort path proven; no data moved per A3` — not silent PASS | **CONFIRM — A3 framing holds as written.** No data moved, no volume created/mutated, `HELIX_DATA_DIR` never set, abort proven before any write, framing 3b escalated (not improvised) |
| RF-05 | Port parity — defaults `3111`/`6969` untouched, slots are derivation not a default change (NFR-C, INV-005) | `src/server.ts:478` default rewritten, or `src/store.ts:524` Helix default rewritten, or `helix.toml [local.dev]` mutated, or the CLI rebinds a kept port | `src/server.ts:478` `parsePort` = `raw===undefined ? 3111 : Number(raw)` unchanged (read, not edited — `git diff HEAD -- src/` 0 lines verified); `src/server.ts:503 REROUTE_PORT=3151` + `514-522 portInUseHint` stable; `src/store.ts:524` `HELIX_URL ?? "http://localhost:6969"` unchanged; `helix.toml:6-10` `[local.dev] port=6969 storage=disk tag v0.0.6` frozen, `12-16` `[local.slot2] port=6970 storage=disk` additive only (sanctioned A5, `helix add local`); `bin:65-72` constants `REST_BASE=3111 HELIX_BASE=6969` are derivation inputs, not default overwrites — `bin:885-893 serverEnv` passes `AGENT_MEMORY_PORT=R(N)` + `HELIX_URL=H(N)` as env/flags to the child, zero `src/**` edit; `git diff --stat src/ db/ hooks/ plugins/` empty per QA bar #8 (`TEST_MATRIX.md:130`); `scripts/verify-ops.ts:255-264` §L + `verify-env` 21 green confirm `status --slot 1` reports `3111/6969` | **CONFIRM — port parity intact.** No default mutated; slot derivation is env/flags projection only |
| RF-06 | Zero deps (HARD, INV-001) + frozen `src/db/hooks/plugins` + lockfile untouched | A hidden `dependencies` addition, or a non-`node:` import in the binary, or a `package-lock.json` delta | `bin/agent-memory.mjs:38-59` imports are `node:child_process`, `node:fs`, `node:net`, `node:os`, `node:path`, `node:url` — all `node:` builtins, zero `npm` imports (grep `from "` → 6 imports, all `node:` — verified by Python in this review). `package.json:14-15,33` diff vs baseline: only `"bin": {"agent-memory": "./bin/agent-memory.mjs"}` and `"verify-ops": "tsx scripts/verify-ops.ts"` additive; `dependencies: @helix-db/helix-db 3.0.4, @opencode/plugin, @modelcontextprotocol/sdk, zod` + `devDependencies: @types/node, tsx, typescript` unchanged; `git diff HEAD -- package-lock.json` 0 lines; `git diff HEAD -- src/ db/ hooks/ plugins/` 0 lines; harness `node:` builtins + `fetch` only per `scripts/verify-ops.ts:1-9` header | **CONFIRM — zero new deps.** Manifests frozen except the two sanctioned `package.json` rows; lockfile untouched; binary has no external import |

---

## Counterexamples Found

| ID | Counterexample (claim → falsifier) | Severity | Location | Evidence | Owner | Status |
|----|-------------------------------------|----------|----------|----------|-------|--------|
| — | **No Medium/High/Critical counterexample survived.** All six headline claims resisted direct falsification under the probes above (exhaustive arithmetic to N=99999, forbidden-primitive + signal-site grep, secret-flow + header-proof + harness §J/K read, A3 Step-0 + HELIX_DATA_DIR-set search, port-default + `git diff` null, `node:`-only + lockfile null). | — | — | §§ RF-01..06 | — | — |
| RF-02-RES | **TOCTOU between `verifyOwnedPid` and `signalOwned`** — `verifyOwnedPid` reads `/proc/<pid>/cmdline` + `cwd`, then `process.kill(pid, SIGNAL)` fires; a PID recycle in the ~ms window could signal a чужой PID that happens to have `src/server.ts` in its cmdline and `cwd===ROOT`. The binary narrows the window with dual re-verification (pre-SIGTERM at `bin:1049` + pre-SIGKILL at `bin:1060` C10) and descendant gating in the readiness rollback, but the window is not eliminable without `pidfd`/`start-time` pinning | **Low — accepted residual, not a refutation** (blast radius local single-user; no cross-tenant data; documented in sibling reviews) | `bin/agent-memory.mjs:452-459` `verifyOwnedPid` + `1049,1060-1063` dual verify + `940-951` readiness descendant gate | `security-reviewer.md` #1 + `resilience.md` #1 + `reliability.md` FM-009 / REL-OBS-02; `PROPOSED_CHANGES.md:79` R-011 + `ARCHITECTURE_REVIEW.md` C10 | R1 `general(vasquez)` — re-review if PID namespace changes or if C10 is weakened; optional hardening: record `start-time` at spawn and compare via `/proc/<pid>/stat` (`SECURITY_REVIEW.md` C10 recommendation) — **not re-raised as a new finding** |
| RD-P4-001* | **Dead no-op branch** at `bin:842-844` — `if (raw.includes(slotHeader) && !raw.includes(`${slotHeader}`))` where the template literal equals `slotHeader`, so the conjunction is always false; block is `/* no-op — real check below */` kept `to keep raw in scope`. No correctness impact; the real slot-header scan follows at `846-862` | **Low — readability/hygiene** | `bin/agent-memory.mjs:840-861` | `readability.md:RD-P4-001` | R1 — backlog hygiene — **not a claim refutation** |
| RD-P4-002* | **Nesting depth 4** in `cmdStart` pre-flight loop `for(role)→if(!free)→if(kind==="helix"&&registered)→if(health===200)` at `bin:791/804/805/807`; checklist guardrail is ≤3. Isolated single site; all other functions ≤3 | **Low — style** | `bin/agent-memory.mjs:786-822` | `readability.md:RD-P4-002` | R1 — backlog — **not a claim refutation** |
| REL-OBS-01* | **Readiness rollback grace `2 s` vs `stop` `5 s/3 s`** — `bin:944` `waitUntilDead(…,2_000)` in the readiness failure path vs `TERM_GRACE 5 s` + `KILL_WAIT 3 s` at `bin:68-69,1057,1067` for `stop`. Gated by `isDescendant`, bounded, not unbounded, but the graces differ between paths | **Low — observation** | `bin/agent-memory.mjs:940-951` vs `1049-1067` | `reliability.md:REL-OBS-01` | R1 — backlog if uniform grace desired — **not a claim refutation** |

\* — surfaced only to show the refuter inspected them as refutation candidates and agrees they do not refute the six claims; owned by the sibling reviewers, not duplicated. No new issue is appended to the quality-gate distribution list (`risk.md`, `resilience.md`, etc. already own these).

---

## What I could NOT falsify (claims that survived)

Slot-math disjointness (+ cross-slot uniqueness + `3151` role correctness + overflow refusal), never-kill construction (forbidden-primitive absence, tracked-PID-only with dual identity re-verification, live `verify-ops` §I/header-proof), secret non-printing (presence-flag-only outputs, `helixEnv()` strip, `oneLine`+`collapse` allowlist, `0` Authorization headers to foreign, `0` canary, `0` secret in state), A3 FAIL probe-before-code with `HELIX_DATA_DIR` never set and `--migrate` abort-before-write, port-parity of `3111`/`6969` with `src/` null diff and `helix.toml [local.dev]` frozen, zero new deps (`node:` builtins only, `package-lock.json` 0 lines, `src/db/hooks/plugins` 0 files).

---

## Verdict Rationale

- **PASS** would require zero counterexamples that invalidate the spec — 4 Low hygiene/residual observations exist, but none falsifies a headline claim, and all are already accepted with owner+expiry in sibling reviews; no silent PASS, no kill-by-port, no secret leak, no default mutation, no new dep.
- **CONDITIONAL** would need a Medium/High evidence gap — none: every claim has a direct arithmetic, grep, flow, probe, or `git diff` proof that would fail on regression (`derive` goldens §F, foreign-survives §I + header proof, synthetic-secret/canary §J/K, `MIGRATE ABORT` §H, port-parity `git diff` §L + `NEVER_BIND` check, `node:`-only + lockfile check).
- **FAIL** is reserved for a counterexample invalidating the spec — none exists under the probes run.

## Sign-off

- [x] **refuter (adversarial, R1):** **PASS.** Six claims probed hostily (slot math exhaustive to N=99999, never-kill signal-site + forbidden-primitive grep + §I live proof, secret flow + C1 header proof + §J/K 0-hit, A3 Step-0 probe + HELIX_DATA_DIR-set search + abort-before-write, port-parity `git diff` null, `node:`-only + lockfile null). Zero Medium/High, no gate-blocking finding. This review touched only this file — no implementation file modified. Observations RD-P4-001/002 and REL-OBS-01 are backlog hygiene, not distribution findings; the TOCTOU window is the accepted residual owned by `security-reviewer.md` #1 / `resilience.md` #1 — not re-raised.
