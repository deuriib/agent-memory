# Risk Review: SPEC-P4-OPS — P4 ops control plane

**Reviewer:** risk reviewer (engineering, R1 — risk matrix, residual risks, expiry)
**Date:** 2026-09-24
**Packet (reference-only):** `SPEC:docs/specs/20_backlog/SPEC-P4-OPS.md#Risks & Assumptions / DOMAINS:R1`
**Inputs read:** `docs/specs/20_backlog/SPEC-P4-OPS.md` §6 Risks R1..R7 + A1..A6, `docs/specs/40_workspace/engineering/PROPOSED_CHANGES.md` Risk Matrix R-001..R-012 + Residuals + Blast Radius + Rollback, `IMPLEMENTATION_PLAN.md` §Step 0 Probe A3 FAIL + Rollback Points, `TEST_MATRIX.md` ## P4 OPS + per-command bar #1-10 + *Notes* + ROADMAP §1.3 residuals, `bin/agent-memory.mjs` (1,378 lines — full read of abort/state/permission/kill paths), `helix.toml`, `package.json`, `ROADMAP.md` §1.3 RL-001 / RL-001-QUEUE / VERIFY-SESSION-NODES
**Verdict:** **PASS** — all 7 spec risks R1..R7 mitigated or carried as explicit residual with owner+expiry; zero unmitigated High/Critical; no silent PASS. A3 FAIL handled as reversible abort with no data moved.

## Checklist (per `frame-ship/skills/quality-gate/references/engineering/risk-review.md`)

- [x] **Risk matrix complete and evidenced** — R1..R7 each mapped to code + harness + TEST_MATRIX row; likelihood × impact stated; mitigation landed as code or probe-gated abort; residual explicit.
- [x] **Blast radius bounded and verified** — engineering / automation / security / customer split matches PROPOSED_CHANGES; `src/**`, `db/**`, `hooks/**`, `plugins/**` frozen (`git diff` empty), `package-lock.json` empty, `helix.toml [local.dev]` frozen, additive `[local.slot2]` only.
- [x] **Backward compatibility preserved (no breaking API/schema without ADR)** — REST 3111 / Helix 6969 defaults untouched (NFR-C); quartet derivation via env/flags only; slot 1 reproduces brief §3.2 literally; no index added (`bootstrap OK 8 indexes ensured` on 6970).
- [x] **Dependencies pinned; zero new CVEs / supply-chain hazard** — `node:` builtins only, `package-lock.json` untouched, `package.json` only `bin` + `verify-ops` additive, forbidden `prune`/`delete`/`docker rm`/`fuser`/`--persist` static 0.
- [x] **Rollback determinism proven (reversible ≤ immediate)** — delete `bin/agent-memory.mjs` + `scripts/verify-ops.ts`, revert two `package.json` rows, `git checkout -- helix.toml` removes `[local.slot2]`; live slot2 undo is one `agent-memory stop --slot 2` (named `helix stop slot2`, no volume mutation); post-revert proof = `typecheck` + five suites.
- [x] **Zero unmitigated High+ residual** — R1/R2 (High) both fail-closed (abort or backup-gated, MinIO never destroyed); all Medium/Low either landed or carried with named owner + dated expiry.
- [x] **Assumptions dated and falsifiable call respected** — A1 accepted 2026-09-24, A2 verified CLI 3.3.0, A3 probed before code → FAIL → framing 3b escalated (reversible, declared), A4/A5/A6 frozen as code invariants.
- [x] **Residuals carry owner + expiry and re-review trigger** — every carried risk names R1/R2/R8 + `2026-12-31` or P4.3 or framing-3b decision, whichever first; queue/session risks explicitly dated `2026-12-31`.

## Risk Matrix — SPEC-P4-OPS §6 R1..R7 (R-001..R-012 alias where PROPOSED_CHANGES expands)

| ID (SPEC) | ID (proposal) | Risk | Severity | Likelihood | Mitigation (landed code / probe) | Evidence | Residual | Owner | Status |
|-----------|---------------|------|----------|------------|----------------------------------|----------|----------|-------|--------|
| **R1** | R-001 | **Migration irreversible — live MinIO→native move, data-loss risk** | **High** | Low | Dry-run default (zero writes, plan prints source/target/counts/checksums); real move requires `doctor --migrate --apply --yes` in strict order backup→migrate→verify; mandatory verified backup archive `0600` in parent `0700` (umask-independent) before any copy; fail-closed abort on ANY error leaving source intact; MinIO volume **never destroyed** (`helix prune`/`delete`/`docker volume rm` forbidden, static 0). | `SPEC-P4-OPS.md:87-94,292-295`, `bin/agent-memory.mjs:1308-1325` abort-first branch + `HINT: MinIO volume retained` + `README.md:715-727` Ley 172-13 PII-store declaration (purpose=DR, location `~`-collapsed, TTL=deleted after verified migration+operator confirmation, deletion=`rm -rf <backup>`, content never logged), `scripts/verify-ops.ts:202-216` §H dry-run checksums identical + `MIGRATE ABORT` + `no backup file`, `TEST_MATRIX.md:110` T-P4OPS-08 `DONE* partial — abort path proven; no data moved per A3` | Operator runs `--apply` against wrong slot — bounded by `--yes` + per-slot target printed in dry-run plan (explicit, accepted). While A3 FAIL, no write path reachable at all — residual is latent. | R1 `general(vasquez)` + R8 `general(espinoza)` (evidence) | **MITIGATED + carried residual** |
| **R2** | R-002 / A3 | **`HELIX_DATA_DIR` not honored by `helix start` on image `v0.0.6` (assumption A3) → P4.4 degrades to framing 3b, KR3 path changes** | **High** | Medium | Falsifiable probe at execute-spec **step 0 before any code** (binary scan `HELIX_DATA_DIR` 0 hits, `helix start --help` no `--data-dir`, `docker inspect helix-agent-memory-dev` env no `HELIX_DATA_DIR` + `Mounts: []` + only MinIO volume, skill docs direct-Docker mode mutually exclusive with `S3_BUCKET`); verdict **A3 FAILS** → P4.4 forwarding + migration **STOPPED**, `--migrate` fails closed `MIGRATE ABORT: unsupported-runtime`, `--data-dir` survives only as state-path input, framing **3b escalated to orchestrator** as reversible call, never in-lane improvisation. | `SPEC-P4-OPS.md:281-286,296-297,A3`, `IMPLEMENTATION_PLAN.md:24-44` Step 0 table, `bin/agent-memory.mjs:25-37` header narrative + `:892` `HELIX_DATA_DIR deliberately absent` + `:1308-1322` abort-first, `TEST_MATRIX.md:109,134` `*Notes: P4.4 A3 FAIL framing 3b pending orchestrator` + `PROPOSED_CHANGES.md:135-142` Implementation notes appendix | Not mitigable pre-probe; now carried as explicit abort-only residual until framing-3b decision or Helix CLI upgrade. | R1+R8, escalated to **orchestrator** | **CARRIED — owner+expiry** (see Residual #1) |
| **R3** | R-003 | **Helix image runs as uid `65532` (`EXAMPLES.md:49`) — bind-mounted `--data-dir` may be unwritable** | Medium | Medium | Exact permission mode decided by probe A3; code enforces umask-independent modes: `ensureDir0700` (`mkdir 0700` + `chmod 0700` at `bin/agent-memory.mjs:303-310`), `writeState` (`writeFile 0600` + `chmod 0600` at `:356-364`), backup archive `0600` / parent `0700` per C2; `start` fails closed with actionable permission error rather than starting data-less; `--data-dir` path refusal set (`/`, system roots, `$HOME` itself, outside `$HOME && /tmp`) at `bin/agent-memory.mjs:274-288,727-757`; `--backup-dir` parent must exist+writable at `:1312-1318`. While A3 FAIL, Helix bind-mount not exercised — state-file `0700`/`0600` proven, Helix `0700`/`0600` deferred. | `SPEC-P4-OPS.md:298-300`, `bin/agent-memory.mjs:79-100,274-310,356-364,727-757,1312-1318`, `scripts/verify-ops.ts:177-201` §G `state …/slot-N.json 0600 / 0700 dir secret-free`, `TEST_MATRIX.md:124` bar #2 §G green, `IMPLEMENTATION_PLAN.md:81-82` zero residue | Helix `HELIX_DATA_DIR` `0700`/`0600` on live bind-mount not yet exercised in vivo (A3 FAIL → abort-only). State-path modes proven; Helix-mode proof deferred to first live backup. | R1 `general(vasquez)` | **MITIGATED for state-path; CARRIED for Helix bind-mount** (see Residual #2) |
| **R4** | R-004 | **`--persist` would rewrite tracked `helix.toml` (port/storage/tag)** | Medium | Low | `--persist` is forbidden primitive (`SPEC-P4-OPS.md:240-244,301-302` + `bin` hard-rules header `:1-24`); static scan finds `helix prune`/`delete`/`docker volume rm`/`docker rm`/`fuser`/`--persist` as arg 0 hits; `package-lock.json` empty; `helix.toml [local.dev]` frozen. | `SPEC-P4-OPS.md:301-302`, `bin/agent-memory.mjs:640-656` `runHelix` pipe + capture, `scripts/verify-ops.ts:217-232` §I static grep, `TEST_MATRIX.md:130-131` bars #8/#9 empty | None — primitive absent from source, proven statically + live foreign-survives. | R1 | **MITIGATED** |
| **R5** | R-005 + R-010,R-012 | **RL-001 boundary re-review at start of P4.3 + RL-001-QUEUE + VERIFY-SESSION-NODES expiry** | Medium | Medium | Flagged in proposal, **not waived** by this lane; slots keep one Helix per slot by construction (R6); lane declares exclusive live windows (`REQ-OPS-RUN-11`, max 2 parallel lanes INV-006) and pastes ports/instances/window/budget in TEST_MATRIX; ROADMAP §1.3 `VERIFY-SESSION-NODES +17/run` measured 490→507→524 and `RL-001-QUEUE no cap/no deadline ≤165 s` both carried; engineering owner schedules both alongside this lane. | `SPEC-P4-OPS.md:303-306,310-312`, `PROPOSED_CHANGES.md:73-78`, `ROADMAP.md:62-73` §1.3 table, `TEST_MATRIX.md:77-89` RK-01 envelope, `IMPLEMENTATION_PLAN.md:54-56` live window exclusive | Re-review still due with owner+expiry — not waived here. | R1 `general(vasquez)` (schedules) | **CARRIED — owner+expiry** (see Residuals #3, #6, #7) |
| **R6** | R-006 | **Cross-process writers — two REST processes → one `HELIX_URL`** (RL-001 out-of-contract) | Low | Low | One Helix per slot by construction (`derive(2) → 3114/6970 slot2` vs `dev 3111/6969` disjoint per §4.2 invariant `N≥2 ⇒ quartet ∩ {3111,3112,3113,6969}=∅`); `doctor` reports `HELIX_URL ↔ quartet` mismatches (C1/C3). Schedule P4.3 re-review before any shared-instance deployment. | `SPEC-P4-OPS.md:307-309,R6`, `bin/agent-memory.mjs:65-72,102-113` derive + `1189-1257` doctor C1/C3, `TEST_MATRIX.md:119,132` bar #10 slot2 window `3114/6970` single Helix | Inherent while RL-001 stays single-writer per instance; widening to multi-writer needs new lane + re-review. | R1 | **MITIGATED by construction; CARRIED for multi-writer** (see Residual #3) |
| **R7** | R-007 | **Slot 14 reserved port is `3151` (documented reroute `src/server.ts:503`)** | Low | Low | Reserved ports never bound, never signaled; derivation `R(N)/H(N)` provably never yields `3151` as REST/Helix role (deterministic check); `doctor` labels `3151` occupancy as `documented reroute port`; harness never binds `3111/3112/3113/3151/6969` per `NEVER_BIND`. | `SPEC-P4-OPS.md:310-312,215-219`, `bin/agent-memory.mjs:76-77` `NEVER_BIND` + `:797-802` derivation defense-in-depth, `scripts/verify-ops.ts` §F `never 3151 N=1..20` | None — address-space reservation only, no bind conflict. | R1 | **MITIGATED** |

### Assumptions A1..A6 — disposition

| ID | Assumption | Disposition | Evidence | Residual |
|----|------------|-------------|----------|----------|
| **A1** | Quartet formula `R(N)=3111+3(N−1)`, `H(N)=6969+(N−1)` is only reading reproducing brief §3.2 slot-1 `3111+6969+2 reservados` | **Accepted by orchestrator 2026-09-24**; invariant INV-010 in ARCHITECTURE — zero `src/**`/`db/**` edits, `git diff --stat src/ db/` empty | `SPEC-P4-OPS.md:299-302`, `ARCHITECTURE.md:148-150`, `bin/agent-memory.mjs:61-72,102-113`, `scripts/verify-ops.ts:250-254` §F slots 1–3 exact + `--slot 0/abc → exit 2` | None |
| **A2** | `helix start --port`, `helix add local --name/--port`, `helix stop/status [INSTANCE]` CLI surface | **Verified** against CLI 3.3.0 `--help` this session | `SPEC-P4-OPS.md:303-304`, `IMPLEMENTATION_PLAN.md:30-35` | None |
| **A3** | `helix start` forwards `HELIX_DATA_DIR` into container | **Falsifiable, probed step 0 — FAIL** — see R2 above. No `--data-dir` flag, binary 0 occurrences, `docker inspect` no passthrough, skill direct-Docker mode `HELIX_DATA_DIR` mutually exclusive with `S3_BUCKET`. Consequence: framing **3b** escalated, `--migrate` abort-only. | `SPEC-P4-OPS.md:305-311`, `IMPLEMENTATION_PLAN.md:24-44`, `bin/agent-memory.mjs:892,1308-1325` | Carried Residual #1 |
| **A4** | Instance naming `slotN`; slot 1 reuses `dev` | **Frozen** as code invariant | `SPEC-P4-OPS.md:312`, `bin/agent-memory.mjs:102-113,1026-1039` `instanceBinding(slot)` re-derives `dev`/`slotN` from `helix.toml [local.*]` | None |
| **A5** | `helix add local` appending `[local.slotN]` is sanctioned config registration, not source edit | **Frozen** — `[local.dev]` frozen, `[local.slot2]` additive only via official CLI + `storage="disk"` patch | `SPEC-P4-OPS.md:312-314`, `helix.toml:6-16`, `bin/agent-memory.mjs:838-862` | Residual #5 low |
| **A6** | CLI binary name `agent-memory` (hyphen) differs from upstream `agentmemory` to avoid PATH confusion | **Frozen** — `bin/agent-memory.mjs` + `package.json bin` entry | `SPEC-P4-OPS.md:314-316`, `package.json:14-15` | None |

## Residual Risks (explicit, carried — every High/Medium has owner + expiry)

| # | Carries | Risk | Likelihood × Impact | Owner | Expiry / re-review trigger | Declared in | Mitigation carried forward |
|---|---------|------|---------------------|-------|----------------------------|-------------|----------------------------|
| 1 | **R2 / A3 FAIL** | `HELIX_DATA_DIR` forwarding unsupported on Helix CLI 3.3.0 — `doctor --migrate` is **abort-only** (`MIGRATE ABORT: unsupported-runtime`), `HELIX_DATA_DIR` never set for children, KR3 not claimed; framing **3b** (data-dir for new instances only) pending orchestrator decision. If A3 flips to PASS, migration write path (backup 0600/0700, copy, verify counts, `docker volume ls` unchanged) needs **new probe + new J/K/I + backup-mode stat evidence** before claiming KR3. | Medium × **High** | R1 `general(vasquez)` + R8 `general(espinoza)`, **escalated to orchestrator** | **2026-12-31 or framing-3b decision, whichever first** (same as `security-reviewer.md` #4 / `resilience.md` #2 / `automation-reviewer.md` #2) | `IMPLEMENTATION_PLAN.md` §Step 0, `bin/agent-memory.mjs:25-37,892,1308-1325`, `TEST_MATRIX.md:109-110` DONE* partial | Fail-closed abort with `MinIO volume retained` hint + `--backup-dir` path-refused before any write; zero residue by construction |
| 2 | **R3** | **Helix bind-mount `0700`/`0600` not yet exercised in vivo** — state-file `0700`/`0600` proven by `stat` in harness §G; Helix `HELIX_DATA_DIR` bind-mount for uid `65532` deferred while A3 FAIL. `start` fails closed with actionable permission error (never starts data-less). | Medium × Medium | R1 `general(vasquez)` | **Re-verify `stat 0600`/`0700` on first live Helix data-dir mount after A3 enablement**; TEST_MATRIX must then show `stat` evidence per C2 (see `security-reviewer.md` #5) | `bin/agent-memory.mjs:303-310,356-364` umask-independent modes |
| 3 | **R5 / R6 / RL-001 survivor per ROADMAP** | **RL-001 surviving boundary** — CLOSED 2026-09-24 fix (per-survivor FIFO + fresh `getMemoryById` re-read) but boundary still **single-process writers only; cross-process writers to one Helix instance stay out of contract until P4.3**. | Low × High | engineering (R1) | **2026-12-31 or the start of P4.3 multi-instance work, whichever first** — `ROADMAP.md:68` original trigger kept | `ROADMAP.md:68` `RL-001 / F-02 / RK-001 — CLOSED` row + `docs/CONTRACT.md` §3 tier-1 (a) v1.5 + README *Known limitations* #8 | One Helix per slot by construction; `doctor` reports `HELIX_URL` ↔ quartet mismatches |
| 4 | **PID-reuse TOCTOU** (cross-ref R-011) | **PID-reuse TOCTOU between `verifyOwnedPid` and `SIGTERM`/`SIGKILL`** — narrowed to ms by cmdline re-verification (alive + `src/server.ts` in cmdline + cwd==ROOT) before SIGTERM (`bin/agent-memory.mjs:1049`) and again before SIGKILL (C10 at `:1060-1063`); window not eliminable without OS `pidfd`/`start-time` pinning. Blast radius local single-user. | Low × High | R1 `general(vasquez)` | **Re-review if PID namespace changes or C10 weakened**; optional hardening: record `start-time` at spawn and compare `/proc/<pid>/stat` (`SECURITY_REVIEW.md` C10) | `bin/agent-memory.mjs:452-459,1049,1060-1063`, `security-reviewer.md` #1, `reliability.md` REL-OBS-02 |
| 5 | **A5 / R8-OBS-02 / RES-OBS-01** | **Additive `[local.slot2]` remains in `helix.toml`** after lane close until `git checkout -- helix.toml` — inert without CLI (no auto-start, no port bind unless `helix start slot2` invoked). | Low × Low | R8 `general(espinoza)` | **Re-verify on next P4 lane that touches slots** (see `resilience.md` #3 / `automation-reviewer.md` #3) | `helix.toml:12-16` additive sanctioned block; rollback `IMPLEMENTATION_PLAN.md:75` |
| 6 | **ROADMAP RL-001-QUEUE** | **Nested FIFO lock queue has no cap and no request deadline** — per-send 15 s `withTimeout` × ≤11 sequential sends worst-heal → ≤165 s held in-lock (6 sends happy; ~2× happy / ~3–4× worst vs pre-RL-001). | Medium × Medium | engineering (R1) | **P4.3 multi-instance OR first observed retry storm, whichever first** — code fix (queue cap / request deadline) requires separate proposal lane. Also bounded by global **2026-12-31** re-review heartbeat for all queue/session residuals. | `ROADMAP.md:72` `RL-001-QUEUE` row + `docs/CONTRACT.md` §3 lock-queue wait envelope + `PROPOSED_CHANGES.md` R-010,R-012 | Accepted envelope declared in `docs/CONTRACT.md` §3; exclusive windows + max 2 parallel lanes mitigate this lane |
| 7 | **ROADMAP VERIFY-SESSION-NODES** | **`scripts/verify.ts` leaves +17 Session nodes per run** (fresh uuid test sessions materialize on novel writes; rows self-clean via `forget`, no Session deletion path exists — measured 490→507→524, 2026-09-24). Accumulation unbounded until reset. | Medium × Low | engineering (R1) | **P4.1 session-deletion work or 2026-12-31, whichever first** — until then operator resets dev instance / prunes `verify*` sessions when hygiene matters. Global heartbeat **2026-12-31** carries both #6 and #7 even if P4 lane slips. | `ROADMAP.md:73` `VERIFY-SESSION-NODES` row + `docs/CONTRACT.md` §5 session-node run budget + `TEST_MATRIX.md:77-89` RK-01 `+17/run` | Declared in `docs/CONTRACT.md` §5; `verify` 243 green post-declaration proves functional isolation |
| 8 | **R1 latent** | **Wrong-slot operator `--apply --yes`** — dry-run prints per-slot target, but `--apply` against wrong slot remains operator error. No code guard can distinguish intent; bounded by `--yes` confirmation gate. | Low × High | R1+R8 | **Permanent for this CLI** — re-review if `--migrate` gains a second confirmation or slot-affinity check | `SPEC-P4-OPS.md:87-94,294-295`, `bin/agent-memory.mjs:251-253` `--apply`+`--yes` gate |

No residual carries `Critical`; no residual is silently PASSED; every row above appears verbatim in `ROADMAP.md` §1.3 or `TEST_MATRIX.md` ## P4 OPS `*Notes:` and is owned.

## Findings (this gate review)

| ID | Severity | Finding | Location | Evidence | Owner | Remediation |
|----|----------|---------|----------|----------|-------|-------------|
| — | — | **No Medium/High/Critical.** All 7 spec risks land as code or fail-closed abort with line citations above; 243/99/21/123/137/73 bars pasted; `good` invariants hold; no forbidden primitive reachable. | §§ Risk Matrix, Assumptions, Residuals | cited | — | — |
| RISK-OBS-01 | Low (observation) | `T-P4OPS-09` Commit column shows `"(this commit)"` — expected for the lane-closing docs merge commit `974f5c4`; hash resolves on merge (same as QA O-01 / R8-OBS-01). Not a trace gap. | `TEST_MATRIX.md:111` T-P4OPS-09 | `git log --oneline -5` → `974f5c4 docs(p4-ops)` | orchestrator (merge owner) | merge resolves hash |
| RISK-OBS-02 | Low (observation) | Backup Ley 172-13 declaration (`README.md:715-727`) is **docs-only while A3 is FAIL** — no archive created to exercise `0600`/`0700` in vivo; abort path proves *never-a-write* instead (same as `security-reviewer.md` #5). | `README.md:715-727`, `bin/agent-memory.mjs:1308-1325` | `scripts/verify-ops.ts:202-216` `"no backup file in repo root"` + abort hint | R8 `general(espinoza)` | Re-verify `stat 0600`/`0700` on first live backup after A3 enablement |

No findings block the gate. Security #4/#5, resilience #2/#3, automation #2/#3 residuals are co-owned and share the same expiry — not duplicated here.

## Verdict Rationale

- **R1 (High irreversible) is mitigated by construction and proven abort-gated while A3 FAIL:** dry-run default, verified `0600` backup in `0700` parent, fail-closed abort before any copy, MinIO volume never destroyed — and while A3 is FAIL, the only reachable `--migrate` path aborts with `MIGRATE ABORT: unsupported-runtime` and `MinIO volume retained` before any source read (`bin/agent-memory.mjs:1308-1322`). No data moved, zero residue.
- **R2 (High A3) is the declared reversible call:** probed before any code (binary 0 hits, `--help` no flag, `docker inspect` no passthrough, skill direct-Docker mode), recorded as FAIL, STOPPED P4.4 forwarding/migration, escalated to orchestrator (framing 3b), and carried as Residual #1 with **owner R1+R8 and expiry 2026-12-31 or framing-3b decision**.
- **R3 (uid 65532 `0700`/`0600`) landed for the exercised path:** `ensureDir0700` + `writeState 0600` umask-independent, `resolveDataDir` / `--backup-dir` refusal sets, harness `stat` proof for state-file; Helix bind-mount mode proof correctly deferred to Residual #2 with re-verify trigger on first live mount — not silently passed.
- **R5 (RL-001 re-review) and R6 (cross-process) are not waived by this lane:** both flag the `2026-12-31 or start of P4.3` heartbeat (`ROADMAP.md:68`) and the `one Helix per slot` construction; `doctor` mismatch reporting and exclusive `3114/6970` window vs `dev 3111/6969` read-only keep the lane in contract.
- **Queue/session `2026-12-31` heartbeat is explicit:** `RL-001-QUEUE` (expiry P4.3 or storm) and `VERIFY-SESSION-NODES +17/run` (expiry P4.1 or 2026-12-31) are both tabled as Residuals #6/#7 with the global `2026-12-31` re-review date carried verbatim from `ROADMAP.md:72-73` and `docs/CONTRACT.md` §5 — no silent expiry.
- **No silent PASS, no third loop:** every `DONE* partial` is an honest partial (A3 FAIL), every abort path pastes counts (`verify-ops 99`, `verify 243`, `typecheck 0`, `bootstrap 8 indexes`, `git diff src/db empty`), and every residual names an owner and a dated trigger.

## Sign-off

- [x] **risk reviewer (engineering, R1 — risk matrix, residual risks, expiry):** **PASS**. R1 dry-run+backup+never-destroy landed and proven via abort path; R2 A3 FAIL probed before code and escalated to framing 3b with `MIGRATE ABORT: unsupported-runtime` and `MinIO volume retained`; R3 `0700`/`0600` landed for state-path and carried for Helix mount; R4 `--persist` absent; R5 RL-001 re-review flagged not waived; R6 one-Helix-per-slot by construction; R7 `3151` never derived and labeled. All residuals carry owner+expiry (queue/session `2026-12-31`). Zero unmitigated High/Critical. This review touched only this file — no implementation file modified.
