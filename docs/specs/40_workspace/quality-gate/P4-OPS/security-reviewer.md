# Security Review: SPEC-P4-OPS — P4 ops control plane

**Reviewer:** security-reviewer (R2, `general(barrera)` — Security Owner, quality-gate)
**Date:** 2026-09-24
**Packet (reference-only):** `SPEC:docs/specs/20_backlog/SPEC-P4-OPS.md#NFR-B,F+REQ-05,08 / HARD:never kill upstream, Ley 172-13, secret hygiene / GATE:implementation done / DOMAINS:R2 security`
**Inputs read:** `docs/specs/40_workspace/engineering/SECURITY_REVIEW.md` (C1..C10, S-001..014), `bin/agent-memory.mjs`, `scripts/verify-ops.ts` (§I,J,K + header proof), `TEST_MATRIX.md` (J/K/I evidence), `docs/specs/20_backlog/SPEC-P4-OPS.md`, `README.md` (ops/§Backup)
**Verdict:** **PASS** — all 10 conditions C1..C10 landed as code and evidenced; zero open High. No gate-blocking finding.

## Checklist (per `frame-ship/skills/quality-gate/references/domains/security-review.md`)

- [x] Threat model complete (STRIDE) — `SECURITY_REVIEW.md` STRIDE table re-verified against landed code; spoofing/tampering/repudiation/info-disclosure/DoS/elevation each have a code control + harness proof (see C1..C10)
- [x] AuthN/AuthZ verified — bearer gating via shared `verifyOwnedPid()` before any `Authorization` header; `status` never sends a bearer (C1/C9)
- [x] Input validation at all boundaries — `--slot` fail-closed, `isValidState()` closed schema, `SYSTEM_ROOTS` + `HOME` + `$HOME|/tmp` refusal set (C3/C4)
- [x] Secrets not in code — no secret values in code/output/state; `helixEnv()` strips secret from all helix children, only server inherits it; header proof 0 Authorization to foreign (C1/C8)
- [x] Dependencies scanned — zero new deps (`node:` builtins only), `package-lock.json` untouched, static forbidden-primitive grep clean (C8/C10/I)
- [x] Data handling compliant (PII, retention, Ley 172-13) — state file secret-free/PII-free, no memory content in outputs (canary 0), backup PII-store declared in README and never created while A3 FAIL (abort path); `~`-collapsed paths only (C2/C3/C7/K)
- [x] Audit logging in place — `appendAudit()` allowlisted line on every `stop` and `migrate --apply` (including abort), store `state/audit.log` 0600/0700 (C6)

## Conditions C1..C10 — implemented as code and evidenced

| Cond | Pre-exec requirement (SECURITY_REVIEW.md) | Landed code | Evidence |
|------|-------------------------------------------|-------------|----------|
| **C1** (S-001 High) | Ownership-gate every authenticated probe: `Authorization` only after listener PID proven slot-owned (`verifyOwnedPid()`), otherwise no header, FAIL via C3 | `bin/agent-memory.mjs:1197-1214` C3 computes `restOwned` via `verifyOwnedPid(state.state.pids.rest)` + `isDescendant` check; `bin/agent-memory.mjs:1260-1265` C2 gated: `if (!restOwned) { emit INFO skipped (NO request sent, no bearer transmitted) } else { headers = secret ? Bearer : {} ; httpStatus(/memory/health, {headers}) }` — header construction lives inside the owned branch only; `bin/agent-memory.mjs:452-459` shared `verifyOwnedPid()` (alive + `src/server.ts` in cmdline + cwd==ROOT) | `scripts/verify-ops.ts:265-287` header proof: synthetic HTTP server on slot 9 REST (`3135`) receives `requestCount` with `authHeaders.length===0`; `TEST_MATRIX.md:124` bar #2 `C1 header proof: synthetic server 0 Authorization headers`; harness `sectionHeaderProof` checks `0 Authorization headers` + `no Authorization header contains secret` + `INFO skipped / NO request sent` — **PASS**. Also `bin/agent-memory.mjs:1143-1144` `status` fetches `/memory/health` with no headers ever (C9 complement) |
| **C2** (S-002 High) | Backup archive `0600` / parent `0700` (umask-independent), Ley 172-13 declaration (purpose+TTL+deletion+`~`-collapsed print), `--backup-dir` validated per C4 | A3 FAIL path supersedes the write path: `bin/agent-memory.mjs:1308-1325` — any `--migrate` sets `failures.checkFailed=true`, prints `MIGRATE ABORT: unsupported-runtime — probe A3 failed` (`oneLine`+`never-KILL hint`), validates `--backup-dir` (`SYSTEM_ROOTS`, outside `$HOME && /tmp`, parent missing → `path-refused`), **never creates an archive, never copies, never touches target**; abort `HINT: no source was read, no backup was written, no target was touched; MinIO volume retained`. When migration is enabled, the declared posture is `README.md:715-727` table: `0600` archive / `0700` parent + Ley declaration rows (purpose=DR of dev memory data, location=`~`-collapsed, TTL=deleted after verified migration+operator confirmation, deletion=`rm -rf <backup-path>`, no content logged) | `scripts/verify-ops.ts:202-216` §H: `doctor --migrate` → `MIGRATE ABORT` + `unsupported-runtime` + `no backup/MinIO`, `--migrate --apply --yes` also aborts, `no backup file in repo root`; `TEST_MATRIX.md:101/110` T-P4OPS-08 `DONE* partial — abort path proven; no data moved per A3`; `README.md:715-727` declaration anchor — **PASS via abort (no PII store created)** |
| **C3** (S-003,S-012) | State hardening: `state/` `0700`, `slot-N.json` `0600` umask-independent, closed-schema validate fail-closed no signal, `helixInstance` re-derived from `helix.toml [local.*]`, `dataDir` informational, one shared `verifyOwnedPid()` | `bin/agent-memory.mjs:303-310` `ensureDir0700` (`mkdir 0700` + `chmod 0700`); `bin/agent-memory.mjs:356-364` `writeState` (`writeFile 0600` + `chmod 0600`); `bin/agent-memory.mjs:337-354` `isValidState` closed schema (`Object.keys` sort `cliVersion,dataDir,helixInstance,pids,slot,startedAt` + `pids: helix,rest` only, unknown/missing→false); `bin/agent-memory.mjs:326-335` `readState` → `invalid` on parse or schema fail; `bin/agent-memory.mjs:452-459` shared `verifyOwnedPid()` used by `stop` pre-SIGTERM (`1049`), pre-SIGKILL (`1060`) and `doctor` C3 (`1209`); `bin/agent-memory.mjs:1026-1039` `stop` re-derives `instanceBinding(slot)` from `helix.toml` and refuses on `state.helixInstance !== binding.name` or unregistered | `scripts/verify-ops.ts:177-201` §G: `state path sibling outside HELIX_DATA_DIR`, `state/.../slot-N.json`, mode `0600` + dir `0700`, secret-free, no secret field; `TEST_MATRIX.md:124` bar #2 §G green; `bin/agent-memory.mjs:767-784` `start` `invalid`→REFUSE no signal, `ok+verifyOwnedPid`→`already running` else stale removed — **PASS** |
| **C4** (S-004) | Path validation: `start` refuses non-empty non-Helix dir, refuses `/`, `$HOME`, system roots; `--backup-dir` parent must exist+writable, fixed archive pattern | `bin/agent-memory.mjs:79-100` `SYSTEM_ROOTS` + `HOME` constants; `bin/agent-memory.mjs:274-288` `resolveDataDir` → `system root` / `home directory itself` / `outside $HOME and /tmp` refusal reasons; `bin/agent-memory.mjs:727-757` `start` checks `!stat.isDirectory()`→refuse, `readdir` non-empty→`never adopting a foreign directory` refuse, `/`/`HOME`/system root already refused via `resolveDataDir`; `bin/agent-memory.mjs:1312-1318` `--backup-dir` validate `SYSTEM_ROOTS`, `outside $HOME and /tmp`, `parent missing` | `scripts/verify-ops.ts:177-201` §G `--data-dir` reflected, env reflected, `state outside data dir`; §H `path-refused` signals; manual code read of both refusal sets — **PASS** |
| **C5** (S-005) | `start` idempotence: live cmdline-verified state → `already running` (exit 0, never overwrite live PIDs); stale → clean then proceed | `bin/agent-memory.mjs:766-785` — `invalid`→exit 1 no signal; `ok && verifyOwnedPid(rest)`→`already running` + `pids: rest=` + `state: ~` exit 0 (no overwrite); stale→`unlinkSync` + `stale record removed` else exit 1 if not removable | `scripts/verify-ops.ts:94-122` §B/C `already running` semantics + `verify-ops` §B foreign still alive; live bar `TEST_MATRIX.md:132` `start --slot 2` exit 0 then second window still idempotent; **PASS** |
| **C6** (S-006) | Governance/audit lines for `stop` and `--migrate --apply` (allowlisted fields only, oneLine, before every non-zero exit including `MIGRATE ABORT`), store=stdout+`state/audit.log` declared like `CONTRACT.md:479-495` | `bin/agent-memory.mjs:369-391` `appendAudit(dataDir,slot,action,status,exitCode)` → `state/audit.log` `0600` in `0700` dir, line `${ISO} slot=${slot} action=${action} status=${status} exit=${exitCode}\n` (single line, `oneLine`-safe fields, no secret/PII/username); called on `stop` invalid (`1018`), unregistered (`1030`), mismatch (`1037`), stale-pid (`1052`, `1062`), helix-missing (`1081`), and final `1106`; on `migrate --apply` abort (`1323`) | `scripts/verify-ops.ts:202-216` §H abort line + §G `state` checks; code grep confirms every non-zero `stop`/`migrate-apply` path hits `appendAudit` before return — **PASS** |
| **C7** (S-007,S-013) | One allowlist renderer for all doctor/status/migrate text: child output captured and re-rendered (never raw), every error `oneLine`+`$HOME→~`, no raw `Error`/env dumps (§4b) | `bin/agent-memory.mjs:119-147` `oneLine()` (collapse `\s+`, trim, max 400) + `collapse()` (`$HOME→~`) + `out()`/`err()` + `neverKillHint()`; `bin/agent-memory.mjs:640-656` `runHelix` → `spawnSync` `stdio pipe` then `found/code` only, child stdout/stderr captured and dropped (never replayed); all error sites use `oneLine(err.code/message,200)` + `collapse(path)` e.g. `902`, `956`, `1375`; `bin/agent-memory.mjs:958` readiness HINT prints `~`-collapsed log path only | `scripts/verify-ops.ts:250-254` §K canary 0 + §J secret 0 prove no raw memory/secret passthrough; `TEST_MATRIX.md:124` J/K green; **PASS** |
| **C8** (S-008,S-011) | Env minimization + binary resolution: `AGENT_MEMORY_SECRET` stripped from every child except `npx tsx src/server.ts`; absolute-path resolution of `helix`/`npx` where feasible, else PATH-trust declared | `bin/agent-memory.mjs:598-635` `resolveSibling`/`resolveOnPath`/`resolveNpx`/`resolveHelix` → absolute `npx`/`helix` pinned; `bin/agent-memory.mjs:627-635` `helixEnv()` strips `AGENT_MEMORY_SECRET`; `bin/agent-memory.mjs:647` `runHelix` uses `helixEnv()`; `bin/agent-memory.mjs:885-893` `serverEnv` (`...process.env` + derived `AGENT_MEMORY_PORT/HOST/URL/HELIX_URL/AGENT_MEMORY_DATA_DIR`) is the **only** spawn that inherits the secret (via `spawn npx tsx src/server.ts` at `905-909`) | `scripts/verify-ops.ts:233-249` §J `secret VALUE 0 occurrences` in stdout+stderr of all four subcommands + state file, `bearer: armed` only; `TEST_MATRIX.md:124` J green; `bin/agent-memory.mjs:598-625` absolute resolution — **PASS** |
| **C9** (S-009,S-014) | Runbook §4c migration lifecycle (target Helix stopped during copy, started for verify); `REQ-OPS-RUN-04` as `doctor without --migrate --apply --yes is read-only`; `status`/bearer row reconciled with C1 gate | `bin/agent-memory.mjs:1110-1160` `cmdStatus` — no `Authorization` header ever constructed; comment `No Authorization header, ever: 401==armed+serving` at `1143`; probe `/memory/health` naked, `healthStatus===401`→`armed` (`1151-1153`), `restUp = livez 200 || health 200 || health 401`; `bin/agent-memory.mjs:1162-1172` `cmdDoctor` header comment `C2 runs ONLY after C3 proves slot-owned; foreign receives NO request`; `SPEC-P4-OPS.md:216`/`:66-83` already scopes §4c as `unsupported-runtime` while A3 FAIL (no lifecycle to declare beyond abort), `REQ-04` status row now `presence only, never attaches Authorization, 401=armed` (E-3 resolved) | `scripts/verify-ops.ts:263-287` header proof `0 Authorization`; `TEST_MATRIX.md:106` T-P4OPS-04 `presence-only, never Authorization header; 401=armed`; **PASS** |
| **C10** (S-010) | Re-verify PID cmdline immediately before SIGKILL; mismatch → no signal, allowlisted `stale-pid` note, exit 1 fail-closed (start-time hardening recommended) | `bin/agent-memory.mjs:1044-1074` `stop` — `verifyOwnedPid(pid)` before SIGTERM (`1049`), `signalOwned SIGTERM`, `waitUntilDead(TERM_GRACE_MS)`, then **C10** `if (!verifyOwnedPid(pid)) { stale-pid note (identity changed during grace — SIGKILL skipped) + appendAudit refuse-stale-pid-kill + return 1 }` at `1060-1063`, else `SIGKILL` + `waitUntilDead(KILL_WAIT_MS)` | `scripts/verify-ops.ts:109-122` §C/`§I` `stale-pid` path + `verify-ops` §I/C foreign-survives proofs; code read of both `verifyOwnedPid` call sites — **PASS** |

## J/K/I evidence (TEST_MATRIX harness green)

| Check | Spec | Harness | Result |
|-------|------|---------|--------|
| **J — secret 0 occurrences** `NFR-B` | `SPEC-P4-OPS.md:129` never prints value; `AC-P4-OPS-B` 0 occurrences in all four subcommands + state file | `scripts/verify-ops.ts:233-249` §J synthetic `TEST_SECRET=synthetic-verify-ops-<uuid>`: `status`/`doctor`/`start`/`stop` stdout+stderr `!includes(TEST_SECRET)`, `bearer: armed` present, state file `!includes(secret)` | **PASS** — `TEST_MATRIX.md:124` bar #2 `secret 0 occurrences` green, 99/0 total |
| **K — canary 0** `NFR-F` Ley 172-13 | `SPEC-P4-OPS.md:141` / `AC-P4-OPS-F` canary 0 in `doctor`/`status`/`--migrate` | `scripts/verify-ops.ts:250-254` §K `canary-verify-ops-<uuid>` 0 in `doctor`/`status`/`migrate` outputs | **PASS** — bar #2 green |
| **I — static forbidden primitives** `NFR-A` | `SPEC-P4-OPS.md:125`/`§4.5` never-kill: no `prune`/`delete`/`docker rm|kill|volume rm`/`fuser`/`--persist` | `scripts/verify-ops.ts:217-232` §I live foreign on stand-in quartet survives all four subcommands + static grep `helix prune`/`helix delete`/`docker volume rm`/`docker rm`/`fuser`/`pkill`/`killall` absent, `--persist` ≤2 doc-only; `bin/agent-memory.mjs` grep confirms 0 hits for those needles outside comments | **PASS** — bar #2 I green; `git diff src/ db/` empty, `package-lock.json` empty |

## Findings (this gate review)

| ID | Severity | Finding | Evidence | Remediation |
|----|----------|---------|----------|-------------|
| — | — | **No new High/Critical.** All 14 prior findings S-001..014 map to C1..C10 above and are closed as code. | `SECURITY_REVIEW.md:73-88` vs `bin/agent-memory.mjs` citations in C1..C10 + `scripts/verify-ops.ts` §J/K/I + header proof | — |
| S-011 residual | **Low** | PATH-resolved `helix`/`npx` are hijackable by a malicious earlier `PATH` entry (local same-user model). Code now pins absolute paths (`resolveNpx`/`resolveHelix`+`helixEnv` strip) where feasible; remaining PATH trust is an accepted residual, not an open finding. | `bin/agent-memory.mjs:598-625` absolute resolution; `helixEnv:627` strip | Accepted — see Residual Risk #2 |

No Medium/High findings remain open. Gate is not failed on security grounds.

## Verdict Rationale

All 10 pre-execution conditions are **implemented as code** and **evidenced by the harness**:

- The two **High** blockers are cleared: **C1** bearer-to-foreign is eliminated by construction (C3 `restOwned` gate encloses the only `Authorization` header site, proven by the synthetic foreign listener capturing **0 `Authorization` headers**); **C2** archive PII-store is not created at all (A3 FAIL → `MIGRATE ABORT: unsupported-runtime`, validated by §H `no backup file` proof and the `README.md:715-727` Ley declaration for the future enabled path).
- **C3–C10** each have a direct `bin/agent-memory.mjs:line` control and a harness `scripts/verify-ops.ts:line` check that would fail on regression (secret leak, foreign-kill, forged state, path escape, missing re-verify, raw child passthrough, env inheritance to `helix`, `status` bearer, SIGKILL without re-verify).
- **J/K/I** (secret 0, canary 0, forbidden-primitive static grep) are green in the published 99/0 `verify-ops` bar and were re-confirmed by source grep in this review. `status` never sends a bearer (`401=armed`), `doctor` C2→C1 ordering is `C1→C3→C2→C4→C5` with `INFO skipped` on foreign, state modes are `0700`/`0600` umask-independent, audit lines cover every non-zero `stop`/`migrate --apply` path, and `oneLine`+`collapse` (`~`) is the sole renderer.

Threat model (STRIDE) remains fully covered; no new attack surface was introduced (zero new deps, `src/`/`db/` frozen, `helix.toml` only additive `[local.slot2]`).

## Residual Risk (accepted, owner + expiry)

| # | Risk | Owner | Expiry / re-review |
|---|------|-------|---------------------|
| 1 | **PID-reuse TOCTOU between `verifyOwnedPid` and `SIGTERM`/`SIGKILL`** — narrowed to milliseconds by cmdline re-verification (and C10 re-verify before SIGKILL). The window cannot be eliminated without OS-level `pidfd`/`start-time` pinning. Blast radius is local single-user; no cross-tenant data. | R1 `general(vasquez)` | Re-review if PID namespace changes or if C10 is weakened; optional hardening: record `start-time` at spawn and compare via `/proc/<pid>/stat` when available (recommended in `SECURITY_REVIEW.md` C10) |
| 2 | **PATH as trust input for `helix`/`npx` resolution** — mitigated by absolute-path pinning (`resolveOnPath` + `resolveSibling`) and `helixEnv()` secret stripping; residual is the local same-user threat model (attacker already running as operator can read env directly). | R1 | Re-review before any packaging that puts `helix` on a shared writable `PATH`; README ops PATH note is the declared control |
| 3 | **Local same-user threat model** — `state/` and `~/.local/share/agent-memory/` are forgeable by any process running as the operator; defenses are intra-user tamper-resistance (closed schema, `0600`/`0700`, `verifyOwnedPid`, `helixInstance` re-derive) and cross-user disclosure mitigation (`0600`/`0700`). No customer/multi-tenant surface, no external send. | R2 | Permanent for this lane; re-review if deployment leaves single-user localhost (would require new DPIA + network AuthZ) |
| 4 | **A3 FAIL — `HELIX_DATA_DIR` not forwarded, `--migrate` abort-only** — no PII-store migration is performed; `HELIX_DATA_DIR` never set for children (`serverEnv` comment `bin/agent-memory.mjs:892`). Framing **3b** (brief §7) is an orchestrator decision, not an in-lane improvisation. If A3 flips to PASS, the backup-persistence write path (REQ-07/08/NFR-D) requires a new probe + new J/K/I + backup-mode evidence before claiming KR3. | R1+R8, escalated to orchestrator | **2026-12-31** or at framing-3b decision — whichever first (carries `ROADMAP.md:72` RL-001-QUEUE + `VERIFY-SESSION-NODES` pattern) |
| 5 | **Backup Ley declaration is docs-only while A3 is FAIL** — `README.md:715-727` declares the archive store (purpose/TTL/deletion/`~`-collapsed, `0600`/`0700`, never-logged content) but no archive is created to exercise the mode bits in vivo; the abort path proves never-a-write instead. | R8 `general(espinoza)` | Re-verify file modes (`stat 0600`/`0700`) on the first live backup after A3 enablement; `TEST_MATRIX.md` must then show `stat` evidence per C2 |

## Sign-off

- [x] **security owner (R2) — `general(barrera)`:** **PASS**. C1..C10 implemented as code (citations above) and evidenced by `scripts/verify-ops.ts` header proof + §J/K/I + live slot-2 window; no High remains open; residual risks explicit with owner+expiry; never-kill, zero-new-deps, Ley 172-13, and secret-hygiene HARD rules intact. No third retry loop; no sideways fix — owners remediate per row if re-opened.
